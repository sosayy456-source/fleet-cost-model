# CHANGELOG — เว็บนำเสนอ (`presentation/`)

รูปแบบ: ใหม่สุดอยู่บน · ทุกรายการบอก **ใคร · ทำอะไร · ไฟล์ไหน · ค้างอะไร**
หัวข้อ "รอเจ้าของงานตัดสิน" เก็บคำถาม/สิ่งที่ต้องแก้นอกโฟลเดอร์ — ห้ามลงมือจนกว่าเจ้าของงานตอบ

---

## รอเจ้าของงานตัดสิน

- Q1–Q6 ใน `PRESENTATION_BRIEF.md` ข้อ 9 (ตัวเลขจริง · ผู้ชม/เวลา · ขึ้น Pages ไหม · ฉากที่ต้องมี · ข้อมูลหน้าปก · ลิงก์เดโม)
  — ตอบก่อน Phase 3 · P0–P2 ใช้ค่าตั้งต้นในตารางไปก่อนได้

---

## 2569-09-26 · Claude · `?tour` รอบ 2 — จัดเป็น pitch (เจ้าของงาน: ~2 นาที · เปิด + 5 ฝ่าย · ตัวเลขจากคลิป · ไม่มีเพลง)

- โครง: **หน้าเริ่ม → เปิดเรื่อง 3 สไลด์ตามเวลา** (คำถาม "เที่ยวไหนกำไร?" · ปัญหาเดิม 3 ข้อ · ทางออก + flow 5 ฝ่าย) →
  **[การ์ดคั่นฝ่ายเต็มเวที 2.2 วิ (เลข · ชื่อ · คุณค่า 1 บรรทัด · เดิม → ตอนนี้) → วิดีโอของฝ่ายนั้น] × 5** → หน้าปิด "ขอบคุณครับ · Q&A" · รวม ~1:52
- **ตัดฝ่ายผู้จัดการออก** (ตามที่ขอ 5 ฝ่าย · คลิปช่วงนั้นเลขเป็น 0 เกือบหมด) · วิดีโอเหลือ 83 วิ (WebM 12.3 MB / MP4 17.5 MB)
- กำกับภาพ: ทุกช่วงมี `focus` ซูมด้วย transform-origin + scale (x คำนวณให้ขอบซ้ายของเนื้อหาไม่หลุดจอ) · **การ์ดตัวเลขลอย** 9 ช่วง
  (Load Factor 13%→35% · กำไรพยากรณ์ 2,334 · กำไรจริง −11,093.50 · ต้นทุนที่จม 24,110,369 / LF 58.9% · PI 56/100 ฯลฯ — อ่านจากคลิปทั้งหมด) ·
  จอโน้ตบุ๊กเอียง 14° ระหว่างการ์ดคั่นแล้วตั้งตรง · ปุ่ม 1–5 ข้ามฝ่าย (การ์ดคั่นขึ้นใหม่)
- บั๊กที่แก้ระหว่างทาง: ตัวจับเวลาของสไลด์เปิดกับการ์ดคั่นใช้ตัวเดียวกัน → cleanup ตอนออกจากสไลด์ยกเลิกเวลาการ์ด วิดีโอค้าง (แยกตัวจับเวลาแล้ว) ·
  ซูมตัดกล่องตัวเลขฝั่งซ้าย (ปรับจุดยึดทุกช่วง) · ตัวเลขในการ์ดลอยตัดบรรทัด (nowrap)
- ตรวจในเบราว์เซอร์: ครบทุก phase · วิดีโอเล่นต่อหลังการ์ด · ถึงหน้าปิด · ไม่มี console error

## 2569-09-26 · Claude · ทดลองโหมดสาธิตด้วยวิดีโอ (`?tour`) — ทำเองเพราะ Codex ใช้โควตาหมด (เจ้าของงานสั่ง)

เหตุผล: วาดหน้าจอจำลองทีละ Role (v3) กินโควตา Codex เต็มแต่ได้แค่ R0+R1 · เจ้าของงานอัดคลิปการใช้งานแอปจริงครบ 6 ฝ่าย (3:33 นาที ใน `presentation/clips/`)
และขอให้ทั้ง flow อยู่ใน **1–1.30 นาที**

- `src/data/tour.json` — ตารางกลาง: 6 บท (ฝ่าย) · 25 ช่วง (`from`/`to` วินาทีในคลิปต้นฉบับ · `speed` · หัวข้อ · คำอธิบาย · `hold` = ซูมช้า 6% จุดสำคัญ)
- `tools/build-tour.py` — ตัด+เร่งตามตาราง → `public/clips/tour.webm` (VP9 12.8 MB) + `tour.mp4` (H.264 18.2 MB) · **ยาว 87 วินาที** · ไม่มีเสียง · keyframe ทุก 1 วิ
  (ffmpeg จาก `tmp/pyff` ผ่าน imageio-ffmpeg หรือ `FFMPEG=`) · ตรวจแล้วทุกช่วงภาพตรงคำอธิบาย (ดึงเฟรมกลางช่วงทั้ง 25 ช่วง · แก้ช่วง Load Factor ที่ตัดพลาดไป 1 รอบ)
- `src/tour/Tour.tsx` + `.module.css` — ธีมแอป · แผงซ้าย = รายการ 6 ฝ่าย (ติ๊กถูกเมื่อผ่าน) + ขั้น x/y + หัวข้อ + คำอธิบาย ·
  ขวา = วิดีโอในกรอบหน้าต่าง + ป้ายชื่อฝ่ายลอยตอนเข้าบทใหม่ + แถบเวลาแบ่งบท · เล่นต่อเนื่องเอง ·
  **→ / ← ข้ามช่วง · Space หยุด/เล่น · 1–6 ข้ามไปฝ่าย · F เต็มจอ** · ไม่ผูกกับ scroll (ลากวิดีโอตาม scroll ทีละเฟรมกระตุก)
- `main.tsx`: มี `?tour` = โหมดวิดีโอ · ไม่มี = ฉาก R0/R1 หน้าจอจำลองของ Codex เดิม (ไม่ได้แตะ)
- `.gitignore` เพิ่ม `clips/` `public/clips/` (คลิปใหญ่ + อาจมีข้อมูลจริง) · `check-size.mjs` ไม่นับ `dist/clips/` ในงบหน้าเว็บ (รายงานแยก)
- ตรวจในเบราว์เซอร์ headless: เล่น WebM ได้ · กด 2/6 กระโดดถูกบท · หัวข้อเปลี่ยนตามเวลา · Space หยุดได้ · เฟรมระหว่างเล่น p95 5.1 ms · ไม่มี console error ·
  build ผ่าน (JS gzip 136 KB · หน้าเว็บ 1.02 MB ไม่รวมวิดีโอ)
- **ยังไม่ได้ตัดสิน:** จะใช้โหมดวิดีโอแทน v3 ทั้งเรื่องไหม · ถ้าใช้ ฉาก R0/R1 หน้าจอจำลองจะเก็บไว้เปิดเรื่องหรือทิ้ง · จะเพิ่มกรอบเน้น/ซูมเฉพาะจุดบนวิดีโอไหม

## 2569-09-25 · Claude · เปลี่ยนทิศเป็น v3 "เดินชมโมเดลตาม flow การกรอกข้อมูล" → งาน V1 ให้ Codex

เจ้าของงานดู P0-R2 แล้วยังไม่พอใจ ต้องการ **นำเสนอตัวโมเดลเป็นหลักในธีมของโมเดล**: เริ่มจากหน้าแรก ไล่กรอกข้อมูลทีละ Role พร้อมคำอธิบาย
กด → ไปทีละขั้นแบบแอนิเมชันเนียน ๆ ตาม flow · ลำดับ: R1 บริการลูกค้า → R2 จัดรถ → R3 คนขับ → R4 บัญชี → Admin (การใช้ประโยชน์กองรถ · Manager Dashboard รายสาขา)

**เจ้าของงานตอบคำถาม 4 ข้อ (ตามที่ Claude แนะนำทุกข้อ):**
1. หน้าจอแต่ละ Role = **วาดหน้าจอจำลองใหม่ในธีมแอป** (ไม่ใช่ภาพหน้าจอ/วิดีโอ) — ขยับได้ทุกชิ้น
2. คนขับแนบ **รูปไมล์** ตามแอปจริง (ไม่ใช่รูปบิลน้ำมัน)
3. ตำแหน่งรถพูดว่า **"สถานะรถล่าสุด"** — ไม่ใช้คำ real-time/GPS (แอปประมาณจากวันปล่อยรถ + ระยะทาง)
4. Load Factor พูดว่า **"ต้นทุนที่จมลดลง x บาท"** ตามตัวจำลองในแอป — ไม่ใช่ "กำไรเพิ่ม"

**สมมติฐานที่ Claude ตั้ง (เจ้าของงานไม่ได้ค้าน):** ทั้งเว็บใช้ธีมโมเดล · เที่ยวสมมติเที่ยวเดียววิ่งทั้งเรื่อง (เลขที่ใบรายการตรงกันทุก Role) ·
Manager Dashboard โชว์ตาราง "ส่งช้า" / "ของตกค้าง" ที่แอปซ่อนไว้ (`SHOW_TABS = false`) ได้ · R2 เลือกรถก่อนเลือกบิล (ตามบท · แอปจริงเลือกบิลก่อนแต่สลับได้)

**เอกสารที่เปลี่ยน:**
- `STORYBOARD.md` **v3 เขียนใหม่ทั้งไฟล์** — 9 ฉาก ~30 step (R0 · R1–R4 · A0–A2 · END) พร้อมไฟล์อ้างอิงในแอปทุกฉาก · v1/v2 ย้ายไป `archive/`
- `DESIGN_DIRECTION.md` **v3 เขียนใหม่** — ธีมแอป (โทเคนจาก `app/src/index.css` · LINE Seed) · โครงจอ แผงคำอธิบาย 34% + หน้าจอจำลอง 66% ·
  ชุดท่า Cursor/Type/Select/Check/Auto-calc/Spotlight/Camera/Modal/Toast/NumberRoll/PageSwitch · สิ่งที่ห้าม · v2 (Pulse) ย้ายไป `archive/`
- `IMPLEMENTATION_PLAN.md` **ข้อ 11 ใหม่** (ทับข้ออื่นที่ขัด): เก็บ engine ทิ้งฉาก/คอมโพเนนต์ v2 · โครง `mock/` `primitives/` `narration/` ·
  หนึ่ง Role = หนึ่ง Scene · `tools/build-scenario.ts` คิดค่าที่ระบบคำนวณด้วยสูตรจาก `app/` (ออฟไลน์) · สัญญา Story ส่วน Admin · Phase V1–V4
- `PRESENTATION_BRIEF.md` เพิ่มหมายเหตุ v3 ด้านบน

**งาน V1 ให้ Codex:** ล้างของ v2 · ธีม + AppWindow + primitive ทั้งชุด + narration · scenario (บิล) · ฉาก **R0 + R1 เท่านั้น** (7 step) ·
เทียบหน้าจอจำลองกับแอปจริง (รัน `app/` แบบอ่านอย่างเดียวได้) · จบแล้วให้เจ้าของงานเปิดดูก่อนทำ R2 ต่อ

## 2569-09-25 · Codex · P0-R2 — ต้นแบบดีไซน์ใหม่สามฉาก

- อ่าน `HANDOFF.md`, `DESIGN_DIRECTION.md` ทั้งไฟล์, หัวข้อ “★ ต้นแบบรอบ 2” ใน STORYBOARD และแผนข้อ 4/4.1/4.2 พร้อมรายการ R1 ที่ต้องเก็บต่อ ไม่แก้เนื้อหาแผนและไม่เพิ่มฉาก
- สร้าง S00 ใหม่: พาดหัวสองบรรทัดและ Highlight, dashboard skeleton โผล่จากขอบล่าง, KpiChip สองใบเคลื่อนต่างระยะ, ประโยคเชื่อมที่ p 0.6; เอารถออกจากปก
- สร้าง S05 ใหม่: sticky split, แท่งซ้อนแนวนอนหกกลุ่ม ค่าเช่าแยกจากคงที่, ท่อนปัจจุบันสีเน้นและท่อนเก่าเทา; สูญเปล่าแยกช่องว่างและใช้แดง; ยอดสะสม 3,300 → 4,200 → 4,900 → 5,400 → 5,600 → 6,000 จากข้อมูลสมมติ; ค่าปรับลดไม่วาดเป็นแท่ง
- สร้าง S13 ใหม่: ซูมเข้า PI ภายใน DashFrame เดิม, จังหวะแรกวงเทาและ “—”, ห้าหมวดนับสะสม 14 → 24 → 33 → 46 → 56; หมวดต่ำกว่า 10 ใช้ leak; รายละเอียดปัจจุบันสองตัวชี้วัดและรายชื่อสะสม; จังหวะที่เจ็ดถอยให้เห็น dashboard live พร้อมการ์ดลอยสามใบและประโยคสรุป (B9 ครั้งเดียว)
- เพิ่ม `components/DashFrame`, `KpiChip`, `Highlight`, `FontComparison` และ `engine/beats.ts` สำหรับชุดท่า B1–B9; DashFrame มีพิกัดอ้างอิง 1440×900, `getWidgetRect()` และ props สถานะ skeleton/gray/live; กราฟใช้ SVG/HTML ไม่ใช้ภาพหน้าจอแทนกราฟ
- โทเคนใหม่พื้นขาวอมครีม/หมึกดำ/เขียวมะนาว/แดงตาม DESIGN_DIRECTION; สีหมวดใช้เฉพาะจุดหน้าชื่อ; ไม่มีรหัสฉากในหน้าผู้ชม, แถบบนเป็น “โมเดลต้นทุนการเดินรถ”, ข้อความหลักสำหรับอ่านบนจอโปรเจกเตอร์ ≥18px (ป้าย mono ตกแต่ง 14px ตามทิศทางใหม่)
- ฟอนต์ออฟไลน์: Anuphan + Inter (tnum) + JetBrains Mono; เก็บ LINE Seed และเพิ่ม IBM Plex Sans Thai สำหรับหน้า `?fonts` มีปุ่มเลือกและจำชุดที่เลือกบนเครื่อง; ค่าตั้งต้นผ่าน `--font-thai`; แนบ OFL และ `src/assets/fonts/SOURCES.md` ทุกชุด ฟอนต์รวม **267.8 KB**
- เพิ่มข้อมูลสมมติเฉพาะคีย์ที่อนุญาต: `kpi`/`kpi.monthly`, `routes`, `lf`; รวมค่าที่เตรียมมาแล้วใน `data/presentation.ts` เพื่อแสดงยอดสะสม ไม่คัดลอกสูตรธุรกิจจากแอปหลัก
- ใช้ engine เดิม พร้อม `gsap.ticker.lagSmoothing(0)`; เพิ่มการคง hash/step ตอน refresh จากการเปลี่ยนขนาดจอ และให้ Lenis อัปเดตระยะเลื่อนก่อนคืนตำแหน่ง เพื่อไม่ให้การกดเต็มจอพาไปจังหวะอื่น
- เก็บ R1 ข้อ 1/2/3/8/9 แล้ว: ยอดและคะแนนสะสม, แยกค่าเช่า, ขนาดตัวอักษร/ชื่อหมวดอังกฤษตามแอป, ซ่อน ScreenShot ที่ไม่มีรูป (กรอบสำรองมีเฉพาะ `?debug`), lagSmoothing และเทสต์ยอดสะสมสุดท้าย
- Build แบบ IIFE ยังเปิด `dist/index.html` ตรง ๆ ได้: แยก font assets ออกจาก JS ปกติเพื่อตรวจงบ JS ให้ถูกส่วน แล้วฝังฟอนต์ใน HTML พกพา; แนบสัญญาอนุญาตทุกชุด; `tools/check-size.mjs` ตรวจงบฟอนต์ 400 KB เพิ่ม และ `check-offline.mjs` ตรวจว่าไม่มีฟอนต์ที่ต้องโหลดแยกใน HTML
- **ผลตรวจ:** `npm.cmd run build` ผ่าน; `npm.cmd test` ผ่าน **14 เคส**; JS gzip **ประมาณ 134 KB**, dist **ประมาณ 1.56 MB**; ไม่มี dependency ใหม่ใน package/lockfile
- **ตรวจเบราว์เซอร์จริงแบบ headless:** ใช้ Playwright/Chromium ที่ติดตั้งอยู่แล้วนอก dependency ของเว็บ ผ่าน `tools/check-presentation.mjs`; กดลูกศรครบ **15 จังหวะ** ที่ **1280×720 และ 1920×1080**, ตรวจยอดทุกจังหวะ, ย้อน PI กลับ “—”, refresh `#s05-3`, เปิดโน้ต P, `?lite`, reduced-motion และ `?fonts` ขณะ offline; ไม่พบ page error; ภาพและผลอยู่ `qa/` (gitignore)
- ตรวจภาพแล้วแก้การ์ดลอยไม่ให้บัง KPI/Load Factor, ขยายตัวหนังสือใน dashboard เมื่อถอยกล้อง, และยืนยันหลัง resize 1280×720 → 1920×1080 ว่า `#s13-4` ยังอยู่จังหวะเดิม คะแนน 33 อยู่ในจอ; การเลือกฟอนต์แล้ว reload และฟอนต์ offline ผ่าน
- เวลาเฟรมในการทดสอบ scroll แบบ headless: เฉลี่ย 7.14 ms (1280) / 7.56 ms (1920), p95 ประมาณ 7.1 ms; รอบ 1920 พบเฟรมสูงสุด 79.7 ms จึง **ไม่อ้างว่าทุกเฟรมผ่านเกณฑ์เครื่องนำเสนอจริง** เจ้าของงานยังต้องลองกับโปรเจกเตอร์/รีโมตจริงก่อนวันนำเสนอ
- ขอบเขต: เขียนเฉพาะ `presentation/`; ยังมีสามฉากเดิม; ภาพจริง `pi-detail` และข้อมูลจริงยังไม่ใช่งานรอบนี้; เพิ่มวิธีเปิด/เทียบฟอนต์/ทดสอบใน `README.md`

➡️ ขั้นต่อไป: เจ้าของงานเปิดดู P0-R2 และเลือกฟอนต์
สิ่งที่ต้องตัดสินก่อน: ชอบฟอนต์ชุดใด และจังหวะซูม/เปิดข้อมูลเหมาะกับการเล่าหรือไม่
ข้อความสั่งงาน (คัดลอกไปวางได้เลย หลังเปิดดู):
> อ่าน presentation/docs/HANDOFF.md, DESIGN_DIRECTION.md และ CHANGELOG.md รายการ Codex P0-R2 ล่าสุด แล้วรีวิว S00 · S05 · S13 เทียบหัวข้อ “★ ต้นแบบรอบ 2” ใน STORYBOARD.md
> จดข้อแก้พร้อมผลเลือกฟอนต์จากหน้า ?fonts ลง CHANGELOG.md เพื่อส่งกลับให้ Codex โดยยังไม่ขยายเกินสามฉาก

---

## 2569-09-25 · Claude · ดีไซน์ใหม่ → รอบแก้ P0-R2 สำหรับ Codex (ทับ P0-R1 บางข้อ)

เจ้าของงานส่งแนวอ้างอิง: Webflow dashboard showcase · Framer Pulse · Dashboard Kit · หน้า product ของ Apple —
อยากได้ modern/clean · ฟอนต์สวยอ่านง่าย · **ค่อย ๆ reveal KPI → chart → insight ตาม scroll และ dashboard เป็นส่วนหนึ่งของเรื่อง ไม่ใช่หน้าหลัก**

- สำรวจหน้า Pulse จริง (ภาพหน้าจอ 12 ตำแหน่ง + วัดฟอนต์/สีจาก DOM): พื้น `#FAFAF6` · หมึก `#12120E` · Space Grotesk 700 76px −3% ·
  eyebrow ตัวพิมพ์ใหญ่ mono · แคปซูลสีเน้นหลังคำ · กรอบ dashboard ขาว ขอบ 1px เงาฟุ้ง · widget ลอยคร่อมขอบ · แท่งเทามีบางแท่งเป็นสีเน้น
- **เพิ่ม `DESIGN_DIRECTION.md`** — หลัก 5 ข้อ · โทเคนสี (accent เขียวมะนาว + leak แดง · เลิกพื้นไล่สีหมวด PI) · ฟอนต์ (Anuphan + Inter tnum + JetBrains Mono ·
  Space Grotesk ไม่มีตัวไทย) · **ชุดท่า B1–B9** · `DashFrame` dashboard ตัวละคร (skeleton → gray → live) · สิ่งที่ห้าม
- `STORYBOARD.md` เพิ่มหัวข้อ "★ ต้นแบบรอบ 2" — **S00 v2** (ปกแบบ product page แง้ม dashboard · เลิกรถบรรทุกในปก) · **S05 v2** (sticky split แท่งแนวนอน
  6 step ค่าเช่าแยก) · **S13 v2** (กล้องซูมวงแหวนใน dashboard → เติมทีละหมวด → ถอยเห็น dashboard ทั้งหน้า = B9 ครั้งเดียวของเรื่อง)
- `IMPLEMENTATION_PLAN.md`: คอมโพเนนต์ `DashFrame` `KpiChip` `Highlight` · `engine/beats.ts` (ข้อ 4.1) · ฟอนต์ + หน้าเทียบ `?fonts` (ข้อ 4.2) ·
  สัญญา `Story` เพิ่ม `kpi.monthly` · BRIEF ข้อ 6 ชี้ไปไฟล์ใหม่

**P0-R2 ให้ Codex ทำ:** ทำ S00 · S05 · S13 ใหม่ตามหัวข้อ "★ ต้นแบบรอบ 2" ใน STORYBOARD + DESIGN_DIRECTION ทั้งไฟล์ + แผนข้อ 4 / 4.1 / 4.2
- **ยังต้องทำจาก P0-R1:** ข้อ 1 (ยอดสะสม S05) · 2 (คะแนนสะสม S13) · 3 (ค่าเช่าแยกกลุ่ม) · 8 (≥ 18px · ไม่มีรหัสฉากบนจอ · ซ่อนกรอบภาพว่าง) · 9 (lagSmoothing + เทสต์ค่าสะสม)
- **ข้ามได้เพราะดีไซน์ใหม่แทนแล้ว:** R1 ข้อ 4 (step ปกเดิม) · 5 (แท่งแนวตั้ง) · 6 (accordion การ์ด) · 7 (สีหมวด) — ทำตาม v2 แทน
- engine (scroll/steps/store/hash/ปุ่มพรีเซนเตอร์/`?lite`) **ใช้ของเดิม** ผ่านรีวิวแล้ว
- เพิ่มคีย์ใน `story.sample.json` ได้เฉพาะที่อยู่ในสัญญาข้อ 5.1 (`kpi.trips` · `kpi.monthly` · `routes.top` · `lf.avg` ฯลฯ สำหรับ widget ของ `DashFrame`) — ตัวเลขสมมติ
- ห้ามขยายฉากเกิน 3 ฉาก · ห้ามเพิ่มไลบรารี (Inter/Anuphan/JetBrains Mono เป็นไฟล์ฟอนต์ ไม่ใช่ไลบรารี)

## 2569-09-25 · Claude · รีวิว P0 (S00 · S05 · S13) → รอบแก้ P0-R1 สำหรับ Codex

**วิธีตรวจ:** `npm run build` แล้วเปิด `dist/index.html` ใน Chromium แบบ headless · กด → ครบทุก step เก็บภาพที่ 1920×1080 และ 1280×720 ·
วัดเวลาเฟรมระหว่างเลื่อนด้วยล้อเมาส์ต่อเนื่อง · ทดสอบ refresh ที่ `#s05-3`

**ผ่าน (ไม่ต้องแก้):**
- build ผ่าน · JS gzip 221.5 KB · dist 1.00 MB · ไม่มี console error / request ล้ม
- ปุ่ม → ไล่ครบ 13 step ตามลำดับ (`#s00-2` → `#s05-1…5` → `#s13-1…6`) · กดเกินท้ายแล้วค้างที่ step สุดท้าย
- refresh ที่ `#s05-3` กลับตำแหน่งเดิมตรง (y = 5389 เท่ากับตอนกดมาถึง)
- เวลาเฟรมระหว่างเลื่อน: เฉลี่ย 6.95 ms · p95 7.1 ms · สูงสุด 14 ms · ไม่มีเฟรมเกิน 33 ms (headless — เจ้าของงานยังต้องลองบนเครื่องจริง)
- 1280×720 ไม่มีอะไรซ้อนกัน · ตัวเลข S05 รวมกันได้ 6,000 ตรงยอด · สถานะคุณภาพบริการ 10/20 = เฝ้าระวัง ตรงเกณฑ์แอปหลัก
- architecture ตรงแผน: ไม่มี router/state library · CSS Modules · story import ตอน build · ไม่ import จาก `app/`

**ข้อแก้ P0-R1 — เรียงตามความสำคัญ (รายละเอียดท่า/จังหวะอยู่ใน STORYBOARD ที่อัปเดตแล้ว):**
1. **S05 ยอดรวมนับสะสมตาม step** (STORYBOARD S05 ข้อแก้ 1) — ตอนนี้ step 1–4 มีป้าย "ต้นทุนเฉลี่ยต่อเที่ยว" แต่ไม่มีตัวเลข ดูเหมือนหน้าพัง
2. **S13 คะแนนกลางวงนับสะสมตามหมวด** (S13 ข้อแก้ 1) — ตอนนี้ค้าง "0/100" ตลอด step 1–5 ขัดกับคะแนนหมวดข้างขวา
3. **S05 ค่าเช่าไม่ใช่ "คงที่"** (S05 ข้อแก้ 3) — ผิดโครงต้นทุนของแอปหลัก · ค่าเช่าต้องแยกกลุ่มและแยกสีจากค่าเสื่อม (ตอนนี้เทาเข้มเหมือนกัน)
4. **S00 step 2 หยุดบนจอว่าง** (S00 step ที่แก้แล้ว) — ย้าย step 2 จาก p 0.95 เป็น p 0.55
5. **S05 แท่งเล็กเกินและไม่มีป้ายติดแท่ง** (S05 ข้อแก้ 2) — แท่ง ~186×400 px ที่ 1920 ในขณะที่รายการด้านขวาเด่นกว่า
6. **S13 รายละเอียดตัวชี้วัดกางใต้การ์ดที่ active** (S13 ข้อแก้ 2) — ตอนนี้เป็นกล่องลอยแยก และหลัง step 5 เหลือพื้นที่ว่าง
7. **S13 สี Route กับ Service ชนกัน** (S13 ข้อแก้ 3) · **ชื่อหมวดใช้ชื่ออังกฤษตามแอป + ไทยบรรทัดรอง** (S13 ข้อแก้ 4) · ชิปสถานะย้ายใต้คะแนน (ข้อแก้ 5)
8. **กติกาจอโปรเจกเตอร์ใหม่ STORYBOARD ข้อ 3** — ตัวหนังสือที่ต้องอ่าน ≥ 18px (ตอนนี้มีข้อความเล็กกว่า 16px ราว 70 จุดต่อจอ เช่น หัวกลุ่ม S05,
   สูตร PI, ป้ายองก์) · เอารหัส `S05`/`S13` ออกจากจอผู้ชม · หัวแถบบนเป็น "โมเดลต้นทุนการเดินรถ" · ซ่อนกรอบ `pi-detail` ที่ยังไม่มีรูป (เห็นเฉพาะ `?debug`)
9. **engine:** เพิ่ม `gsap.ticker.lagSmoothing(0)` (IMPLEMENTATION_PLAN ข้อ 3) · เพิ่มเทสต์ค่าสะสม step สุดท้าย = `costPerTrip.total` / `pi.total`

**ขอบเขต:** แก้เฉพาะ 3 ฉากเดิม **ไม่ขยายฉาก** (รอลำดับเรื่องจริงจากเจ้าของงาน) · ไม่เพิ่มคีย์ใน `story.json` · ไม่เพิ่มไลบรารี

## 2569-09-25 · Codex · P0 ต้นแบบสามฉาก พร้อมให้เปิดตรวจ

- อ่านเอกสารใน `presentation/docs/` ครบ โดยเริ่มจาก `HANDOFF.md` และทำเฉพาะ P0 ไม่แก้แผนหรือลำดับเรื่อง
- เพิ่ม package/lockfile แยก, Vite + React 19 + TypeScript, GSAP/ScrollTrigger + Lenis, CSS Modules, tokens และฟอนต์ LINE Seed สามน้ำหนักพร้อม OFL ภายใน `presentation/`
- `src/engine/`: Scene ตรึงตามระยะ 150/350/400vh, timeline ต่อฉาก, snap ตาม step, store ด้วย `useSyncExternalStore`, hash กลับจุดเดิม, คีย์บอร์ด/รีโมต, โหมด `?lite` และ reduced-motion; ไม่มี router หรือไลบรารี state
- `src/scenes/`: S00 ตัวอักษรขึ้นทีละคำ รถและล้อเคลื่อนตาม scroll; S05 แท่งต้นทุนเติมห้าจังหวะ ก้อนสูญเปล่าแยกและยอดรวมนับขึ้น; S13 วงแหวนห้าหมวด รายละเอียดสองตัวชี้วัดต่อหมวด คะแนนรวมและสถานะบริการ
- `src/components/`: Truck, CountUp, StackBar, Ring, ProgressRail, PresenterPanel และ ScreenShot แบบกรอบสำรอง; แถบองก์คั่น 60vh และฉากเก่าจาง/ย่อขณะส่งต่อ; โหมดผู้นำเสนอมีโน้ต เวลา และจังหวะถัดไป
- `src/data/story.ts` + `story.sample.json`: สัญญาเฉพาะคีย์ที่ P0 ใช้ ตัวเลขสมมติทั้งหมด ต้นทุนรวม 6,000 บาท/เที่ยวและ PI 56/100 เป็นเพียงตัวอย่าง; ชื่อผู้นำเสนอ/สัมมนา/สถาบันว่างตามค่าตั้งต้น; ไม่มีข้อมูลจริงหรือ import จากแอปหลักตอนรัน
- รายละเอียดตัวชี้วัดใช้ `g/y/r/n` ตามสัญญาใน IMPLEMENTATION_PLAN ข้อ 5.1 (STORYBOARD เรียกว่า `tally` แต่สัญญาไม่มีฟิลด์ชื่อนี้); ไม่เพิ่มสูตรธุรกิจในฉาก
- เพิ่ม `tools/standalone.mjs`: ฝัง IIFE, CSS และฟอนต์ใน `dist/index.html` เพราะ `base: "./"` เพียงอย่างเดียวไม่ทำให้ ES modules เปิดผ่าน `file://` ได้; ใช้ Node มาตรฐาน ไม่เพิ่มไลบรารีนอกแผน; แนบ `dist/OFL.txt`
- เพิ่ม `tools/check-size.mjs`, `tools/check-offline.mjs` และ `README.md` พร้อมวิธีเปิดและปุ่มควบคุม
- ตรวจแล้ว: `npm.cmd ci` ผ่าน; `npm.cmd test` ผ่าน 11 เคส (ข้อมูล/รูปแบบเลข, ต้นทุนติดลบและศูนย์, PI ไม่มีข้อมูล, ตำแหน่ง step/การกดเร็ว/การคืน hash/lite); `npm.cmd run build` ผ่าน TypeScript, งบขนาด และตรวจโครงไฟล์ออฟไลน์ ฟอนต์ฝังครบสามน้ำหนัก; JS gzip ประมาณ 222 KB, dist ประมาณ 1.00 MB
- Vite แจ้งเตือน chunk ดิบเกิน 500 KB เพราะฝังฟอนต์กับสคริปต์เพื่อเปิดไฟล์ตรง ๆ แต่ขนาด gzip ยังต่ำกว่าเกณฑ์ 300 KB ตามแผน
- **สถานะเกณฑ์รับงาน: ยังรอตรวจในเบราว์เซอร์** เครื่องมือ browser ในเซสชันไม่มีทั้ง `iab` และ `chrome` (ตอบ `Browser is not available`) จึงยังไม่ได้เห็นภาพจริง/วัด ≥55 fps/ลองคีย์บนหน้า/ปิดเน็ตทดสอบ และไม่ถือว่าเกณฑ์เหล่านี้ผ่านจาก unit test
- ตรวจต่อบนจอ 1280×720 และ 1920×1080: เลื่อนครบสามฉาก, กด →/← และ PageDown/PageUp, เปิด `#s05-3` แล้วรีเฟรช, เปิด `?lite`, ลอง `P`/`F`, เปิด `dist/index.html` ขณะไม่ต่ออินเทอร์เน็ต; ดูว่าหัวข้อ ป้าย และรายละเอียดคะแนนไม่ซ้อนกัน
- ค้างตามระยะถัดไป: ภาพ `pi-detail` ใช้กรอบสำรองตามข้อ 6.1; ภาพจริง/สคริปต์ข้อมูลจริง/ฉากอื่นยังไม่อยู่ใน P0 ไม่มีการขึ้น P0.5 หรือ P1
- ขอบเขต: เขียนเฉพาะ `presentation/`; ไฟล์ untracked ที่มีอยู่ก่อนใน `docs/` ชั้นรากไม่ได้แตะ

➡️ ขั้นต่อไป: เจ้าของงานเปิดดูต้นแบบ
สิ่งที่ต้องตัดสินก่อน: รูปแบบภาพและจังหวะเลื่อนเหมาะกับการนำเสนอหรือไม่
ข้อความสั่งงาน (คัดลอกไปวางได้เลย หลังเปิดดู):
> อ่าน presentation/docs/HANDOFF.md และ CHANGELOG.md รายการ Codex P0 ล่าสุด แล้วรีวิว S00 · S05 · S13 เทียบ STORYBOARD.md และ IMPLEMENTATION_PLAN.md ข้อ 8
> จดข้อแก้และผลตรวจภาพ/การควบคุม/ประสิทธิภาพลง CHANGELOG.md ก่อนส่งกลับให้ Codex โดยยังไม่ขยายฉากจนกว่าเจ้าของงานส่งลำดับเรื่องจริง

---

## 2569-09-25 · Claude · วิธีทำงานสอง agent + ต้นแบบ

- เพิ่ม `HANDOFF.md` — หน้าที่ Claude (แผน/รีวิว เขียนแค่ `docs/`) กับ Codex (เขียนเว็บ) · **จบงานทุกรอบต้องปิดด้วยบล็อก "➡️ ขั้นต่อไป"**
  บอกเจ้าของงานว่าให้ไปสั่งใครต่อ พร้อมข้อความสั่งงานที่คัดลอกได้ · IMPLEMENTATION_PLAN ข้อ 0 เพิ่มกติกาข้อ 7
- **P0 เปลี่ยนเป็น "ต้นแบบโชว์ฝีมือ"** (เจ้าของงานอยากเห็นก่อนว่าทำได้ประมาณไหน): โครงตาม architecture ครบ + 3 ฉากเต็ม S00 · S05 · S13
  ใช้ข้อมูลตัวอย่าง · เพิ่ม P0.5 (ฉากว่างครบ) หลังเจ้าของงานส่งลำดับเรื่องจริง
- STORYBOARD ติดป้าย "ร่าง" — ลำดับจริงรอเจ้าของงาน

## 2569-09-25 · Claude · เจ้าของงานอนุมัติ architecture

- `IMPLEMENTATION_PLAN.md` ข้อ 1 (ใหม่) — ตารางการตัดสินใจ: แยกขาดจากแอปเดิมตอนรัน · Vite+React+TS package แยก ·
  **ไม่มี router** (hash `#sXX-n`) · **ไม่มีไลบรารี state** (`engine/store.ts` ด้วย `useSyncExternalStore` + Context ของ story) ·
  GSAP+ScrollTrigger+Lenis · กราฟเป็น SVG · **CSS Modules ต่อ component** · `story.json` import ตอน build ไม่ fetch
- ตัวเลขจริง: เจ้าของงานเลือก **ข้อ 1 — สคริปต์ออฟไลน์ `tools/build-story.ts` เรียกสูตรจาก `app/src/lib/`** (ตัวเลขตรงแอปหลัก ·
  สคริปต์ไม่อยู่ในเว็บจึงไม่ทำให้เว็บช้า) · กรอกเองเป็นทางสำรอง
- **ภาพหน้าจอแอปจริงเป็นรูป WebP** ใน 4 ฉาก (S04 S08 S13 S15 · 5 รูป) — ข้อ 6.1 ใหม่: ≤ 300 KB/รูป · รูปจากข้อมูลจริงอยู่ `shots/real/` (gitignore) ·
  ไม่มีรูปแล้ว build ต้องไม่พัง · กราฟยังเป็น SVG เสมอ
- โครงโฟลเดอร์: `ui/` → `components/` (โฟลเดอร์ละตัว + `.module.css`) · เพิ่ม `engine/store.ts` `data/shots.ts` `assets/shots/{sample,real}/` · คอมโพเนนต์ `ScreenShot`
- งบขนาด: `dist/` < 6 MB · รูปรวม < 2.5 MB (เดิม 5 MB ไม่มีรูป) · P4 รวมงานภาพหน้าจอ · checklist เพิ่มข้อภาพหน้าจอ
- `STORYBOARD.md` เพิ่มจังหวะภาพหน้าจอใน S04 S08 S13 S15 · `PRESENTATION_BRIEF.md` ปรับงบขนาด
- เลขข้อใน IMPLEMENTATION_PLAN เลื่อน: แผนที่ = ข้อ 7 · Phase = 8 · งบประสิทธิภาพ = 9 · checklist = 10

## 2569-09-25 · Claude · วางแผน (ยังไม่มีโค้ด)

- สำรวจ `app/` `etl/` `map/` `docs/` `CLAUDE.md` และตัวเลขชุดตัวอย่าง เพื่อใช้เล่าเรื่อง
- สร้าง `docs/PRESENTATION_BRIEF.md` — เป้าหมาย ข้อบังคับ (ห้ามแก้ระบบเดิม · ห้ามตัวเลขจริงใน git · ไม่พึ่งเน็ต) โทเคนหน้าตา เกณฑ์ว่าเสร็จ
- สร้าง `docs/STORYBOARD.md` — 4 องก์ 16 ฉาก (S00–S15) พร้อมท่าแอนิเมชันตาม `p`/step · คีย์ข้อมูลของ `story.json`
- สร้าง `docs/IMPLEMENTATION_PLAN.md` — เทคโนโลยี โครงโฟลเดอร์ engine การเลื่อน/ปุ่มพรีเซนเตอร์ type `Story` วิธีสร้างตัวเลขจริง
  (import `app/src/lib/` ห้ามคัดลอกสูตร) แผนที่ SVG นิ่ง · Phase P0–P5 พร้อมเกณฑ์ผ่าน · checklist วันนำเสนอ
- **ไม่ได้แก้ไฟล์นอก `presentation/`** · ยังไม่มี `package.json` — Codex เริ่มที่ Phase P0
