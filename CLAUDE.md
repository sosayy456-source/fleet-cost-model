# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

โค้ดคอมเมนต์ เอกสาร และข้อความ commit ในโปรเจกต์นี้เป็นภาษาไทยทั้งหมด — เขียนของใหม่ให้เข้าชุดกัน

## คำสั่ง

```bash
cd app
npm ci                    # lockfile commit ไว้แล้ว ใช้ ci ไม่ใช่ install
npm run dev               # → http://localhost:5173/fleet-cost-model/  (ต้องมี path ย่อยด้วย)
npm run build             # tsc -b แล้วค่อย vite build
npm test                  # vitest run — 20 ตัว
npm test -- src/lib/cost/computeCost.test.ts     # เฉพาะไฟล์เดียว
npm test -- -t "ชื่อเทส"                          # เฉพาะเคสเดียว
npx tsc -b --force        # เช็ค type อย่างเดียว ไม่ build
```

ข้อมูลจริง: วาง `.xlsx` ใน `etl/data/revenue/` → plugin `autoEtl` ใน `vite.config.ts` รัน ETL ให้เองตอน dev (หน่วง 2 วิ, หา python จาก `etl/.venv` หรือ venv ของ Streamlit ก่อน `python` บน PATH) → plugin ส่งสถานะผ่าน HMR (`etl:status`) หน้าที่ใช้ `useDataset` ขึ้นแถบ "กำลังแปลง…" แล้ว `reload()` เองตอนเสร็จ (`lib/data/etlStatus.ts`) · แอปเลือกชุดข้อมูลเองตอนรัน (`lib/dataset.ts`: มี `data/real/costrev/manifest.json` ที่เป็น json จริง = real ไม่งั้น sample — เดิมดู `data/real/manifest.json` ของ `build_json.py` ซึ่งเลิกรันอัตโนมัติแล้ว) `VITE_DATASET` ยังบังคับได้ · **Vite dev ตอบ 200 + text/html ให้ทุก path ที่ไม่มีไฟล์** ตรวจแค่ status ไม่ได้ ต้องดู content-type

Windows PowerShell: ถ้า `npm.ps1 cannot be loaded` ให้ใช้ `npm.cmd run dev`

```bash
cd etl
python build_json.py --dataset sample    # → app/public/data/sample/
python -m pytest
```

สคริปต์ครั้งเดียวใน `app/tools/`: `extract-refdata.mjs` (ดึงตารางอ้างอิงออกจาก v5), `gen-golden.mjs` (สร้าง fixture ของ golden test), `extract-custmap.mjs` (**เลิกใช้แล้ว** — สร้าง `custmap.bin` รุ่นเก่าที่เก็บรหัสต้นฉบับแค่ 12 ตัวแรก)

```bash
python etl/build_custmap.py "แปลงรหัสลูกหนี้รวม.xlsx"   # → app/public/custmap.bin (17.9 MB, commit ไว้แล้ว)
```

```bash
python etl/build_fleet.py "ทะเบียนในกองรถ.xlsx"   # → refdata/fleet.json (899 คัน · ไฟล์ 24 ก.ย. 2569) + เติมสาขาใน enums.json
python etl/build_costrev.py --dataset sample|real  # ต้นทุน+รายได้รายเที่ยว → public/data/<ds>/costrev/ (openpyxl ล้วน)
python etl/build_loadfactor.py --dataset sample|real  # Load Factor รายเที่ยว → public/data/<ds>/loadfactor/
```

`build_costrev.py` อ่าน `.xlsx` ด้วย **python-calamine** (เร็วกว่า openpyxl ~10 เท่า ไฟล์รายได้จริงเดือนละ ~110k แถว) ถอยไป openpyxl ถ้าไม่มี · **calamine คืนเซลล์ตัวเลขเป็น float เสมอ** เลขที่ใบรายการ 13 หลักจะกลายเป็น `"6250753132426.0"` ถ้าไม่ตัด `.0` ทิ้งใน `text()` จะจับคู่หายไปเงียบ ๆ (ตอนพบ: ได้ 526 แทน 536) · **อ่านไฟล์รายได้หลังไฟล์ต้นทุนเสมอ** แล้วเก็บบิลเฉพาะใบที่มีในไฟล์ต้นทุน — ข้อมูลรายได้จริง 29 ไฟล์เกือบ 2 ล้านแถว ถ้าเก็บทุกใบจะกินหน่วยความจำหลาย GB

(เคยมีช่วงที่ pandas ใช้ไม่ได้เพราะ Windows Application Control บล็อก DLL ของ pyarrow — 16 ก.ย. 2569 กลับมาใช้ได้แล้ว)

**`build_json.py` ต้องอ่าน-ล้าง-ยุบทีละไฟล์ ห้ามอ่านครบทุกไฟล์ก่อนแล้วค่อย `clean_data`** ข้อมูลรายได้จริง
29 ไฟล์ ~5 ล้านแถว วัดแล้วกิน **1.06 GB ต่อล้านแถว** ของเดิมถือทั้งชุดดิบและชุดที่ล้างแล้วพร้อมกัน
รวมตอน `pd.concat` แล้วเกิน 10 GB บนเครื่อง 16 GB → OS ฆ่า python **พร้อม dev server ที่เป็นแม่ของมัน**
(อาการ: หน้าเว็บขึ้น "Failed to fetch" แล้วรีเฟรชอีกทีก็ localhost ปฏิเสธการเชื่อมต่อ)
· `CATEGORICAL_COLUMNS` ใน `loaders/revenue.py` ยุบคอลัมน์ที่ค่าซ้ำเยอะเป็น category เหลือ 176 MB/ล้านแถว
· **`concat_shared()` ต้องรวมชุดหมวดให้ตรงกันก่อน `pd.concat`** ไม่งั้น pandas คลาย category กลับเป็นสตริง
  แล้วที่ประหยัดมาหายหมดในบรรทัดเดียว (ชุดหมวดเรียงแล้ว `sort_values("month")` จึงยังได้ลำดับเวลาถูก)
· `build_cube()` ก๊อปเฉพาะคอลัมน์ที่ใช้ ไม่ใช่ `df.copy()` ทั้งก้อน
· เกณฑ์ว่าแก้แล้วยังถูก: `python build_json.py --dataset sample` แล้ว diff กับของเดิม ต้องเท่ากันทั้ง 17 ไฟล์
  (ต่างได้แค่ `generatedAt`)

**plugin `autoEtl` ห้ามใช้ `server.watcher.add()` กับโฟลเดอร์ข้อมูล — โพลรายชื่อ+ขนาดไฟล์เองทุก 2 วิ**
chokidar บน Windows เฝ้ารายไฟล์ด้วย `fs.watch` ตอน Explorer ยังคัดลอก `.xlsx` ไม่เสร็จจะได้ EBUSY แล้ว emit
`'error'` ที่ไม่มีใครดัก → **node ตายทั้งโปรเซสทันที** ยังไม่ทันรัน ETL (อาการเดียวกับหน่วยความจำหมด:
"Failed to fetch" แล้ว localhost ปฏิเสธการเชื่อมต่อ — เกิดจริงตอนวางไฟล์จริง 29 ไฟล์ 16 ก.ย. 2569)
การโพลยังดีกว่าตรงที่ไฟล์ที่กำลังคัดลอกขนาดเปลี่ยนทุกรอบ ETL จึงไม่มีทางเริ่มก่อนคัดลอกเสร็จ

**ตารางรายการทุกตัวแสดงครบทุกแถวผ่าน `lib/ui/GrowBox.tsx`** (เจ้าของขอ 16 ก.ย. 2569 — เดิมตัด 300 แถวแรก)
กล่องเลื่อนในตัว สูงสุด 60vh หัวตารางติดบน วาดทีละ 300 แถวแล้วเติมเมื่อเลื่อนถึงท้าย เพราะข้อมูลจริงมีหลายหมื่นแถว
วาง DOM ทีเดียวเบราว์เซอร์ค้าง · **`rows` ที่ส่งเข้าต้อง `useMemo` แล้ว** ไม่งั้น identity เปลี่ยนทุก render กล่องจะเด้งกลับบนตลอด
· ห้ามกลับไปใช้ `list.slice(0, 300)`

**`useSort` ใน `features/dash-costrev/common.tsx` = ตัวเรียงของทุกตารางทั้งระบบ** (Exec Dashboard · ต้นทุน · Damage ·
เที่ยววิ่งเปล่า · ปันส่วน · Demo) **กดหัวคอลัมน์วนสามจังหวะ: มากไปน้อย ▼ → น้อยไปมาก ▲ → ล้างกลับค่าเริ่มต้น**
(เจ้าของงานสั่ง 22 ก.ย. 2569) · ทุกคอลัมน์เริ่มที่มากไปน้อยเสมอ ทั้งตัวเลขและข้อความ · ค่าตั้งต้นเก็บใน `useRef`
เพราะทุกหน้าส่ง `initial` มาเป็น object literal ใหม่ทุก render ถ้าอ้างตรง ๆ จะเทียบไม่เจอว่ากลับถึงค่าเริ่มต้นแล้ว

**แก้อาการข้อมูลจริงช้า/ค้าง/ล่ม 26 ก.ย. 2569** (แผนและผลวัด `docs/แผนแก้-ข้อมูลจริงช้า.md` — ทุกข้อไม่เปลี่ยนตัวเลข):
· **`costrev/trips.json` เก็บเป็นคอลัมน์ + ตารางข้อความ** (`etl/src/tripcols.py` → `lib/data/tripCols.ts` แปลงกลับเป็น `Trip[]` ใน `useCostRev`)
  หน้าจอไม่ต้องรู้ · รูปข้อมูลที่ encoder ไม่รู้จัก = ETL เขียนแบบแถวเหมือนเดิม แอปอ่านได้ทั้งสองแบบ · **เพิ่มคีย์ใน trip ได้เลย
  แต่ค่าต้องเป็น ตัวเลข/null · bool · ข้อความ · list ข้อความ · dict ข้อความ→ตัวเลข · list ของ object คีย์ชุดเดียวกัน** ไม่งั้นถอยเป็นแถว (ไฟล์ใหญ่ 3 เท่า)
  · ตัวแปลงสร้างแถวด้วย `new Function` (เร็วกว่าเติมทีละคอลัมน์ ~6 เท่า) ถอยวิธีช้าเมื่อโดน CSP · ตรวจว่าเท่าเดิม: คัดลอก `trips.json` เป็น
  `trips.old.json` → รัน ETL → `node tools/verify-trips.mjs --dataset real` (พิมพ์แค่ PASS/FAIL + ชื่อฟิลด์ ไม่พิมพ์ค่า) → ลบ `trips.old.json`
· **Executive Dashboard (`DemoDash`) ทุกส่วน/กล่อง PI ห่อ `memo`** — กล่อง PI แจ้งคะแนนขึ้นมาทีละกล่อง ถ้าไม่ memo ทั้ง 4 ส่วนวาดใหม่ทุกครั้ง ·
  **props ที่ส่งเข้าส่วนต้องคงที่** (useMemo/state) · ตัวกรองที่เนื้อหาใช้คือ `fv = useDeferredValue(f)` (แถบตัวกรองใช้ `f`) ระหว่างคิดส่วนจาง `.dm-stale`
· `old_records.json`/`old_debtors.json` โหลดเฉพาะหน้า `FILE_OLD_PAGES` ใน `App.tsx` (records · debtors · fleet-status) — หน้าใหม่ที่อ่าน
  `state.oldRecords/oldDebtors/fileOld` ต้องเติมชื่อลงชุดนั้น ไม่งั้นได้ลิสต์ว่างเงียบ ๆ
· ไฟล์ข้อมูล fetch แบบ `no-cache` (ถาม ETag ก่อน ไม่ได้ของเก่าค้าง) · `manifest.json` ยัง `no-store`
· **ETL แคชแถวดิบของ .xlsx ใน `etl/.cache/sheets/`** (`src/sheetcache.py` ผ่าน `iter_sheet` — คีย์ ไฟล์+ขนาด+เวลาแก้ไข+รุ่น calamine)
  `build_alloc` อ่านไฟล์รายได้อีก 3 รอบจากแคช · โฟลเดอร์เป็นข้อมูลดิบของลูกค้า ติด .gitignore ลบได้ทุกเมื่อ (ข้อมูลจริงหลายร้อย MB)
· **plugin ไม่รัน `build_json.py` อัตโนมัติแล้ว** (ผลไม่มีหน้าไหนใช้ตั้งแต่ลบ Dashboard รายได้ · ไฟล์รายได้เปลี่ยนยังสั่ง costrev + alloc)
  · ตัวเฝ้าไฟล์ดูแค่ รายชื่อ + ขนาด ไม่ดูเวลาแก้ไข (OneDrive/แอนตี้ไวรัสแตะเวลาไฟล์แล้วสั่งแปลงซ้ำทั้งชุด)
· **แถบ "กำลังแปลง…" โชว์ % + หลอด** (เจ้าของงานขอ 26 ก.ย. 2569): python เรียก `report()`/`span()` ใน `etl/src/progress.py` → พิมพ์ `##ETL## <pct> <ขั้น>`
  เฉพาะเมื่อ plugin ตั้ง `ETL_PROGRESS=1` → plugin แยกบรรทัดนี้ออกจาก log แล้วส่ง `pct`/`step` (ไม่เปลี่ยน `at` = เวลาเริ่ม) · % คิดจากจำนวนไฟล์ที่เสร็จ ไม่ใช่เวลาที่เหลือ
  · **หัวทุก Dashboard (Executive · Overall · Manager) แสดงสถานะรวมทุกงาน `useEtlStatus("all")`** (`lib/data/etlBatch.ts` · % ถ่วงน้ำหนักตามงาน ·
  "งานที่ x/y" + ลำดับงาน) — เดิมหัวเห็นแค่งาน costrev แล้วงานปันส่วนที่รันต่ออีกนานไม่มีบอก · ส่วน/แท็บย่อยไม่แสดงแถบซ้ำ
  แต่**การรีเฟรชข้อมูลยังฟังช่องของชุดตัวเอง** (`useAutoReloadOnEtl` กับ costrev/alloc/debtors/loadfactor) ห้ามเปลี่ยนไปฟัง "all"

**plugin `autoEtl` รัน ETL ทีละตัว ห้ามให้ `build_json.py` กับ `build_costrev.py` รันพร้อมกัน** — วางไฟล์ต้นทุน
กับไฟล์รายได้ไล่ ๆ กันจะสั่งทั้งสองงาน ถ้ารันซ้อนกันหน่วยความจำจะบวกกันจนเครื่องหมด `busy`/`want`/`pump()`
ใน `vite.config.ts` คือคิวนั้น งานรายได้มาก่อนเพราะ `build_costrev` เอาผลของมันไปจับคู่ · งานที่รอคิวต้องส่งสถานะ `running` (ข้อความ `QUEUED`/`COPYING`) ตั้งแต่โพลเจอไฟล์ ไม่ใช่รอถึงคิวตัวเอง — Executive Dashboard จะได้ขึ้นแถบทันทีที่วางไฟล์

**แท็บ "Vehicle Utilization Cost" (เดิม "รายละเอียด ข้อ 3") ของ Overall Dashboard (สเปก ข้อ3.pdf 23 ก.ย. 2569)** — สูตร `lib/detail3/calc.ts` · หน้าจอ
`dash-costrev/detail3/` · นิยามและสมมติฐานทั้งหมดอยู่ใน `docs/หลักข้อ3.md` ข้อ 7 (อ่านก่อนแก้) · แยกชนิดรถ**รายคัน**
(น้ำหนัก/ระยะทางเต็มของใบทุกคัน) · สเปกกำหนดวิธีรวมต่างกัน: KPI/กราฟชนิดรถ = SUM÷SUM · ตารางเส้นทาง × ชนิดรถ/Flag = AVG รายเที่ยว ·
**ดีไซน์ใช้ชุดของโมเดลหลักเท่านั้น** (Hero · KC · ZT · dz-cc + DBar · SortTable · ป้าย .fu-pill — เจ้าของงานสั่ง 23 ก.ย. 2569 หลังรุ่นแรกใช้สไตล์เฉพาะ) ·
**ทุกตารางในแท็บนี้ใช้ `detail3/D3Table.tsx`** — **หน้าตาเดียวกับตารางอื่นของ Executive Dashboard** (แบบ "รายละเอียดรายปี × ชนิดรถ"
ของแท็บตัน-กม. · หัว `.tk-th` + บรรทัดรอง "แสดง x จาก y" + ป้ายสี · ช่องค้นหาขวา · ไม่มีกรอบ/หัวชมพู/แถวสลับสี/แถบท้าย — เจ้าของงานสั่ง 24 ก.ย. 2569
แทนดีไซน์กรอบดำหัวชมพูของ 23 ก.ย.) · ข้างในยังเป็น SortTable/GrowBox · แท่ง VC/FC ในส่วนที่ 2 **FC เป็นส้มทึบ ไม่มีลายทาง** ·
ส่วนคุ้มค่าเสื่อมต้องใช้ **`vs[].d` (ค่าเสื่อมรายคัน)** ที่ ETL เติม 23 ก.ย. 2569 — Σd = dep ของใบ · ไฟล์เก่าถอยไปให้คันแรกรับทั้งก้อน

**แท็บ "กองรถ" เปลี่ยนชื่อเป็น "การใช้ประโยชน์ของกองรถ" (ชื่อปัจจุบัน "Vehicle Utilization" ตั้งแต่ 25 ก.ย. 2569) และเปลี่ยนหน้าทั้งหมดตามภาพดีไซน์ที่เจ้าของงานส่ง 23 ก.ย. 2569**
(`dash-costrev/FleetUtilizationView.tsx` · สูตร `lib/fleetcompare/utilization.ts`) — การ์ดหลัก 4 · การ์ดรอง 2 ·
สัดส่วนประเภทรถในแต่ละกลุ่มบริการ (กลุ่ม "ไม่ระบุ" อยู่ท้ายเสมอ) · เส้นทาง 5 อันดับแรกพร้อมสัดส่วนชนิดรถ · โดนัทประเภทรถ ·
ตารางสรุป เส้นทาง × กลุ่มบริการ × ชนิดรถ **แนะนำฝั่งที่ Margin สูงกว่า** (ต่างจาก `SummaryTable` ที่เทียบต้นทุน/เที่ยว —
ที่นี่รายได้แบ่งเข้ารถตามสัดส่วนต้นทุน Margin ของคันจึงเท่ากับของใบ) ฝั่งเดียว = "มีแต่…" ไม่เดาค่าแทน ·
อันดับความคุ้มค่า (กำไร/เที่ยว ↔ กำไร/คัน) กับชนิดรถ 10 อันดับของเดิม **ถูกเอาออกทั้งหมด** ตามภาพ ·
ส่วนสัดส่วนนับใบไม่ซ้ำต่อประเภท ใบที่หัวกับหางคนละประเภทนับทั้งสอง ยอดรวมโดนัทจึงเกินจำนวนเที่ยวได้ (มีโน้ตบอก)

**แท็บ "Empty Trips" (เดิม "เที่ยววิ่งเปล่า") ทำแล้ว** (`dash-costrev/EmptyTab.tsx`) สเปกที่ตกลงไว้อยู่ใน
`docs/spec-เที่ยววิ่งเปล่า.md` — แก้แท็บนี้ให้อ่านเอกสารนั้นก่อน ไม่ต้องถามเจ้าของข้อมูลซ้ำ ·
นิยาม `empty` **ทั้งระบบ** คือ "ราคารวมจากรายได้ = 0 และ ค่าบรรทุกทั้งใบรายการ = 0"
(ไม่ดูประเภทใบรายการแล้ว ไม่จำกัดแค่ "รถว่างไปสาขา") ตัวเลขในแท็บกองรถจึงไม่เท่าของเดิม — ตั้งใจ ไม่ใช่บั๊ก ·
**แท็บนี้ใช้ชุด `inProfitScope()` เหมือนทุกแท็บ** (เจ้าของงานเคาะ 24 ก.ย. 2569 — เดิม 22 ก.ย. ใช้ทุกแถวในไฟล์ผ่าน `ALL_TRIPS`
ซึ่งลบไปแล้ว) เที่ยวเปล่ายังอยู่ครบทุกเที่ยว แต่ตัวหารเหลือเฉพาะเที่ยวที่หน้าอื่นนับ % จึงสูงขึ้น
(ชุดตัวอย่างรวมทุกปี 2.58% → 6.44% · ปี 2569 2.15% → 3.47%) · โน้ต "นับไม่เหมือนแท็บอื่น" ถูกเอาออกแล้ว · **หน้าตาปรับรอบสอง 23 ก.ย. 2569** (การ์ด 2 ใบ · กราฟรายเดือน
แท่งเทาต้นทุนรวม + เส้น % · Top 10 กรอบเดียวปุ่มสลับ · ป็อบอัพรายการเที่ยวเปล่า · ตัดกราฟรายปี/แท่ง 100%/กราฟจุด) ดูหัวข้อ
"ปรับหน้าตารอบสอง" ในเอกสารสเปก ซึ่งทับข้อ 3 เดิมทุกจุดที่ขัดกัน ·
**การ์ดแรก (% ต้นทุนเที่ยวเปล่า) ส่วนเปรียบเทียบข้างล่าง = "มูลค่ารถเที่ยวเปล่า (YTD ม.ค.–พ.ค. 69)" + 3 บรรทัด** (เจ้าของงานสั่ง 24 ก.ย. 2569
แทน "เทียบปีก่อน ± จุด" · สูตร `lib/empty/ytd.ts`): เฉลี่ย/เดือน (÷ จำนวนเดือนในช่วง) · % ของต้นทุนวิ่งรวม · YoY =
(ปี Y − ปี Y−1) ÷ ปี Y−1 **ช่วงเดือนเดียวกันเท่านั้น** · ช่วง = ม.ค. ถึงเดือนสุดท้ายที่ปีนั้นมีข้อมูล (เลือกช่วงเดือน = ช่วงนั้น ตัดท้ายที่เดือนสุดท้ายที่มีข้อมูล) ·
การ์ดมูลค่า (ใบสอง) = ยอดตามตัวกรอง + "x เที่ยววิ่งเปล่า จาก y เที่ยว = z% ของเที่ยวทั้งหมด" (นับเที่ยว ไม่ใช่ต้นทุน) ·
**การ์ดสองใบนี้อยู่ใน `dash-costrev/EmptyHeroes.tsx` ใช้ร่วมกับ Demo › ข้อ 2 กล่องที่ 3–4** (เจ้าของงานสั่ง 24 ก.ย. 2569 แทนการ์ด
"เที่ยววิ่งเปล่าเทียบเที่ยวทั้งหมด" กับ "มูลค่าเที่ยววิ่งเปล่า" เดิม) ใน Demo ตามตัวกรองของหน้า และกดแล้วเปิดแท็บนี้ · แก้ที่เดียวได้ทั้งสองหน้า ·
**คะแนนเที่ยววิ่งเปล่ารายเส้นทางด้วย percentile** (`lib/empty/routeScore.ts` · เจ้าของงานสั่ง 24 ก.ย. 2569 — ทุกข้อถามแล้ว ห้ามเปลี่ยนโดยไม่ถาม):
% = เที่ยวเปล่า ÷ เที่ยวทั้งหมด (**นับเที่ยว ไม่ใช่ต้นทุน**) ต่อเส้นทาง**ตามทิศ** (`rt`) · P25/P50/P75 = PERCENTILE.INC จาก**ทุกเส้นทาง
ที่มีเที่ยววิ่ง** (รวม 0%) ในช่วงเวลา + ประเภทรถที่กรอง **ไม่ตามต้นทาง/ปลายทาง** · ≤ P25 = 🟢 ดี 10 คะแนน · P25–P75 = 🟡 10 × (P75 − %) ÷ (P75 − P25)
· > P75 = 🔴 แย่มาก **คะแนนติดลบตามสูตรเดียวกัน** · **P25 = P75 → ≤ P25 ได้ 10 · เกินได้ 0** (ชุดตัวอย่างเป็นแบบนี้: เส้นทางเป็น 0%/100%
เกือบทั้งหมด P25 = P75 = 0%) · แสดงเป็นคอลัมน์ในตารางเส้นทาง (แถวคงเดิม = เฉพาะเส้นทางที่มีเที่ยวเปล่า) · **กราฟรายเดือนไม่มีเส้น % นับเที่ยว
และเส้นประ P25** (เคยเพิ่มแล้วเจ้าของงานสั่งเอาออก 24 ก.ย. 2569 — กราฟเหลือแท่งต้นทุนรวม + เส้น % ต้นทุนเหมือนเดิม) · คอลัมน์ "% เที่ยวเปล่า" เดิมในตารางคือ **% ต้นทุน** (คงชื่อไว้)

**ชื่อเมนูเปลี่ยน 25 ก.ย. 2569 (เจ้าของงานสั่ง) — อ่านเอกสารนี้ให้ถูกตัว:** เมนู **Demo** (id `demo` · `features/dash-demo/`)
ย้ายขึ้นบนสุดของเมนูและชื่อหน้าจอเป็น **"Executive Dashboard"** (ยังเห็นเฉพาะผู้ดูแลระบบ · เป็นหน้าแรกของผู้ดูแลระบบ) ·
เมนู **Executive Dashboard เดิม** (id `exec-dash` · `features/dash-costrev/`) ชื่อหน้าจอเป็น **"Overall Dashboard"** ·
**Overall Dashboard อยู่ใต้ Executive Dashboard ทันที และทั้งคู่ใช้ไอคอน `I.dash` เดียวกับ Manager Dashboard** (เจ้าของงานสั่ง 25 ก.ย. 2569 ·
แถบเมนูเรียงตาม `PAGES` ใน `App.tsx` ไม่ใช่ `ROLE_VIEWS` — ผู้จัดการจึงเห็น Overall Dashboard อยู่เหนือ Manager Dashboard แม้หน้าแรกยังเป็น Manager Dashboard) ·
**ข้อความ "Executive Dashboard" / "Demo" ในหัวข้ออื่นของเอกสารนี้ที่เขียนก่อน 25 ก.ย. หมายถึงชื่อเดิม** (โค้ด/คอมเมนต์ก็เช่นกัน) ·
ส่วนของ Demo ชื่อหน้าจอเป็นภาษาอังกฤษ (id เดิม · `lib/ui/demoNav.ts`): "กำไรรายเส้นทาง" = **Profit Per Route** (`route`) ·
"ข้อ 2" = **Inefficient Transportation Cost** (`item2`) · "ข้อ 3" = **Vehicle Utilization Cost** (`item3`) · "กำไรลูกค้า" = **Customer Performance** (`cust`) ·
แท็บของ Overall Dashboard ก็เปลี่ยนเป็นภาษาอังกฤษ (id เดิม · `TABS` ใน `CostRevDash.tsx`): "การใช้ประโยชน์ของกองรถ" = **Vehicle Utilization** (`fleet`) ·
"เที่ยววิ่งเปล่า" = **Empty Trips** (`empty`) · "ต้นทุนที่จมกับที่ว่าง" = **Inefficient Transportation Cost** (`lf` — ชื่อซ้ำกับส่วนข้อ 2 ของ Demo ตั้งใจ) ·
"กำไรส่วนเกิน/ตัน-กม." = **Contribution Margin** (`tonkm`) · "รายละเอียด ข้อ 3" = **Vehicle Utilization Cost** (`detail3`) · Damage Rate คงเดิม ·
**หัวข้อข้างล่างใส่ชื่อใหม่แล้ว แต่ในเนื้อความยังมีชื่อไทยเดิมปนอยู่ (โค้ด/คอมเมนต์/ชื่อเทสต์ก็เช่นกัน) — เทียบกับตารางนี้** ·
**ลบโค้ดทิ้งแล้ว:** เมนู "กำไรรายเส้นทาง" (`route-profit` · `features/dash-join/`) · แท็บ "กำไรรายเที่ยว" (ProfitTab + SummaryTable ·
ServiceTable · TripTable) และแท็บ "ต้นทุน" (CostTab) ของ Overall Dashboard — แท็บแรกจึงเป็น Vehicle Utilization ·
หัวข้อของสิ่งที่ลบไปแล้วข้างล่างเก็บไว้เป็นประวัติการตัดสินใจ (`lib/fleetcompare/compare.ts` ยังใช้อยู่ที่ utilization/ข้อ 3)

**เมนู "Cost to Serve" (id `cost-to-serve` · เฉพาะผู้ดูแลระบบ · อยู่ใต้การตั้งค่า — เจ้าของงานสั่ง 25 ก.ย. 2569)** = เครื่องคำนวณตามไฟล์ HTML
ที่เจ้าของงานส่งมาทั้งเนื้อหา/ค่าตั้งต้น/สูตร **ไม่เชื่อมข้อมูลในโมเดล** (เจ้าของงานสั่ง) · หน้าเดี่ยว `public/cost-to-serve/index.html` ฝังผ่าน iframe
ใน `features/costserve/CostToServe.tsx` (ยืดสูงตามเนื้อหา) — ไม่แปลงเป็น React เพราะ CSS ชื่อสามัญ (.card/table/input) จะชนกับ `index.css` ·
ต่างจากต้นฉบับแค่พื้นหลัง = `--bg` ของโมเดล (#F3F0EC) และตัดโหมดมืด · สูตรในหน้านี้เป็นของมันเอง **ไม่ใช่** `computeCost`/`alloc.py` ห้ามอ้างเป็นแหล่งสูตร

**ตัวกรองเวลา = ปี + ช่วงเดือน ตั้งแต่–ถึง แบบแท็บ Damage Rate** (เจ้าของงานสั่ง 24 ก.ย. 2569 · `lib/filter/period.ts` +
`PeriodFF` ใน `dash-costrev/common.tsx`) ใช้ที่ Demo ทั้งหน้า · Empty Trips · Vehicle Utilization · Inefficient Transportation Cost ·
Contribution Margin · Vehicle Utilization Cost · Damage Rate — **เลือกเดือนได้เมื่อเลือกปีแล้ว** ล้างปี = ช่วงกลับเป็นทั้งปี ·
`BaseFilter` มีทั้ง `month` (เดือนเดียว — แท็บกำไรรายเที่ยว/ต้นทุนยังใช้ MonthFF เพราะเจ้าของงานไม่ได้สั่ง) และ `from`/`to`
แท็บหนึ่งใช้แบบเดียว · `passBase(..., { ignoreYear })` **ยังตัดช่วงเดือน** ชุดที่เทียบหลายปี (YTD · ข้อ 3) จึงได้เดือนเดียวกันทุกปี ·
ตัน-กม. เทียบ **ช่วงเดือนเดียวกันของปีก่อน** (`prevPeriod` · เจ้าของงานเลือก — เดิมเดือนเดียวเทียบเดือนก่อน) แท็บนี้ต้องมีปีเสมอ ·
การ์ด YTD เที่ยวเปล่าใช้ช่วงที่เลือก ตัดท้ายที่เดือนสุดท้ายที่ปีนั้นมีข้อมูล (เจ้าของงานเลือก) ·
Top 10 กำไรลูกค้าของทุกช่วงเดือน **ETL คัดไว้ใน top.json** (`"2026|03-05"` · เจ้าของงานเลือก — ลูกค้าที่ต้องเก็บบิลเพิ่มแค่ 209 → 214
ในชุดตัวอย่าง) top.json รุ่นก่อนไม่มีคีย์ช่วง → ตารางขึ้นข้อความให้รัน `build_alloc.py` ใหม่

### Executive Dashboard (`features/dash-costrev/`)

**24 ก.ย. 2569 เจ้าของงานสั่งลบ** เมนู "Dashboard ค่าเดินทาง(ไม่ใช้)" (เดิม Dashboard รวม · `all-dash` · โหมด `all` = ทุกเที่ยวในไฟล์)
และแท็บ Dashboard รายได้ · Dashboard ลูกหนี้ · กำไรลูกค้า (ปันส่วนต้นทุน) (`features/dash-revenue/` · `features/dash-alloc/`) —
**ลบโค้ดทิ้งแล้ว** `CostRevDash` ไม่มี prop `mode` อีก (Executive Dashboard อย่างเดียว) · กำไรลูกค้าดูที่ Demo › Customer Performance ·
แท็บที่อ่านชุดของตัวเอง (ไม่ใช้ `trips`) เหลือ `STANDALONE` = Inefficient Transportation Cost (`lf`) · Contribution Margin (`tonkm`) **ต้องเข้าได้แม้ไฟล์ต้นทุนจะหาย**

อ่าน `data/<ds>/costrev/trips.json` (1 แถว = 1 เที่ยว คีย์สั้น ดู `interface Trip` ใน `lib/data/useCostRev.ts`) ไม่ผ่าน `computeCost`/`recCost` เพราะต้นทุนเป็นยอดสำเร็จรูปจากไฟล์บริษัท · ใช้**ชุดข้อมูลกำไรชุดเดียวทั้งระบบ = `inProfitScope()` = จับคู่กับไฟล์รายได้ได้ (`m`) + เที่ยววิ่งเปล่า** (เจ้าของงานเคาะ 24 ก.ย. 2569 — อนาคตทุกแท็บจะรวมเป็นหน้าเดียว
Executive Dashboard เป็นหน้ารายละเอียดที่ลิงก์ไป ข้อมูลต้องชุดเดียวกับ Demo · เดิมใช้ `m` อย่างเดียว เที่ยวเปล่าหลุดจากกำไร)
แท็บที่ไม่ควรนับเที่ยวเปล่าตัดออกเอง (Damage Rate · ตารางสรุป · ต้นทุนพยากรณ์) · ตัวกรอง "จับคู่ได้/ไม่ได้" ของแท็บกำไรดูเฉพาะเที่ยว
**มีรายได้**ที่จับคู่ไม่ได้ ไม่งั้นเที่ยวเปล่าทำให้มันโผล่ใน exec · เลือกชุดข้อมูลแยกจากแดชบอร์ดรายได้ (ตรวจ `data/real/costrev/manifest.json`) · **แยกขาดจาก `dash-fleet` ห้ามแก้แดชบอร์ดเดิมเพราะเมนูนี้** ใช้ร่วมแค่คอมโพเนนต์ใน `dash-fleet/parts.tsx` และ `lib/chart/dcharts.tsx`

**ไฟล์ต้นทุนรุ่นใหม่ (22 ก.ย. 2569) — `etl/sample_data/ExampleCost.xlsx` (เดิม `ExampleCostandRevenue.xlsx`)**
คอลัมน์เปลี่ยนชื่อ `ค่าซ่อมรวม`→`รวมค่าซ่อม` · `ค่าเสื่อม`→`รวมค่าเสื่อม` (ETL รับทั้งสองชื่อผ่าน `COL_ALIASES`
ถ้าไม่แมป ค่าซ่อม/ค่าเสื่อมจะเป็น 0 เงียบ ๆ) · **ใบเดียวมีได้ถึง 3 ทะเบียน** (หัว `ทะเบียนรถ` · คันที่ 2 `ทะเบียนรถคันที่2`
ซึ่งเป็นค่าเช่า · พ่วง `ทะเบียนพ่วง`) ไฟล์ปันต้นทุนมาให้แล้ว `ต้นทุน = ต้นทุนรถคันที่ 1 + คันที่ 2 + รถพ่วง` (ตรวจแล้ว
9,999/9,999 แถว) ETL เก็บเป็น `vs[]` ใน trips.json ส่วน `pl`/`vk`/`ft`/`cost` ระดับใบยังเป็นของคันที่ 1 + ต้นทุนทั้งใบเหมือนเดิม
แท็บอื่นจึงไม่กระทบ · **แท็บกองรถนับทุกทะเบียนเป็นคัน และแบ่งรายได้ตามสัดส่วนต้นทุน R:S:T** (เจ้าของงานเคาะ
22 ก.ย. 2569 · `fleetSlices()` แบ่งสองชั้น รถ → กลุ่มบริการ) ทำให้**ตัวกรองประเภท/ชนิดรถของแท็บกองรถต้องกรองที่ระดับรถ
ไม่ใช่ระดับใบ** (`passBase(..., { ignoreVehicle: true })` แล้วค่อยกรอง slice) ไม่งั้นเลือกชนิดของหางแล้วจะไม่เจออะไรเลย ·
ไฟล์รุ่นเก่าไม่มี `vs` → ถอยไปนับทะเบียนเดียวต่อใบ ผลเท่าเดิมทุกบาท
· **แถว `หมายเหตุต้นทุน = ค่าเช่า` ไม่นับค่าเสื่อม/ค่าซ่อม "หัว" และไม่นับค่าประกันสินค้า** เพราะคอลัมน์ `ต้นทุน` ของแถวนั้น
= ค่าเช่ารวม + ต้นทุนรถพ่วง เท่านั้น (รถเช่าไม่ใช่รถบริษัท) — ถ้านับจะทำให้กลุ่มต้นทุนบวกเกิน `ต้นทุน` แล้วกลุ่ม "อื่น ๆ"
ติดลบ (ชุดตัวอย่าง −631,445 จากหัว และ −205,950 จากค่าประกัน) เจ้าของงานเคาะ 22 ก.ย. 2569 · แถว `ค่าเดินทาง` ใช้ยอดรวมหัว+หางตามเดิม
· แถว `ค่าเดินทาง+ค่าเช่า` (3 คัน) ค่าเช่า = `ต้นทุนรถคันที่ 2` ตามที่ต้นทุนรวมใช้ ไม่ใช่ `ค่าเช่ารวม` ที่ต่างกัน 200
· **ไฟล์ต้นทุนแก้ใหม่ 24 ก.ย. 2569 (ไฟล์จริงรูปแบบเดียวกัน): คันที่ 1 เป็นหางพ่วง + มีทะเบียนพ่วง → ทะเบียนพ่วงคือหัว**
  ไฟล์ปัน R = ค่าเสื่อม+ค่าซ่อมของหาง T = ส่วนอื่นทั้งหมด · **คอลัมน์ค่าเสื่อม/ค่าซ่อม "หัว"/"หาง" ผูกกับตำแหน่ง K/O ไม่ใช่ชนิดรถ**
  ของหาง (K) จึงอยู่ในคอลัมน์ "หัว" · แถวค่าเช่าของกรณีนี้รถเช่าคือ O → ETL นับค่าเสื่อม/ค่าซ่อม "หัว" แทน "หาง" (`trailer_first`
  ใน `build_costrev.py` — ถ้าไม่แยก "อื่น ๆ" ติดลบ −186,706 ในชุดตัวอย่าง) · **ตัดสินว่าคันไหนเช่าจากตัวเลข (คันที่ต้นทุน = ค่าเช่ารวม)
  ไม่ใช่ชื่อชนิดรถ** เพราะชื่อหางมีหลายแบบ (`รถ 10 ล้อพ่วง(ลูก)` ไม่ขึ้นต้นด้วย "หาง") · `manifest.costChecks` + คำเตือนท้าย log
  (แถวค่าเช่าที่บอกไม่ได้ว่าคันไหนเช่า · ใบที่ "อื่น ๆ" ติดลบ) ไว้จับรูปแบบใหม่ในไฟล์จริง · ตัวเลขระดับใบ (`pl`/`vk`/`ft`) **ยังเป็นคันที่ 1 แม้เป็นหาง**
  (เจ้าของงานเลือก 24 ก.ย. 2569 — แท็บแยกรายคันเห็นหัวถูกต้องผ่าน `vs[]` อยู่แล้ว)

นิยามที่ตกลงกับเจ้าของข้อมูล (16 ก.ย. 2569 ฉบับแก้ครั้งที่ 2 · ดูของแก้ 20 ก.ย. 2569 ข้างล่างด้วย) อยู่ใน docstring ของ `etl/build_costrev.py` — รายได้ = `ราคารวมจากรายได้` ถ้าว่างใช้ `ค่าบรรทุกทั้งใบรายการ` · ต้นทุน = คอลัมน์ `ต้นทุน` · เที่ยววิ่งเปล่า = `ราคารวมจากรายได้` = 0 **และ** `ค่าบรรทุกทั้งใบรายการ` = 0 ไม่จำกัดประเภทใบรายการ (เปลี่ยน 17 ก.ย. 2569 · ETL ต้องอ่านสองคอลัมน์แยกกัน ห้ามดูจาก `rev` — ดู `docs/spec-เที่ยววิ่งเปล่า.md`) · **สูญเปล่า = 3 คอลัมน์น้ำมันนอกเส้นทาง** (`เบี้ยเลี้ยงนอกเส้นทาง` ย้ายไปอยู่ในค่าเบี้ยเลี้ยง `a_off`) · น้ำมัน 8 คอลัมน์ (ไม่มีแก๊ส/Fleet Card) · ค่าธรรมเนียม 7 คอลัมน์ · **ค่าซ่อม = "ต้นทุนคงที่กึ่งผันแปร" (`semiOf`) ไม่อยู่ในผันแปร** · ค่าเช่าเป็นต้นทุนเฉพาะแถว `หมายเหตุต้นทุน=ค่าเช่า` · **กลุ่มบริการ `sg` ไม่มีในไฟล์ต้นทุน** ETL เดาจากประเภทสินค้าที่พบมากสุดในบิลรายได้ของใบนั้น เที่ยวที่จับคู่ไม่ได้เป็น `""` (แอปแสดง "ไม่ระบุ") แท็บการใช้ประโยชน์ของกองรถไม่ใช้ `sg` แล้ว (ใช้ `serviceRevenue` ผ่าน `fleetSlices()`) · **`วันที่ปล่อยรถ` ปนปี ค.ศ./พ.ศ. ในคอลัมน์เดียว** และ**เลขที่ใบรายการต้องเทียบเป็นสตริง** ห้ามแปลงเป็นตัวเลข

### เมนู Demo (`features/dash-demo/`)

**Demo เป็นหน้ายาวหน้าเดียวแล้ว** (เจ้าของงานสั่ง 24 ก.ย. 2569) — 4 ส่วนเรียงลงมา (Profit Per Route · Inefficient Transportation Cost · Vehicle Utilization Cost · Customer Performance)
**ปุ่มแท็บย้ายจากหัวหน้าไปอยู่ใต้ปุ่ม "Demo" ในแถบเมนูซ้าย** — **ชี้เมาส์ที่ Demo = 4 ส่วนกางลงมาใต้ปุ่มเลย ไม่ต้องคลิก**
พื้นชมพูเดียวกับแถบเมนู ไม่มีกล่องขาว ข้อความเยื้องตรงกับคำว่า "Demo" (`.navgroup`/`.navsub` · เจ้าของงานสั่ง 24 ก.ย. 2569 รอบสาม —
เดิมเป็นป็อบอัพกล่องขาวที่ต้องกด Demo ก่อน) · รายการ `DEMO_PARTS` อยู่ใน `lib/ui/demoNav.ts` จึงขึ้นได้ทุกหน้า ·
กดจากหน้าอื่น = เปิดหน้า Demo แล้วเลื่อนไปส่วนนั้นเมื่อข้อมูลโหลดเสร็จ (`takeDemoPending` · เลื่อนซ้ำอีกรอบหลัง 700ms
เพราะกราฟ/แผนที่ดันความสูงหลังเลื่อนครั้งแรก) · รายการที่ไฮไลต์ = ส่วนที่เลื่อนถึง · จอ ≤ 720px ซ่อนแท็บย่อย · App กับ DemoDash (โหลดแยกก้อน) คุยกันผ่าน `lib/ui/demoNav.ts`
(DemoDash ลงทะเบียนรายการส่วน + ฟังก์ชันเลื่อนตอนเปิดหน้า ล้างตอนออก) · hook `useDemoNav()` ใน App ต้องอยู่ก่อน return ของหน้าเลือกตำแหน่ง ·
คำว่า "แท็บ" ที่เหลือข้างล่างหมายถึงส่วนของหน้านี้ · **ตัวกรองชุดเดียวคุมทั้งหน้า** (`dash-demo/filter.tsx`: ปี + ช่วงเดือน · ต้นทาง ·
ปลายทาง · ประเภทรถ · ชนิดรถ · กลุ่มบริการ) แต่ละส่วนกรองเท่าที่ข้อมูลของตัวเองมี แล้วขึ้น `FilterScope` บอกตัวที่ข้าม:
trips ครบทุกตัว · ไฟล์ Load Factor (ข้อ 2 กล่อง 1–2 · การ์ดตัน-กม.) ปี/ช่วงเดือน/ประเภทรถ/ชนิดรถ · กำไรลูกค้าส่วนที่ 1 ปี/ช่วงเดือน (วันที่บิล) ·
**ยกเว้นลูกหนี้ DSO ที่มี "ข้อมูล ณ วันที่" ของตัวเอง** ไม่ขึ้นกับตัวกรองของหน้า · ข้อ 3 ส่วนที่ 1 เทียบปีที่เลือกกับปีก่อน
(ส่ง `costTrips` ที่ไม่กรองปีเข้าไป) · การ์ดตัน-กม. ใช้ `periodFor()` (ไม่เลือกปี = เดือนล่าสุด) · ส่วนกำไรลูกค้าวาดเสมอแม้ไฟล์ต้นทุนพัง

หน้าทดลอง **เห็นเฉพาะผู้ดูแลระบบ** (มีใน `ROLE_VIEWS.admin` เท่านั้น) · ใช้ `costrev/trips.json` ชุด
**`inProfitScope()` = จับคู่กับไฟล์รายได้ได้ (`m`) + เที่ยววิ่งเปล่า** (`lib/data/useCostRev.ts` · เจ้าของงานเคาะ 24 ก.ย. 2569 —
อนาคตทุกแท็บจะรวมเป็นหน้าเดียวและ Executive Dashboard เป็นหน้ารายละเอียด ข้อมูลต้องชุดเดียวกัน · เดิมใช้ `m` อย่างเดียว
เที่ยวเปล่าหลุดไปเองเพราะไม่มีบิลให้จับคู่ กำไรสูงเกินจริง 2.44 ล้านในชุดตัวอย่าง) · ห้ามเขียนเงื่อนไขนี้ซ้ำในหน้าจอ ·
ทุกแท็บของ Demo ใช้ชุดนี้ รวม "ข้อ 2" (เดิมทุกแถว) และ "ข้อ 3" (เดิม `m`) ตรงกับหน้าปลายทางใน Executive Dashboard · แท็บแรก "กำไรรายเส้นทาง" เรียงตามที่เจ้าของงานสั่ง 21 ก.ย. 2569: การ์ดเด่น 3 ใบ (**กำไรใบใหญ่สุดขึ้นก่อน** แล้วรายได้ ต้นทุน — สลับ 24 ก.ย. 2569) →
การ์ดย่อย 8 ใบ (2 แถว · **%Margin กับ %เที่ยวที่ขาดทุนอยู่หัวแถว** · กด %เที่ยวที่ขาดทุน = ป็อบอัพเที่ยวขาดทุนทั้งหมดผ่าน `TripsModal` แบบมีคอลัมน์เส้นทาง) → กราฟรายได้/ต้นทุน/กำไรรายเดือน (**ตามตัวกรองปีด้วย** ต่างจากแท็บกำไรรายเที่ยวที่โชว์ทุกปีเสมอ) →
ตารางกำไรรายเส้นทาง (ซ้าย) + แผงรายละเอียดเส้นทางที่เลือก (ขวา) → การ์ดอัตรากำไร 3 กลุ่มบริการ **กดแล้วกางแผงใต้การ์ด กดซ้ำเพื่อปิด** (`ServicePanel.tsx`) ·
แผงมีกราฟเส้นกำไรสุทธิรายเดือน 3 เส้น สีเดียวกับการ์ด เส้นของกลุ่มที่กดหนา/ทึบ อีกสองเส้นจางไว้เทียบ ·
ตารางในแผงยุบ **รายเส้นทางอย่างเดียว (รวมทุกชนิดรถ)** คอลัมน์ชุดเดียวกับตารางสรุปของ Executive Dashboard
แต่ตัด ชนิดรถ · คำแนะนำ · บริษัท : ร่วม ออก · **กดแถวไหนก็ได้เพื่อเปิดป็อบอัพรายการเที่ยวของเส้นทางนั้น**
(เฉพาะกลุ่มบริการที่เลือก) ใช้ `TripsModal.tsx` ตัวเดียวกับปุ่ม i ของแผงซ้าย ·
**`TripsModal` ต้องวาดผ่าน portal ไป `#view-dash` (ไม่ใช่ body)** เพราะ `.dm-panel` มี transform จากแอนิเมชัน
ซึ่งทำให้ `position:fixed` ข้างในอ้างอิงกล่องนั้นแทนทั้งจอ ป็อบอัพจะไปโผล่กลางหน้าเว็บ — แต่ portal ไป body ไม่ได้เพราะ
โทเคนสีของแดชบอร์ดประกาศใต้ `#view-dash` เท่านั้น เส้นคั่นแถวในตารางจะหายไปเงียบ ๆ · กลุ่มบริการล็อกตามการ์ด
ตัวกรองอื่น (ต้นทาง/ปลายทาง/ชนิดรถ + 10 อันดับแรก/ทั้งหมด) แยกของตัวเอง **ทุกตัวในแถวนี้สูง 38px เท่ากันและชิดเส้นล่างเดียวกัน
สามกล่องเลือกกว้าง 150px เท่ากัน** (`.dm-head` ในส่วนที่ 2 ของ `index.css` — เจ้าของงานสั่ง 22 ก.ย. 2569) ·
**ห้ามแก้ `SummaryTable` ของ Executive Dashboard เพื่อหน้านี้** แผงเขียนตารางของตัวเองแยกไว้แล้ว ·
ข้อมูลที่ส่งเข้าแผงผ่านตัวกรองแท็บทุกตัว **ยกเว้นกลุ่มบริการ** เพราะกราฟต้องวาดครบสามเส้น ·
ตัวกรองเหมือนแท็บกำไรรายเที่ยว + **กลุ่มบริการ** · ตารางซ้าย **กดได้ทั้งแถว** (ไม่ใช่เฉพาะชื่อเส้นทาง) มีคอลัมน์แท่งแนวนอน
ของกำไรต่อเที่ยว เขียว=กำไร แดง=ขาดทุน แล้วค่อยเป็นตัวเลขกำไรต่อเที่ยวกับอัตรากำไรชิดกันด้านขวา ·
**แผงขวาแยกต้นทุนตามการจัดประเภท สองชั้น** (เจ้าของงานสั่ง 21 ก.ย. 2569): ต้นทุนปกติ = ผันแปร (ชั้นสอง: น้ำมัน ·
เบี้ยเลี้ยง · ค่าธรรมเนียม) + กึ่งผันแปร (ค่าซ่อม) + คงที่ (ค่าเสื่อม) + ค่าเช่า + อื่น ๆ · ต้นทุนสูญเปล่า ·
**หยุดที่สองชั้น ไม่ลงรายก้อนย่อย** แม้ไฟล์จะมีคอลัมน์ย่อยของน้ำมัน 5 · เบี้ยเลี้ยง 3 · ค่าธรรมเนียม 7 ·
`ค่าซ่อมรวม`/`ค่าเสื่อม`/`ค่าเช่ารวม` เป็นคอลัมน์เดียวในไฟล์ต้นฉบับ แยกย่อยไม่ได้จริง ๆ ·
สูญเปล่าในไฟล์ต้นฉบับมี 3 คอลัมน์ แต่ ETL รวมเป็นก้อนเดียวและเจ้าของงานให้ใช้ก้อนเดียวต่อ ·
"อื่น ๆ" ติดลบได้ (เป็นส่วนต่างที่เหลือจากต้นทุนปกติหลังหักกลุ่มอื่น) แถบสีจึงวาดเฉพาะค่าบวก ·
ปุ่ม **i** เปิดป็อบอัพรายการทุกเที่ยวของเส้นทางนั้นตามตัวกรองใหญ่ (มีคอลัมน์ %Margin ท้ายสุด) ·
**เส้นทางที่รายได้ = 0 แล้วขาดทุน ให้อัตรากำไรเป็น −100%** (หารด้วยศูนย์ไม่ได้ — เสียต้นทุนทั้งก้อนโดยไม่ได้อะไรกลับ)
เฉพาะตารางนี้ ส่วนป็อบอัพรายเที่ยวยังเป็น "–" ตามเดิม

**ส่วนที่ 4 ของ "Profit Per Route" (เดิม "กำไรรายเส้นทาง") เปลี่ยนตามภาพดีไซน์ที่เจ้าของงานส่ง 24 ก.ย. 2569** — แผนที่เส้นทาง (ซ้าย) ·
จัดอันดับกำไรต่อเที่ยว (ขวาบน · คอลัมน์ # = อันดับตามกำไร/เที่ยว คงที่) · รายละเอียดต้นทุนของเส้นทางที่เลือก (ขวาล่าง · `RouteDetail` —
% กลุ่มต้นทุนเทียบต้นทุนรวม · ค่าเป็นบาท/เที่ยว · **หน้าตาตามภาพที่เจ้าของงานส่ง 24 ก.ย. 2569 รอบสอง**: หัวไม่มีไอคอน
"n เที่ยว · กำไรรวม x บาท" · ตัวเลข 3 กล่องแถวเดียว (กำไรตัวเขียว/แดง) · แถบสัดส่วน**ต้นทุนเท่านั้น ไม่รวมกำไร** ·
รายการแยกหัวกลุ่ม ต้นทุนปกติ/ต้นทุนสูญเปล่า จุดกลมสีต่อก้อน (น้ำมัน คราม · เบี้ยเลี้ยง ม่วง · ค่าธรรมเนียม เขียวน้ำทะเล ·
กึ่งผันแปร ส้ม · คงที่ เทาเข้ม) · `.rp-cl` จองที่ 11 แถว (414px — วัดแล้วยาวสุดในชุดตัวอย่าง 10 แถว 379px)) ·
**หลักการแผนที่ (เจ้าของงานกำหนด 24 ก.ย. 2569 ห้ามย้อน)**: เปิดมา = ไม่มีเส้นทาง แต่ **plot จุดขึ้นลงทุกจุดใน `STOPS` เสมอ**
(ป้ายชื่อเฉพาะเมืองหลัก `MAJOR` + ต้นทาง/ปลายทางของเส้นที่เลือก · เลือกเส้นแล้วจุดอื่นจางลง ·
**จุดที่เส้นวิ่งผ่านและจุดในรัศมี 100 กม. จากแนวเส้นเป็นสีเหลือง** — `nearStops()`/`NEAR_KM` ใน map.js วัดถึงแนวเส้นที่แบ่งทุก ~5 กม. ไม่ใช่แค่ปมถนน) ·
**เลย์เอาต์ตาม "Route Profit Dashboard.html" ที่เจ้าของงานส่ง 24 ก.ย. 2569**: การ์ดเดียวสองคอลัมน์ `.rp-wrap` แผนที่ซ้ายลากแถบ `.rp-resize`
ปรับกว้างได้ 20–65% (ค่าเริ่ม 38 · จำใน localStorage `rpMapPct` · ลูกศรซ้าย/ขวาทีละ 2) · ขวาบนจัดอันดับ (หัวไอคอนเขียว) ขวาล่างรายละเอียด (หัวไอคอนม่วง) · **เส้นโผล่เฉพาะเมื่อกดแถวในตารางด้านขวา**
ทีละเส้น พร้อม **รถวิ่งตามหนึ่งคัน** · **กดแถวเดิมซ้ำ = เอาเส้นออก แผนที่กลับเป็นแผนที่เปล่า** (การ์ดรายละเอียดยังโชว์แถวแรกไว้ก่อนได้ แต่แผนที่ไม่วาดจนกว่าจะกด — `pickedRow` ไม่ใช่ `detail`) ·
**ไม่ใช่ "5 เส้นทางกำไรสูงสุด" แล้ว** (รุ่นแรกเป็นแบบนั้น เจ้าของงานให้ตัดออก) · คลิกเส้นบนแผนที่ไม่ทำอะไร ·
**ขนาด/มุมมองคงที่ทุกครั้งที่เปลี่ยนเส้นทาง**: มุมมองตั้งครั้งเดียวให้ครอบทุกจุดใน `STOPS` (`fitAllStops`) ไม่ซูมตามเส้น · การ์ดแผนที่ยืดตาม
คอลัมน์ขวา `.rp-cl` จึงจองที่ 11 แถว (`min-height`) และคำอธิบายใต้หัวการ์ดแผนที่บรรทัดเดียว ไม่งั้นการ์ดสูง/เตี้ยตามเส้นที่กดแล้วแผนที่จัดกรอบใหม่ ·
**แผนที่เป็นแอปแยกที่ `map/` (d3 + topojson ต้นฉบับจากโฟลเดอร์ "แมพประเทศไทย Google Map") build ด้วย `cd map && npm run build:map`
ลง `app/public/map/` แล้วฝังผ่าน iframe (`dash-demo/RouteMap.tsx`)** ไม่ลาก d3 เข้า bundle หลัก · **แก้ `map/src/` แล้วต้อง build:map ใหม่
และ commit ผลใน `public/map/` ด้วย** (workflow ของ Pages ไม่ build ให้) · iframe ต่อ `?v=<เวลา>` กันแคชแผนที่รุ่นเก่า ·
คุยกันด้วย postMessage: แผนที่ส่ง `map:ready`/`map:drawn` หน้าเว็บส่ง `map:routes` (เส้นเดียวหรือว่าง) — **ต้องรอ `map:ready` ก่อนส่ง**
(แผนที่โหลดขอบเขตจังหวัดจาก CDN ข้อความที่มาก่อนหายเงียบ) · ต้นทาง/ปลายทางต้องมีชื่อใน `map/src/network.js` (`STOPS`)
ไม่งั้นวาดไม่ได้ การ์ดขึ้นข้อความบอก (เพิ่ม แม่ฮ่องสอน · ขุนยวม · เถิน พร้อมถนนใน `roads.js` แล้ว) ·
`#embed&bare` = โหมดฝังการ์ดเล็ก: เต็มกล่องทุกสัดส่วน (`preserveAspectRatio slice`) ขยายป้าย/หมุด/รถกลับตามขนาดจริง (`ui` ใน map.js)
ซ่อนปุ่มซูม/สเกลบาร์ · ต้องต่ออินเทอร์เน็ตตอนเปิด (ขอบเขตจังหวัดจาก jsdelivr ตามต้นฉบับ) ·
**การ์ดแผนที่ไม่มีหัว/คำอธิบาย แผนที่เต็มการ์ดชนขอบ** (ข้อความแนะนำลอยบนแผนที่ `.rp-map-hint`) · **`#thai` = เฉพาะประเทศไทย 2 มิติ**
(เจ้าของงานขอ 24 ก.ย. 2569): ซ่อนทะเล/ประเทศเพื่อนบ้าน/ป้ายชื่อ พื้นโปร่งเห็นพื้นการ์ด · ใช้ `meet` ไม่ใช่ `slice` (ต้องเห็นทั้งประเทศ)
มุมมอง = ทั้งประเทศ · **`#dark` = หน้าตาตามดีไซน์ "Route 1c" (เจ้าของงานส่ง 24 ก.ย. 2569)**: พื้น `#0f1e1b` มีตาราง 20px ·
ประเทศไทยโทนเขียวเข้ม · เส้นเรืองแสงไล่สีเขียว→ฟ้า (`neonTube` ใน motion.js) **ลากตามรถ** (Cars เรียก `progress(q)` ทุกเฟรม) + ดาวหางขาววิ่งวน ·
รถแบบใหม่ `drawTruckNeon` มีวงแสงฟ้า · ต้นทางจุดเขียวไส้ขาว ปลายทางจุดเขียวมีวงกระเพื่อม · เส้นเรืองแสงตั้ง `vector-effect:none`
แล้วคิดความหนาเป็นหน่วย viewBox เอง (`place(k)`) เพราะ dasharray ที่ใช้ลากเส้นต้องอยู่หน่วยเดียวกับ `getTotalLength()` ·
**ปุ่ม ขาว/ดำ มุมขวาบนของแผนที่ (`.rp-theme` · เจ้าของงานขอ 24 ก.ย. 2569)** ค่าเริ่ม = **ขาว** (เจ้าของงานเปลี่ยน 25 ก.ย. 2569 — เดิมดำ) จำใน localStorage `rpMapTheme` (เครื่องที่เคยกดเลือกแล้วคงตามที่เลือก) ·
โหมดตอนเปิดใส่ใน # ของ iframe (ไม่เห็นสีผิดแวบ) เปลี่ยนทีหลังส่ง postMessage `map:theme` — ไม่โหลดแผนที่ใหม่ ·
**เส้นทางที่ขาดทุน (กำไร < 0) ไล่สีแดง→ส้ม** (`NEON_LOSS` · จุดต้นทาง/ปลายทางและจุดบนชิปชื่อเส้นทางแดงตาม · รถยังเป็นแบบเดิม) ·
**เส้นเรืองแสง + รถแบบใหม่ใช้ทั้งสองโหมด** (เลือกจาก `fed` ไม่ใช่ `#dark`) สลับโหมดเปลี่ยนแค่พื้น/ประเทศ/จุดขึ้นลง ·
**แผนที่มีโหมด `#3d` (ผนังซ้อน 16 ชั้น + เงาเบลอ `.thai3d` + เอียง CSS) แต่ปิดไว้** — ลองแล้วเจ้าของงานให้กลับเป็น 2 มิติ ห้ามเปิดคืนโดยไม่ถาม

**ส่วน "Vehicle Utilization Cost" (เดิม "ข้อ 3") ของ Demo (`dash-demo/Item3Tab.tsx` · สเปก ข้อ3ส่วนDEMO.pdf 23 ก.ย. 2569)** การ์ด 3 ใบกดได้ทั้งใบ →
Executive Dashboard: ต้นทุนแต่ละชนิดรถ (ปีล่าสุด + % เปลี่ยนแปลงบาท/ตัน-กม. จากปี ค.ศ. − 1 · `kindYearCost()`) → "Vehicle Utilization Cost" ส่วนที่ 1 ·
คุ้มค่าเสื่อมทุกชนิดรถบริษัท → ส่วนที่ 2 · ภาพรวมกองรถ → "Vehicle Utilization" · ใช้ `m === true` เหมือนแท็บปลายทาง ·
**ดีไซน์ตาม handoff "Fleet Report v3" (เจ้าของงานส่ง 24 ก.ย. 2569)** — การ์ดส่วนเรียงแนวตั้ง หัวข้อป้ายไล่สี ม่วง/เขียว/ฟ้า ·
หัวตารางไล่สีเดียวกับหัวข้อ · **แท่งกับโดนัทเป็นสีทึบปลายมน แบบเดียวกับกราฟอื่นของโมเดล** (เจ้าของงานสั่ง 24 ก.ย. 2569 —
เดิมแท่ง 3D ชั้น gloss + โดนัทไล่เฉดตาม handoff · ตัด `--i3-gloss` ทิ้งแล้ว) · **ลิงก์คือปุ่ม "ดูรายละเอียด ↗" ไม่ใช่ทั้งการ์ด** ·
สีเป็นค่าตายตัวของ handoff (`.i3-*` ท้าย `index.css`) · ฟอนต์ยังเป็น LINE Seed ของทั้งโมเดล (handoff ใช้ Prompt) ·
**เลื่อนไปส่วนที่ระบุด้วย `openExecTab(tab, anchor)`** — anchor เก็บ sessionStorage แยกคีย์ และ**แท็บปลายทางลบเอง** (`clearExecAnchor`)
ไม่ใช่ CostRevDash เพราะแท็บที่ใช้ trips วาดหลังโหลดไฟล์เสร็จ ถ้าลบตอน CostRevDash mount จะหายก่อนได้อ่าน ·
**กดปุ่ม C = กลับไปจุดเดิมก่อนกดกล่องลิงก์ไป Overall Dashboard** (เจ้าของงานขอ 25 ก.ย. 2569 · รุ่นแรกเป็นคลิกขวา แล้วเปลี่ยนเป็นปุ่ม C · `lib/ui/returnPoint.ts` ·
หัวข้อนี้ใช้ชื่อเมนูชุดใหม่ของ 25 ก.ย.: Executive Dashboard = เมนู `demo` · Overall Dashboard = `exec-dash`) —
`openExecTab()` จำ hash + scrollY ลง sessionStorage (`returnStack` ซ้อนได้ 10 จุด) · App ดัก `keydown` ที่ `e.code === "KeyC"`
(ไม่ใช่ `e.key` — แป้นไทยปุ่มเดียวกันพิมพ์ "แ" ก็ใช้ได้) แล้ว `goBack()` **รอจน `main.app[data-page]` เป็นหน้าปลายทาง (hash เปลี่ยนก่อน React วาด) แล้วเลื่อนใน rAF เฟรมแรกที่หน้ายาวพอ**
— rAF รันก่อนเบราว์เซอร์วาดภาพ จอจึงโผล่ที่จุดเดิมเลย ไม่เห็นตำแหน่งอื่นและไม่มีจอขาว · ซ่อนเนื้อหา (`html.rp-returning`) เฉพาะตอนหน้าวาดแล้ว
แต่ยังสั้นกว่าจุดเดิม นานสุด 1.5 วิ (รุ่นแรกเลื่อนตามทุกเฟรม = จอวิ่งผ่านตาราง · รุ่นสองซ่อนรอความสูงนิ่ง = จอขาว ~2 วิ — เจ้าของงานแจ้งทั้งสองรอบ ·
เวลาที่เหลือ ~1.2 วิ คือเบราว์เซอร์วาดหน้า Executive Dashboard ทั้งหน้าใหม่ ข้อมูลอยู่ในแคชแล้ว) · **เฉพาะกล่องลิงก์ไป Overall Dashboard** (เจ้าของงานเลือก) · กดเมนูซ้ายเอง = ล้าง (`goto`) ·
ไม่ทำงานตอนพิมพ์ในช่องกรอก และตอนกดคู่ Ctrl/⌘/Alt (Ctrl+C คัดลอกยังใช้ได้) · คลิกขวาเป็นเมนูปกติของเบราว์เซอร์ · ไม่มีปุ่มกลับบนจอ ·
ได้คืนแค่หน้า + ตำแหน่งเลื่อน ตัวกรอง/เส้นทางที่เลือกในหน้านั้นหายตามหน้า

**Performance Index ในหน้า Demo** (เจ้าของงานส่งตารางเกณฑ์ 25 ก.ย. 2569 · สูตร/เกณฑ์ `lib/pi/score.ts` ที่เดียว · กล่อง `dash-demo/PiIndex.tsx`)
— 5 หมวด × 2 ตัวชี้วัด × 10 = 100 · กล่องยาวท้ายทุกส่วน: Route & Service (ท้าย Profit Per Route) · Fleet & Trip (ท้าย Inefficient Transportation Cost) ·
Cost & Vehicle (ท้าย Vehicle Utilization Cost) · Customer (ท้าย Customer Performance ซ้าย) คู่กับการ์ด **Damage Rate ใบเดียว** (ขวา ขนาดเท่ากัน · ใบเดียวกับการ์ดแรกของแท็บ Damage) ·
Service Quality · **คะแนนรวม XX/100 บรรทัดสุดท้าย** · **คะแนนตัวชี้วัด = (เขียว + 0.5 × เหลือง) ÷ จำนวนรายการ × 10 โดยให้สีทีละรายการ**
(เจ้าของงานเลือก — ไม่ใช่ให้สีครั้งเดียวจากค่ารวม): Load Factor = Max LF **รายเที่ยว**ของไฟล์ LF (`passLfDemo` ตัวกรองเดียวกับกล่อง LF ข้อ 2) ·
Cost per Ton-km / Fixed Cost Coverage (= Contribution ÷ ค่าเสื่อม) **รายคัน** (`VRow` ของรายละเอียด ข้อ 3 · Coverage เฉพาะรถบริษัทที่มีค่าเสื่อม) ·
Customer Net Profit **รายลูกค้า** (`rollupCustomers` ชุดเดียวกับส่วนที่ 1) · DSO **รายบิล** ตามวันที่จ่ายช้า (`lib/debtors/aging.ts` —
ย้ายออกมาจาก OverdueSection ให้ใช้ร่วม · ณ "ข้อมูล ณ วันที่" ของส่วน DSO ผ่าน `onAsOf`) ·
**เกณฑ์สีเป็นตัวเลขตายตัวตามตารางของเจ้าของงาน** (LF ≥84/47–84/<47% · Ton-km ≤1.28/–2.64/>2.64 บาท · Coverage ≥47.27/12.93–47.27/<12.93 เท่า ·
ลูกค้า ≥10/0–10/<0% · DSO ตรงกำหนด/ช้า 1–30/>30 วัน) — ไม่ใช่ percentile สด · **On-time Delivery ตัดออก** ·
**Service Quality = Damage Performance ตามสเปก "dashboard คชจ.pdf" ไม่ใช้การนับสี** (`lib/pi/damage.ts`): DR + DIR ตัวละ
`MAX(0, 10 − 5 × KPI ÷ P75)` (SUM÷SUM ของช่วงที่กรอง · เที่ยวที่จับคู่ได้ไม่รวมเที่ยวเปล่า) · **P75 = ชุดอ้างอิงคงที่ = KPI รายเดือนของบริษัททุกเดือนในไฟล์
ไม่ตามตัวกรอง** (ต่างจากแท็บ Damage ที่คิดตามช่วงที่เลือก · รายวัน × สาขาใช้ไม่ได้ — ชุดตัวอย่าง 96% เป็น 0 → P75 = 0) ·
P75 = 0 → ใช้เดือนที่ > 0 · รายได้ 0 = DR "ประเมินไม่ได้" · DIR ต้องมีเที่ยว ≥ 100 ÷ (2 × P75 ของ DIR) ไม่งั้น "ข้อมูลไม่เพียงพอ" ·
สถานะ ผ่าน 15–20 / เฝ้าระวัง 10–<15 / ไม่ผ่าน < 10 (เกณฑ์ภายใน ไม่ใช่มาตรฐานสากล · ขาดตัวใดตัวหนึ่ง = ไม่ขึ้นสถานะ) ·
ผลของสูตร: ช่วงที่ KPI ≈ P75 ได้ราว 5/10 ต่อตัว ชุดตัวอย่างทั้งไฟล์จึงได้ ~9/20 "ไม่ผ่าน" — ไม่ใช่บั๊ก ·
**ตารางรุ่น "Performance Index.pdf" (25 ก.ย. 2569) เติมช่องที่เคยว่างครบแล้ว — ไม่มีตัวไหน "รอเกณฑ์"**:
Route = %Margin **รายเส้นทาง** · Service group = %Margin ของ **3 กลุ่มบริการบนการ์ด** Profit Per Route (`lib/pi/route.ts`) ·
เกณฑ์ **> 10% / 5–10% / < 5%** (10% พอดี = เหลือง · `above()`) · %Margin = Σกำไร ÷ Σรายได้ (= กำไรเฉลี่ย/เที่ยว ÷ รายได้เฉลี่ย/เที่ยว)
ห้ามเฉลี่ย % รายเที่ยว · รายได้ 0 แล้วขาดทุน = −100% (`routeMargin` ตัวเดียวกับตารางจัดอันดับ) · ตารางเขียนว่า "% ของสาขา"
แต่ตัวชี้วัดชื่อ Route จึงนับรายเส้นทาง (ยังไม่ได้ถามยืนยัน — ถ้าหมายถึงสาขาแก้ที่ `routeMarginValues()`) ·
**Empty Return ใช้เกณฑ์ของแท็บ Empty Trips** (เจ้าของงานสั่ง · `lib/pi/empty.ts`): รายเส้นทางตามทิศ ≤ P25 เขียว · P25–P75 เหลือง · > P75 แดง
แล้วนับสีสูตรเดียวกับตัวอื่น (ไม่ใช้คะแนนต่อเนื่อง/ติดลบของแท็บ) · ให้สีตามตัวกรองของหน้า**ยกเว้นกลุ่มบริการ** (เที่ยวเปล่าไม่มี sg) ·
P25/P75 ไม่ตามต้นทาง/ปลายทาง · ข้อมูลที่ P25 = P75 ไม่มีเหลือง · ช่องที่ไม่มีข้อมูลยังไม่นับทั้งคะแนนและฐาน
คะแนนหมวด/รวมบอก "คิดได้ x จาก y" ไว้ (เจ้าของงานเลือก) · ชุดตัวอย่างมี Ton-km แพงกว่าเกณฑ์มาก เกือบทุกคันแดง — ผลของเกณฑ์ ไม่ใช่บั๊ก ·
กล่องแจ้งผลขึ้น DemoDash ผ่าน `PiReportProvider` ให้บรรทัดรวมอ่าน · ผลที่ส่งเข้า `PiBox` ต้อง `useMemo`/คงที่ ·
**กดกล่องคะแนนรวม /100 = ป็อบอัพที่มาของคะแนน** (`PiDetailModal` · เจ้าของงานขอ 25 ก.ย. 2569): ทุกหมวด × ทุกตัวชี้วัด —
วัดอะไร · ข้อมูล/ตัวกรองที่ตาม · ชิปเกณฑ์สี + ค่าเกณฑ์ที่คิดจากข้อมูล (`MetricResult.basis`: P25/P75 · P75 ของ DR/DIR) · แถบ + จำนวนเขียว/เหลือง/แดง ·
สูตรแทนค่า แล้วตารางบวกรวมท้าย · ข้อความอยู่ใน `METRICS` (`measure`/`source`/`criteria`) ที่เดียว · อ่านจากผลที่กล่องแจ้งขึ้นมา ไม่คิดซ้ำ
(`usePiReports` จึงเทียบผลทั้งก้อน ไม่ใช่แค่คะแนน) · portal ไป `#view-dash` แบบ TripsModal

**ETL เติม `bn` (จำนวนบิลของใบนั้น) กับ `cus` (รหัสผู้จ่ายเงินไม่ซ้ำ) ให้ทุกเที่ยวที่จับคู่บิลได้** — หน้า Demo ใช้เป็นตัวหารของ
กำไร/บิล และ กำไร/ลูกค้า · **ลูกค้า = ผู้จ่ายเงิน** (สด/เชื่อต้นทาง → ผู้ส่ง · ปลายทาง → ผู้รับ) กติกาเดียวกับ `src/alloc.py` ·
บิลเคลียร์นับเป็นบิลแต่ไม่นับเป็นลูกค้า · **ต้องนับก่อนตัวกรองสถานะชำระเงิน** ไม่งั้นบิลที่ชำระแล้วหายไปเงียบ ๆ
(`old_debtors.json` เก็บเฉพาะบิลค้างชำระ แต่ `bn`/`cus` นับทุกบิล) · **จำนวนลูกค้าเป็นเซ็ตไม่ซ้ำทั้งชุดที่กรอง ห้ามบวกรายเที่ยว**
· ไฟล์ costrev ที่สร้างก่อน 21 ก.ย. 2569 ไม่มีสองคีย์นี้ ต้องรัน `build_costrev.py` ใหม่

**รายการที่น้ำหนัก/ขนาดเชื่อไม่ได้ปันต้นทุนตามรายได้** (`etl/src/alloc.py` `data_flag`/`pool_of`/`share_in_trip` · เจ้าของงานสั่ง 23 ก.ย. 2569
— เปลี่ยนจากเอกสาร Cost Allocation Spec) สามแบบ: ไม่มีน้ำหนักและขนาด (เงื่อนไข 4 เดิมนับ 1 ชิ้น = 1 ตัน) · ชิ้นเดียวเกิน 10 ลบ.ม. ·
ไม่มีน้ำหนัก + ปริมาตรรวม < 0.001 ลบ.ม. (ขนาดหลอก 1×1×1) · **แบ่งต้นทุนเที่ยวสองก้อนตามสัดส่วนรายได้** ก้อนรายการผิดปกติ =
ต้นทุนเที่ยว × รายได้ของมัน ÷ รายได้ทั้งเที่ยว ที่เหลือแบ่งรายการปกติตามภาระงานเหมือนเดิม · เที่ยวรายได้รวม 0 ใช้ภาระงานทั้งเที่ยว ·
ต้นทุนที่ปันได้ทั้งหมดเท่าเดิมทุกสตางค์ แต่**ย้ายระหว่างลูกค้ากับของที่ไม่เข้าลูกค้า**: บิลเคลียร์ส่วนใหญ่ไม่มีน้ำหนัก/ขนาด
(ข้อมูลจริง 342/439 รายการ) ต้นทุนบิลเคลียร์จึงลดจาก 805,878 เหลือ 34,230 และเข้าลูกค้าเพิ่มราว 7.7 แสน ·
รอบแรก/สอง/สามของ `build_alloc.py` ต้องคิดสัดส่วนผ่าน `share_in_trip()` ตัวเดียวกัน (`TripAcc` แยกยอดปกติ/ผิดปกติ) ·
Demo › กำไรลูกค้าติดป้ายเล็ก "ปันตามรายได้" เมื่อรายได้จากรายการพวกนี้ ≥ 50% (`lib/alloc/review.ts` ต้องตรงกับ `needs_review`)
นับรวมภาพรวม/Top 10 ตามปกติ

**ETL เติม `wt` (น้ำหนักสินค้ารวมต่อใบ หน่วยตัน) ให้เที่ยวที่จับคู่บิลได้** — ต่อแถวบิลใช้ `line_weight_kg()`:
**ยึด `น้ำหนักรวม` ในไฟล์ · เป็น 0 ค่อยใช้ จำนวน × น้ำหนักต่อหน่วย** (เจ้าของงานเคาะ 23 ก.ย. 2569 · ชุดตัวอย่าง
4,166.5 ตัน เหลือน้ำหนัก 0 แค่ 3 เที่ยว) · **ห้ามเปลี่ยนเป็น "ผลคูณชนะเสมอ"** เคยลองแล้ว ช่องน้ำหนักต่อหน่วยบางแถว
เก็บน้ำหนักทั้งรายการ (196 × 9,800 แต่รวม 9,800) ได้รถ 10 ล้อพ่วงบรรทุก 1,920 ตัน · ไม่นับบิลเคลียร์ ·
นับก่อนตัวกรองสถานะชำระเงินเหมือน `bn` · ไฟล์ที่สร้างก่อน 23 ก.ย. 2569 ไม่มีคีย์นี้ ·
ข้อมูลยังมีเที่ยวที่ไฟล์ผิดเองทั้งสองช่อง (รถ 10 ล้อ 145 ตัน) — กติการายแถวแก้ไม่ได้

**ต้นทุน/Ton×Kms (ยังไม่ขึ้นหน้าจอ — เจ้าของงานให้เก็บวิธีคิดไว้ก่อน 23 ก.ย. 2569)**
Ton×Kms = `wt` × `km` · ต้นทุน/Ton×Kms = `cost` ÷ Ton×Kms · ค่าเสื่อม/Ton×Kms = `dep` ÷ Ton×Kms (ตรงกับ Excel ของเจ้าของงาน) ·
ชุด `inProfitScope()` · `wt` หรือ `km` เป็น 0 = หารไม่ได้ ไม่นับ (เที่ยวเปล่า `wt` = 0 จึงไม่อยู่ในตัน-กม. เองโดยอัตโนมัติ) ·
**รวมหลายเที่ยวใช้ "เฉลี่ยค่าต่อเที่ยว" ไปก่อนตามที่เจ้าของงานเลือก** ทั้งที่รู้ว่าเที่ยวบรรทุกไม่กี่กิโลดันค่าขึ้นได้มาก
(ชุดตัวอย่าง เฉลี่ยต่อเที่ยว 81.84 · Σต้นทุน÷ΣTon×Kms 11.89 · ค่ากลาง 16.62) — จะปรับทีหลัง จึงต้องเขียนสูตรรวม
ไว้ในฟังก์ชันเดียวใน `lib/` ให้เปลี่ยนวิธีได้ที่เดียว · ทางเลือก Σ÷Σ ก็มีจุดอ่อน: เที่ยวน้ำหนักเกินจริงดึงค่าลงแรง
จึงควรคู่กับป้ายเตือนเกินความจุ (`capacityKg`) และค่ากลาง

**ส่วน "Customer Performance" (เดิม "กำไรลูกค้า") ของ Demo (`dash-demo/CustomerProfitTab.tsx` · สเปก `ปรับปรุงโมเดล.pdf` 22 ก.ย. 2569) ไม่ใช้ `trips` เลย**
— อยู่ใน `STANDALONE` ของ `DemoDash` ต้องเข้าได้แม้ไฟล์ต้นทุนหาย · สองส่วนต่อกัน คนละชุดข้อมูล ตัวกรองไม่เกี่ยวกัน:
· **ส่วนที่ 1** ชุด `alloc/` (ตัวเดียวกับ "กำไรลูกค้า (ปันส่วนต้นทุน)") — **ไฟล์ต้นทุนของชุดจริงอ่านจาก `etl/data/Dashboard real data/`
  ไฟล์เดียวกับ Executive Dashboard** (เจ้าของงานเคาะ 24 ก.ย. 2569 · เดิม `etl/data/travel/` ที่ไม่มีไฟล์ ส่วนนี้จึงค้างเป็นข้อมูลตัวอย่าง ·
  งาน `al` ของ plugin เฝ้าโฟลเดอร์เดียวกับ `cr`) · วิธีปันไม่เปลี่ยน · `build_alloc.py` ออกไฟล์เพิ่ม 3 ไฟล์
  `cust_months.json` (ลูกค้า × เดือน แอปยุบเองตามตัวกรองปี/เดือน) · `top.json` (คีย์ `"ปี|เดือน"` · ช่วงเดือน `"2026|03-05"` = `allocTopKey()` → Top 10 **กำไรสูงสุด/ขาดทุนมากสุดเป็นบาท**
  **คัดใน ETL** ทุกช่วงที่ตัวกรองเลือกได้ — เดิมจัดด้วย % แล้วติดแต่รายบิลเดียวต้นทุน ~0 = 100% เพราะบิลกรอกขนาดหลอก
  1×1×1 ซม. (2,358 รายการในข้อมูลจริง) เปลี่ยน 23 ก.ย. 2569 · ต้นเหตุแก้แล้วด้วยการปันตามรายได้ ดูหัวข้อถัดไป) · `bills.json` (บิลรายใบ**เฉพาะรายที่ติด Top 10**
  ได้จากการเดินไฟล์บิลรอบที่สาม — ข้อมูลจริงบิล ~2 ล้านใบ เก็บทุกใบไม่ไหว) · แอปกดดูบิลได้เฉพาะแถวที่ติด Top 10 มีโน้ตบอกข้อจำกัด ·
  `marginOf()` ในแท็บต้องตรงกับ `margin_of()` ใน ETL (รายได้ 0 แล้วขาดทุน = −100) ไม่งั้นป้าย Top 10 กับบิลที่แนบมาไม่ตรงกัน ·
  กำไร = 0 นับเป็น "ทำกำไร" · เส้นในการ์ดใหญ่เป็นลายตกแต่ง · การ์ด 3 ใบและแท่งกราฟ %Margin กดเพื่อกรองตารางได้ ·
  ไฟล์ alloc ที่สร้างก่อน 22 ก.ย. 2569 ไม่มีสามไฟล์นี้ → แท็บขึ้นข้อความให้รัน ETL ใหม่ (หน้าเดิมยังใช้ได้ `fetchOptional`)
· **ส่วนที่ 2** ชุด `debtors/` จากไฟล์ `ข้อมูลการรับชำระ_วิเคราะห์ 99.xlsx` — ไฟล์รุ่นใหม่มี**สองชีต** ("สรุปวิเคราะห์" อยู่ก่อน `Sheet1`)
  `build_debtors.py` จึงต้องเลือกชีตชื่อ `Sheet1` ไม่ใช่ชีตแรก (ETL รุ่นก่อนอ่านชีตสรุปแล้วล้มว่าไม่มีคอลัมน์) และอ่าน
  วันที่อ้างอิง (J1/K2 ของชีตสรุป) ใส่ `manifest.refDate` · **ค่าเริ่มต้นของช่อง "ข้อมูล ณ วันที่" = 31/05/2026** (`DEFAULT_AS_OF` ใน
  `OverdueSection.tsx` · เจ้าของงานสั่ง 24 ก.ย. 2569) ถ้าไฟล์เริ่มหลังวันนั้นถอยไปใช้ `refDate` → `asOf` · **สถานะคำนวณในแอป**จากวันที่ที่เลือก
  (วางบิล ≤ วัน · ชำระ = จบ ≤ วัน · ค้าง = ครบกำหนด < วัน · ยังไม่ถึง = ที่เหลือ) ตรวจแล้วเท่าชีตสรุป ณ 01/03/2569 ทุกช่อง ·
  คอลัมน์วิเคราะห์ 17 คอลัมน์ในไฟล์ **ไม่อ่าน** · ป็อบอัพรายลูกค้า: เกินกำหนด = มากสุด (เฉลี่ยในวงเล็บ) · เครดิต = ค่าที่พบบ่อยสุด ·
  ชุดตัวอย่างของ debtors ไม่มีใน repo (ดู README ในโฟลเดอร์) บน Pages ส่วนนี้จึงขึ้นข้อความบอกวิธีสร้างไฟล์แทน
· **23 ก.ย. 2569** เจ้าของงานให้**คงส่วนที่ 1 ไว้แบบเดิมทั้งหมด** (การ์ดนับคน + กราฟ 8 ช่วง %Margin + ตารางใต้กราฟ)
  — ลองทั้งการ์ดยอดเงิน/กราฟ 3 แท่งตามสเปกรุ่นแก้ และกราฟ 3 แท่งนับคนแล้ว เจ้าของงานเลือกกลับเป็น 8 ช่วงทั้งสองรอบ
  ห้ามเปลี่ยนอีกโดยไม่ถาม · **24 ก.ย. 2569 เพิ่มยอดเงินใต้ชิปของการ์ด 3 ใบ** (`Hero foot`) = กำไรสุทธิของกลุ่มนั้น
  (ทั้งหมด = กำไรสุทธิรวม · มีกำไร = กำไรรวม · ขาดทุน = ขาดทุนรวม ไม่ใส่เครื่องหมายลบ) ตัวเลขการ์ดยังนับคนเหมือนเดิม ·
  ส่วนที่ 2 เปลี่ยนเป็น**การ์ดใหญ่ไล่สี 5 ใบ** (`Hero` ชุดเดียวกับส่วนที่ 1 · `.dso-heroes`) เพิ่ม **DSO แบบมาตรฐาน**
  (ยอดค้าง ÷ ยอดวางบิล × วันตั้งแต่ใบแรกในไฟล์ถึงวันที่เลือก) · กราฟเป็น**ยอดเงิน**ไม่ใช่จำนวนใบ · ยอดชำระแล้ว
  จัดช่วงตาม **วันที่จบ − วันครบกำหนด** (เจ้าของงานเลือก) · ปุ่มหัวกราฟเลือกได้ ยอดค้าง / ชำระแล้ว / ทั้งคู่
  (เหลืออย่างน้อยหนึ่ง · ทั้งคู่ = แท่งคู่ข้างกัน ไม่ซ้อน เพราะยอดชำระใหญ่กว่า ~10 เท่า) · กดแท่ง = ป็อบอัพรายลูกค้าของช่วงนั้น
  สองแท็บ ยังค้าง / ชำระแล้ว เปิดตรงแท็บของแท่งที่กด · **ยังไม่ทำ**: กล่องข้อความวิเคราะห์/ข้อเสนอแนะ (ทำทีหลัง)
  และตัวกรอง "รวม/ไม่รวมบิลค้างชำระ" (ต้องแก้ `build_alloc.py` ให้อ่าน `สถานะการชำระเงิน` — พักไว้)
· `DBar` รับ `onBarClick`/`activeIndex`/`showValues`/`tooltipExtra` และ `Hero` รับ `onClick`/`active`/`vSub` เพิ่ม (ทั้งหมด optional)

**แท็บ "Inefficient Transportation Cost" (เดิม "ต้นทุนที่จมกับที่ว่าง") ของ Overall Dashboard (`dash-costrev/lf/` · สูตรใน `lib/loadfactor/calc.ts` · ETL `etl/build_loadfactor.py`)**
สเปกจากไฟล์ `lf_executive_dashboard.html` ที่เจ้าของงานส่ง 22 ก.ย. 2569 — เอา 5 ส่วนแรก (ภาพรวม · เสียตรงไหน · คุ้มทุน ·
เที่ยวไหนควรแก้ · จำลองผล) ไม่รวมส่วนเอกสารสำหรับทีมโมเดล · ชุดข้อมูลอิสระ `loadfactor/` (ตัวอย่าง `etl/sample_data/LoadFactor/`
จริง `etl/data/Loadfactor/` plugin job `lf`) **ไม่ใช้ `trips` ของ costrev** อยู่ใน `STANDALONE` ของ `CostRevDash` ·
`trips.json` เก็บเป็นคอลัมน์ ~1.3 MB ต่อ 5,700 เที่ยว ทุกส่วนคิดสดในเบราว์เซอร์ · **ใช้ Max LF/ต้นทุนรวม/รายได้/เป้าของกลุ่มจากไฟล์
แต่ Idle = ต้นทุนรวม × MAX(0, 1−LF) และ Recoverable = ต้นทุนรวม × MAX(0, เป้า−LF) คำนวณใหม่** (คอลัมน์ AP ในไฟล์ติดลบได้เมื่อ LF > 100%
ห้ามใช้) · นับเฉพาะสถานะข้อมูล ปกติ + เฝ้าระวัง · เส้นทางใช้คอลัมน์ "เส้นทางมาตรฐาน" (L) ไม่ใช่ "เส้นทาง" (K) ·
Break-even = Σต้นทุนรวม ÷ Σ(รายได้ ÷ LF) · `manifest.check` เก็บยอดรวมที่ ETL คิด หน้าเว็บตอนไม่กรองต้องเท่ากัน ·
ตัวกรองสองชั้น: แถบหัว (ปี + ช่วงเดือน · ประเภทรถ) + กดแถวชนิดรถ/เส้นทางในส่วน "เสียตรงไหน" กรองทั้งแท็บ (ชิปใต้หัว) —
รายการชนิดรถไม่กรองด้วยชนิดรถที่กดอยู่ (และกลับกัน) · ส่วนแนวโน้มเทียบปีไม่ตามตัวกรองปี/เดือน ใช้เฉพาะเดือนที่ปีล่าสุดมีข้อมูล ·
**กราฟจุด 4 กลุ่ม วาดรายกลุ่ม (ชนิดรถ/เส้นทาง) เป็นค่าเริ่มต้น** รายเที่ยวเปิดได้เมื่อกรองเหลือ ≤ 1,000 เที่ยว ไม่งั้นโชว์ตาราง
เรียงตามต้นทุนที่จมแทน (เจ้าของงานเคาะ — ข้อมูลจริงหลายหมื่นเที่ยว ห้ามตัดข้อมูลทิ้ง) · เส้นแบ่งโหมดค่าเฉลี่ยถ่วงด้วยจำนวนเที่ยว
จุดกลุ่มจึงให้เส้นเดียวกับรายเที่ยว · รายการจัดอันดับ/คุ้มทุนอยู่ใน `GrowBox` (เส้นทางจริงเกือบร้อยกลุ่ม) ·
**แท่งทุกตัวของแท็บนี้ใช้รูปเดียวกับแท็บอื่น** (ปลายมน พื้น `--d-rail` ไม่มีกรอบ) แทนลาย hazard เหลือง + ม่วงน้ำเงินของสเปกเดิม
(เจ้าของงานสั่ง 23 ก.ย. 2569) สีจาก `--lf-used` (เขียวน้ำทะเล = สีการ์ดกำไร) กับ `--lf-idle` (แดงส้ม = สีการ์ดขาดทุน) ·
**ท่อนของแถบตู้รถชื่อคลาส `.seg` ชนกับปุ่มแบ่งช่องในส่วนที่ 1 ของ `index.css`** (ใส่กรอบ+มุมมนให้) จึงต้องล้าง `border/border-radius` ใน `.lf-cargo .seg`

**แท็บ "Contribution Margin" (เดิม "กำไรส่วนเกิน/ตัน-กม.") ของ Overall Dashboard (`dash-costrev/tonkm/` · สูตร `lib/tonkm/calc.ts` · 23 ก.ย. 2569)**
ใช้ชุด `loadfactor/` เดียวกับแท็บต้นทุนที่จม (STANDALONE) — ETL เก็บ `km` (ระยะทาง) · `wt` (น้ำหนักจริง **ตัน**) · `vc` เพิ่ม
(ไฟล์เก่าไม่มี = null แท็บขึ้นข้อความให้รัน ETL ใหม่) · ตัน-กม. = wt × km · Contribution = รายได้ − VC (ไม่หัก FC) ·
**แถวที่ไฟล์ LF ไม่มีระยะทาง (ว่าง/0) ETL เติมจาก `routes.json` ด้วยเส้นทางมาตรฐาน** (`route_km()` · เจ้าของงานสั่ง 24 ก.ย. 2569 —
ไม่ต้องแก้ไฟล์ LF · แถวที่มีระยะทางใช้ค่าในไฟล์ · ต้นทุนรวม/FC/VC/LF ในไฟล์ไม่ได้คิดจากระยะทาง ตรวจแล้ว) ·
`manifest.distanceFill` + บรรทัด `[!]` ท้าย log บอกเส้นทางที่ยังหาระยะทางไม่เจอ · แก้ `routes.json` แล้วต้องรัน ETL ต้นทุน/ปันส่วน/LF ใหม่เอง
(plugin เฝ้าแค่ .xlsx) และนำเข้ารายงานค่าซ่อมใหม่ ·
**อัตรา = ΣContribution ÷ Σตัน-กม. ห้ามเฉลี่ยอัตรารายเที่ยว** (ชุดตัวอย่าง: ถูก 2.75 · เฉลี่ยตรง ๆ 4.56 · `manifest.check.tonKm` ต้องตรง) ·
Baseline ของปี Y = 0.4 × ปี Y−2 + 0.6 × ปี Y−1 **ทั้งปี** ไม่ใช้ปี Y (ป้าย "2ปี") · มีปีฐานปีเดียวใช้ปีนั้นปีเดียว (ป้าย "1ปี" ตามสไลด์) · ไม่มีเลย = "ไม่มีฐาน" — ปีแรกของข้อมูลจึงไม่มีฐานเสมอ · เป้า = Baseline × (1+X%) X กรอกเองเก็บ localStorage
`tonKmTargetPct` · สถานะ ≥100% / 80–99% / <80% · **Baseline ≤ 0 ไม่คิดเป้า/%** (เจ้าของงานเลือก — ลบ×1.05 ได้เป้า
ที่ขาดทุนมากขึ้น และลบ÷ลบทำให้ยิ่งขาดทุน % ยิ่งสูง) แต่**ป้ายขึ้น "ต่ำกว่าเป้า"** สีเดียวกับต่ำกว่าเป้ามาก และนับในการ์ดต่ำกว่าเป้า
(เจ้าของงานให้ใช้คำตามสไลด์ แทน "ขาดทุนผันแปร") · ไม่นับเที่ยวที่ตัน-กม. = 0 · การ์ด 4 ใบ (`TonKmCards`) อยู่ใน Demo ›
กำไรรายเส้นทางด้วย ต่อจากแถวกำไรเฉลี่ย/บิล (ไม่มีหัวบรรทัด/ปุ่มดูรายละเอียด — เจ้าของงานให้เอาออก) **ตามตัวกรอง ปี/ช่วงเดือน/ประเภทรถ/ชนิดรถ ของหน้า Demo** (เจ้าของงานเปลี่ยน 24 ก.ย. 2569 ตอนรวมเป็นหน้ายาว — เดิมตรึงเดือนล่าสุด · ไฟล์ LF ไม่มีต้นทาง/ปลายทางแยกและกลุ่มบริการ)
ตารางรายปี × ชนิดรถ**กดแถวได้** → ป็อบอัพ `TonKmRowModal` (ตัวเลขสรุป · ที่มาของเป้า · สลับรายเดือน/รายเส้นทาง/รายเที่ยว — อัตรารายเที่ยวโชว์ประกอบเท่านั้น) · การ์ดใน Demo กดแล้วเปิดแท็บนี้ผ่าน `openExecTab()` (sessionStorage `execDashTab` → `takeExecTab()` ใน CostRevDash — hash ของแอปต่อ ?tab= ไม่ได้)

**%การใช้งานรายคันย้ายจากเมนูแดชบอร์ดมาแท็บ "Vehicle Utilization" (เดิม "การใช้ประโยชน์ของกองรถ") ของ Overall Dashboard** (24 ก.ย. 2569 ·
สูตร `lib/fleetcompare/vehicleUse.ts`) — การ์ด %การใช้งานเฉลี่ยต่อคัน · ระยะทางรวมทุกเที่ยว อยู่ใต้การ์ดรถที่ขาดทุนสะสม/
สัดส่วนเที่ยวขาดทุน ตารางรายคันอยู่ท้ายสุดใน `SortTable` (เลื่อนในกล่อง) · เมนูแดชบอร์ดเดิมเอาออกแล้ว ·
**วันพร้อมใช้งานนับเฉพาะช่วงที่ไฟล์ต้นทุนมีข้อมูล (เที่ยวแรก–เที่ยวสุดท้าย) ไม่ใช่ถึงวันนี้** และตัดตามตัวกรองปี/ช่วงเดือนทั้งตัวเศษตัวหาร
(ของเดิมนับถึงวันนี้และไม่ตัดตัวหารตามปี) · ใบหลายทะเบียนนับเที่ยวให้ทุกคัน · ระยะทางรวมไม่นับเที่ยวที่ไม่มีระยะทาง
(บอกจำนวนไว้ใต้การ์ด) · ชุดตัวอย่างได้ %การใช้งานต่ำ (~2%) เพราะไฟล์ต้นทุนตัวอย่างมีไม่ถึงเที่ยวต่อคันต่อเดือน ไม่ใช่บั๊ก ·
การ์ด % ใช้ `Meter` (แถบยาวตามค่า) ไม่ใช่ `KC bar` ที่วาดแถบเต็มเสมอ

**Manager Dashboard เปลี่ยนทั้งหน้าตามสเปก 26 ก.ย. 2569** (`features/dash-manager/ManagerDash.tsx` · สูตร `lib/manager/manager.ts` ·
แทนเนื้อหา FleetDash ในเมนู `dash-fleet` — โค้ด FleetDash ยังอยู่ให้สถานะกองรถของฝ่ายจัดรถ) — **ใช้ไฟล์ต้นทุน (`inProfitScope`) + Load Factor
จากไฟล์ LF จับคู่ด้วยเลขที่ใบรายการ ไม่ใช้ใบที่บันทึกใหม่** (เจ้าของงาน: หน้านี้ให้ผู้จัดการดูรายวัน/เดือนของสาขา) ·
ตัวกรองช่วงเวลา รายวัน/รายเดือน/รายไตรมาส + เลือกค่า **ตั้งต้น = ช่วงล่าสุดที่ไฟล์มีข้อมูล** (ไฟล์ย้อนหลัง ไม่ใช่วันนี้) ·
**ผู้จัดการเลือกสาขาตอนเลือกตำแหน่ง** (`BranchPick` ในหน้าเลือกตำแหน่ง · sessionStorage `sessionBranch` ล้างพร้อมตำแหน่ง ·
รายชื่อ = สาขาในไฟล์ต้นทุน ∪ ไฟล์ลูกหนี้) หน้า Dashboard ล็อกสาขานั้น · **ผู้ดูแลระบบ = ทุกสาขา** (ตั้งต้น) มีตารางเปรียบเทียบรายสาขา
(ตารางอย่างเดียว — เจ้าของงานเลือก) เลือกสาขาเดียวแล้วตารางเปรียบเทียบหาย · ไม่มีตำแหน่ง "ผู้บริหาร" แยก ·
**แท็บหน้างาน = เที่ยวที่กำลังวิ่งในช่วง** = [วันปล่อยรถ, วันปล่อยรถ + max(1, ⌈กม. ÷ 500⌉)] ทับช่วงที่เลือก (กฎของ `tripEta`) ·
การ์ด 4 ใบ + ตาราง · LF ≥ 70 ผ่าน · 40–<70 เฝ้าระวัง · < 40 ไม่ผ่าน (ต่างจาก PI) · **เที่ยวเปล่า = LF 0%** · เที่ยวอื่นที่ไม่มีในไฟล์ LF
= "ไม่มี LF" สีเทา นับในเที่ยวทั้งหมดแต่ไม่เข้าสามสี (ชุดตัวอย่างจับคู่ได้แค่ ~8% เพราะสองไฟล์สุ่มคนละชุด) ·
**แท็บการเงิน = เที่ยวที่ปล่อยรถในช่วง** (ไม่ซ้ำข้ามเดือน) · ลูกหนี้ = บิลที่**วางในช่วง** สถานะ ณ **วันสุดท้ายของช่วง**
(`ageBills` ตัวเดียวกับ Customer Performance) · ยอดค้างรวม = ยังไม่ชำระทั้งหมด · DSO = ยอดค้าง ÷ ยอดวางบิล × วันในช่วง ·
กดการ์ดค้าง 1–30 / 31+ = กรองตาราง · ตารางทุกตัวเรียงได้ + **แถวกรองรายคอลัมน์** (`SortTable` prop `filterRow` + `dash-manager/colFilter.tsx`)
ช่องค้นหาเลขที่ใบ/บิล/ลูกค้าอยู่ในแถวนี้ · การ์ดเหลือง = `Hero kind="warn"`

**เมนู "แดชบอร์ด" เปลี่ยนชื่อเป็น "Manager Dashboard"** (id ยังเป็น `dash-fleet` · เจ้าของงานสั่ง 24 ก.ย. 2569) และ**เหลือแค่มุมมอง "หน้างาน"**
— แถบแท็บ (หลัก · กำไรรายเที่ยว · กำไรลูกค้า · การใช้ประโยชน์กองรถ · สถานะกองรถ · Service Performance · ลูกหนี้) กับปุ่ม หลัก/หน้างาน
ซ่อนด้วย `SHOW_TABS = false` ใน `FleetDash.tsx` โค้ดยังอยู่ครบ เปิดกลับได้ด้วยค่านั้นค่าเดียว · `FleetStatusPage` (เมนูสถานะกองรถของฝ่ายจัดรถ) ไม่กระทบ


**แท็บ Damage Rate (`dash-costrev/DamageTab.tsx` · สูตร `lib/damage/damage.ts`)** รื้อใหม่ 23 ก.ย. 2569 ตามสเปก
`ออกแบบ Dashboard.pdf` (หน้าตา) + `dashboard คชจ.pdf` (นิยาม) · ใช้บิลที่ `ประเภทสินค้า = "บิลเคลียร์"` รวม `ราคารวม` ต่อใบ
(`clrAmt`/`clrN` จาก ETL) — มีเฉพาะเที่ยวที่จับคู่เลขที่ใบรายการได้ จึงแสดงเฉพาะใน Executive Dashboard ·
**เหลือสองอัตรา**: **Damage Rate** = มูลค่าบิลเคลียร์ ÷ รายได้รวม · **Damage Incidence Rate** = เที่ยวที่มีบิลเคลียร์ ÷ เที่ยวทั้งหมด
(Incident Rate ตัดออกทุกที่แล้ว) · **ตัวหารไม่รวมเที่ยววิ่งเปล่า** (เจ้าของงานเคาะ 20 ก.ย. 2569) ฐานที่ถูกคือ
`manifest.matched` − เที่ยวเปล่าที่จับคู่ได้ ซึ่งหมายเหตุใต้การ์ดตรวจให้ทุกครั้ง ·
**ระดับความเสียหาย** (สเปกเรียก "คำแนะนำ" เจ้าของงานเปลี่ยนชื่อ) เทียบกับ **P75 = Global Threshold** คิดจาก KPI
**รายเดือนของภาพรวมบริษัท** ในช่วงเวลาที่เลือก **ไม่ตามตัวกรองเส้นทาง/รถ** (`damageThresholds()` ต้องได้ชุดที่กรองแค่เวลา)
สูตร = `PERCENTILE.INC` ของ Excel · นับทุกเดือนแม้เที่ยวน้อย · **เกณฑ์ 5 กรณี (`CASES` · ไฟล์ `dashboard คชจ (1).pdf` หน้า 1 ·
เจ้าของงานสั่ง 24 ก.ย. 2569 แทน logic หน้า 7 เดิม)**: DR = 0 → ไม่มีความเสียหาย · ไม่เกินทั้งคู่ → ระดับต่ำ · DIR เกิน/DR ไม่เกิน → ปานกลาง
(ปรับปรุงกระบวนการ) · DIR ไม่เกิน/DR เกิน → ปานกลาง (ควบคุมความเสียหายมูลค่าสูง — **เดิมเป็นระดับต่ำ**) · เกินทั้งคู่ → **ความเสี่ยงสูง**
(เดิมชื่อ "ระดับสูง") · เท่ากับ P75 ไม่ถือว่าเกิน · ป้ายมี 4 ระดับ (`LEVELS`) ปานกลางสองกรณีแยกแนวทางกัน (วางเมาส์บนป้ายเห็นแนวทาง) ·
**เลือกเดือนเดียวยังจัดระดับได้** (P75 = ค่าของเดือนนั้น) แต่ขึ้นโน้ตให้เลือก ≥ 2 เดือน
· กลุ่มย่อยที่มีบิลเคลียร์แทบทุกกลุ่มจะเกิน P75 ของ Incidence เพราะเที่ยวน้อย (เสีย 1 จาก n เที่ยว = 100/n %)
ชุดตัวอย่างจึงไม่มีกลุ่มไหนเป็น "ระดับต่ำ" เลย — เป็นผลของสเปก ไม่ใช่บั๊ก ·
**ตัวกรองด้านบน = ปี + ช่วงเดือน (ตั้งแต่–ถึง เลือกได้เมื่อเลือกปีแล้ว) · เส้นทาง · ประเภทรถ · ชนิดรถ** ·
กราฟแนวโน้ม 2 เส้น **แกนเดียว** + ปุ่มเปิด-ปิดเส้นที่หัวการ์ด (เจ้าของงานเลือกแทนสองแกน) + เส้นประ P75 สีเดียวกับเส้น ·
**แสดงทั้งปีที่เลือกเสมอ แรเงาช่วงเดือนที่เลือก** · Damage Alert ใต้กราฟ = เดือนระดับสูง · วงกลม = สัดส่วนเที่ยวที่มีบิลเคลียร์
ตามชนิดรถ (5 อันดับ + อื่น ๆ) ใต้วงกลมเป็น % ตามประเภทรถ · **แท่งนอนเส้นทาง 10 อันดับ: ความยาว = Incidence ของเส้นทาง
แบ่งท่อนตามสัดส่วนเที่ยวของชนิดรถที่ใช้ในเส้นทางนั้น** (เจ้าของงานอธิบาย — ไม่ใช่ชนิดรถของเที่ยวที่เสียหาย) สีชนิดรถชุดเดียวกับวงกลม ·
ตาราง เส้นทาง × ประเภทรถ × ชนิดรถ ตั้งต้นแสดงเฉพาะกลุ่มที่มีความเสียหาย คอลัมน์ "เที่ยวที่มีบิลเคลียร์ / เที่ยวทั้งหมด" เป็นตัวคั่น
ไม่ใช่การหาร · **ตัวกรองหัวตาราง (เส้นทาง · ประเภทรถ · ชนิดรถ · ระดับ) ว่าง = ตามด้านบน เลือก = ทับมิตินั้น
ไม่มีตัวกรองเวลาของตัวเอง** เพราะระดับต้องเทียบ P75 ของช่วงเดียวกับด้านบน · Damage Alert ใต้ตาราง = ระดับสูง ·
**Alert ทั้งสองจุดและกราฟแท่งนับเฉพาะกลุ่ม ≥ 5 เที่ยว** (`MIN_TRIPS_ALERT` — เจ้าของงานเคาะ) ตารางยังแสดงระดับครบทุกกลุ่ม ·
**ห้ามเขียนหมายเหตุอธิบายใต้กราฟ** (เจ้าของงานสั่ง — มีได้แค่ป้ายสีบอกชื่อเส้น) · ป้ายแกน % ใช้ `pctTick`
ไม่งั้นค่าต่ำกว่า 1% จะขึ้น "0%" ทุกขีด

**(ลบแล้ว 25 ก.ย. 2569 — เก็บไว้เป็นประวัติ) ตารางสรุปท้ายแท็บ "กำไรรายเที่ยว" (`dash-costrev/SummaryTable.tsx` · ตรรกะ `lib/fleetcompare/compare.ts`)**
ยุบตาม **เส้นทาง × ชนิดรถ** แสดงแถบสัดส่วนเที่ยว บริษัท : ร่วม (คราม/เขียวน้ำทะเล) · รายได้ · ต้นทุนรวม · กำไรสุทธิ ·
Margin · **คำแนะนำ** (คงรถบริษัท · พิจารณาใช้รถร่วม · ทบทวน Margin ต่ำ/ขาดทุน · เปลี่ยนเป็นรถร่วม) ·
คำแนะนำดู 2 อย่างคู่กัน: **Margin** (เส้นแบ่ง 5% / 10%) กับ **ฝั่งที่ต้นทุนเฉลี่ยต่อเที่ยวถูกกว่า** (ต่างไม่ถึง 5% = พอ ๆ กัน) ·
**เทียบด้วยต้นทุน ไม่ใช่กำไร** เพราะรายได้มาจากงาน ไม่ได้มาจากรถ — ในกลุ่มเดียวกันฝั่งหนึ่งได้งานมูลค่าสูงกว่าได้ (ข้อมูลจริงต่าง ~25%) ·
**กลุ่มที่มีรถวิ่งฝั่งเดียวใช้ค่าเฉลี่ยต้นทุนของชนิดรถนั้นทั้งชุดมาแทน** แล้วติด `*` ให้รู้ว่าเป็นค่าประมาณ —
ส่วนใหญ่เป็นแบบนี้ (ชุดตัวอย่าง: มีรถจริงสองฝั่ง 48 จาก 643 กลุ่ม · ชนิดรถ 4 จาก 18) ห้ามเอาชนิดรถอื่นมาเทียบแทน
จนกว่าเจ้าของข้อมูลจะยืนยันว่ารถกลุ่มไหนใช้แทนกันได้ · **กดที่กำไรสุทธิ = เปิดหน้าต่างรายละเอียด** (ยอดรวม ต้นทุนแยกกลุ่ม
และรายการเที่ยวทั้งหมดของกลุ่มนั้น) ปิดด้วยปุ่ม/คลิกนอกกล่อง/Esc · ตั้งต้นโชว์ 10 อันดับแรกให้เห็นครบโดยไม่ต้องเลื่อน
สลับเป็นทั้งหมดได้ · **ห้ามตัดคอลัมน์ต้นทุนรวม และห้ามเอาคอลัมน์กลุ่มบริการกลับมา** (เจ้าของงานสั่ง 20 ก.ย. 2569
กลุ่มบริการเหลือเป็นตัวกรองเท่านั้น) · รถร่วม + รถร่วมนอกพิเศษ = ฝั่งเดียวกัน · ไม่รวมเที่ยววิ่งเปล่า

**`old_debtors.json` เก็บเฉพาะบิลที่ยังค้างชำระ (`สถานะการชำระเงิน = ยังไม่ได้ชำระ`)** — เจ้าของข้อมูล
เลือกทางนี้เอง 16 ก.ย. 2569 · ข้อมูลจริงเกือบทุกใบที่จับคู่ได้มีบิลติดมาด้วย ค้างชำระจริงราว 0.5%
ถ้าเขียนทุกบิลลงไฟล์จะใหญ่ระดับ GB เบราว์เซอร์โหลดไม่ไหว และ ETL เองก็หน่วยความจำไม่พอ
· บิลที่ชำระแล้วยังนับไว้ใน `manifest.debtorPaid` (จำนวน + ยอดรวม) ไม่ได้หายเฉย ๆ
· ข้อมูลตัวอย่างจับคู่ได้น้อยเลยไม่เห็นปัญหานี้ — **อย่าตัดสินขนาดไฟล์จากชุดตัวอย่าง**

**"ข้อมูลเก่า" ทั้งระบบมาจากไฟล์ในโฟลเดอร์เท่านั้น** (`costrev/old_records.json`/`old_debtors.json` = `state.fileOld`) — เลิกอ่านแท็บ "ข้อมูลเก่า*"/"ข้อมูลเก่าลูกหนี้*" ในชีตแล้ว (เจ้าของงานสั่ง 24 ก.ย. 2569) · `state.oldRecords` = `fileOld.records` + แถว "ข้อมูลใหม่" ที่พิมพ์ตรงในชีต (action `loadOld` ยังชื่อเดิม แต่ Apps Script อ่านแค่แถวพวกนั้น) · `state.oldDebtors` = `fileOld.debtors` · ฝั่งแอปกรองแถว source "เก่า" ที่ Apps Script รุ่นก่อนยังส่งมาทิ้งเอง จึงใช้ได้แม้ยังไม่ Deploy ใหม่

ทะเบียนรถในกองรถ: ไฟล์ต้นทางเป็นบันทึกปล่อยรถรายเที่ยว (88k แถว · ไฟล์ 24 ก.ย. 2569) ยุบเหลือ 1 ระเบียนต่อคันใน `refdata/fleet.json` (899 คัน)
· **ทะเบียนมีทุกคันรวมรถร่วมนอกพิเศษ** (611 คัน · บริษัท 136 · ร่วม 152) แต่**รถร่วมนอกพิเศษคิดต้นทุนแบบรถร่วม** ผ่าน `costFleetType()`
ใน `lib/refdata/index.ts` (ตารางอัตราต้นทุนมีแค่สองฝั่ง ถ้าส่งประเภทนี้เข้าสูตรตรง ๆ ค่าซ่อมหาแถวไม่เจอแล้วเป็น 0 เงียบ ๆ) —
หน้าจัดรถบันทึกประเภทรถของใบผ่านตัวนี้ · ฟอร์มเลือก "รถร่วม" แล้วเห็นรถร่วมนอกพิเศษด้วย (`matchesKind`) ·
%การใช้งานเฉลี่ยในแท็บกองรถนับเฉพาะรถบริษัท + รถร่วม (`AVG_FLEET_TYPES`) · ชื่อชนิดในไฟล์รุ่นนี้ที่เจ้าของงานเลือกให้:
`รถ 10 ล้อพ่วง(แม่)` = **ชนิดใหม่ใน vehicles.json** (ค่าซ่อมใช้ `repairKey` ของรถ 10 ล้อ · ปริมาตร/น้ำหนักยืมจากรถ 10 ล้อ แก้ได้ในหน้าตั้งค่า) ·
`รถ 10 ล้อพ่วง(ลูก)` → หางพ่วงคอก · `รถปิกอัพ` → รถปิกอัพ 3 ตัน (ทั้ง `KIND_MAP` ใน build_fleet.py และ `VEHICLE_ALIASES`) ·
แถวที่ประเภท/ชนิดรถเป็น "-" ไม่นับ · — `start` = วันปล่อยรถครั้งแรก (วิ่งครั้งแรกใน ม.ค. 2567 → 1 ม.ค. 2567) · วันที่ว่างเติมจากแถวบน**ตามลำดับแถวในไฟล์** · `kinds[]` = ทุกคู่ (ประเภทรถ, ชนิดรถ) ที่คันนั้นเคยวิ่ง ฟอร์มใช้กรองทะเบียนด้วย `vehiclesForKind()` ส่วน `fleetType/vehicle` หลักคือคู่ที่วิ่งบ่อยสุด · ชนิดที่อ้างอิงอัตราต้นทุนจากชนิดใกล้เคียงใช้ `approxFrom` แต่ปริมาตร/น้ำหนักสูงสุดเป็นค่าของตัวเอง · ชื่อชนิดรถมาตรฐานต้องยึดชื่อเดิมจากประวัติกองรถ (`รถเทรเลอร์` / `รถ 10 ล้อยาว`) ส่วน `รถ 6 ล้อคอก` และ `หางเทรเลอร์` เป็นชนิดใหม่ที่เพิ่มเข้ามา · 4 ชนิดจาก v5 ที่ไม่มีรถวิ่งจริง (รถ 10 ล้อสั้น / รถ 12 ล้อตู้แห้ง / รถ 6 ล้อตู้เย็น / รถ 6 ล้อกลาง) ติดธง `retired` — **ห้ามลบออกจาก `vehicles.json`** เพราะ golden test และใบเก่าใช้อยู่ UI ใช้ `ACTIVE_VEHICLES` / `vehicleOptions(current)` แทน `REF.vehicles` · `lib/store/roster.ts` = ฐานจาก fleet.json ทับด้วย localStorage (`fleetRoster` เก็บเฉพาะส่วนต่าง, `fleetRoster.removed` เก็บที่ลบ)

**การล้าง `public/data/real/` ของ plugin ต้องลบ `manifest.json` ก่อนและลบทีละไฟล์ในบล็อก try ของตัวเอง** — Windows ล็อกไฟล์ที่ dev server เพิ่งเสิร์ฟ ทำให้ `rmSync` ได้ EPERM เป็นบางไฟล์ ถ้าครอบทั้งลูปด้วย try เดียว พอไฟล์ที่สองพังจะข้ามการแจ้งสถานะ แล้วหน้าเว็บค้างกับข้อมูลเก่าโดยไม่มีอะไรบอก

ตารางรหัสลูกค้าเก็บ **รหัสต้นฉบับเต็ม 64 ตัว ระเบียนละ 32 ไบต์** ตำแหน่งระเบียน = เลขในรหัส (`CUS0000001` อยู่ที่ไบต์ 0-31) ตัวโหลดยังอ่านไฟล์รุ่นเก่า (6 ไบต์) ได้ แยกด้วยขนาดที่หารลงตัว · **การเทียบรหัสต้องเป๊ะทั้งสองทาง** `CUS` + เลข 7 หลักพอดี และรหัสต้นฉบับครบ 64 ตัว — ของเดิมใช้ `parseInt` กับ "ขึ้นต้นด้วย" ทำให้ `CUS1`/`CUS001`/`CUS0000001` ชี้ไปที่รายเดียวกัน

ลูกค้าที่ไม่มีในไฟล์ได้รหัสรันต่อจากตัวสุดท้าย เก็บใน localStorage `custNewCodes` (`lib/custmap/newCodes.ts`) **ออกรหัสจริงตอนกดบันทึกเท่านั้น** ตอน render เป็นแค่พรีวิว ไม่งั้นเปิดหน้าดูใบเฉย ๆ ก็กินเลขไปเรื่อย ๆ · ต้อง `await ensureCustCount()` ก่อนออกรหัสเสมอ ถ้าไม่รู้ว่าไฟล์มีถึงเลขไหนจะออกเลขทับของเดิม (`registerBills` กันไว้อีกชั้นด้วยการไม่ออกรหัสเลยถ้ายังไม่รู้จำนวน)

**หน้าบันทึกข้อมูลห้ามโหลด `custmap.bin` ทั้งก้อน** — `ensureCustCount()` ยิง HEAD อ่าน `Content-Length` แล้วหารด้วยขนาดระเบียน ได้จำนวนโดยไม่เสียเน็ต 18 MB ตารางเต็ม (`ensureCustMap()`) โหลดเฉพาะตอนมีรหัสต้นฉบับ hex 64 ตัวอยู่ในช่องผู้ส่ง/ผู้รับจริง ๆ เพราะ `codeFor()` รับเฉพาะรูปแบบนั้น ชื่อบริษัทที่พิมพ์เองไม่มีทางแมตช์อยู่แล้ว · ลำดับการเดารุ่นไฟล์ใน `headCount()` ต้องตรงกับ `build()` (ลอง 32 ก่อน แล้วค่อย 6) ไม่งั้นสองทางนับได้ไม่เท่ากันแล้วรหัสจะเลื่อน

## สถาปัตยกรรม

ข้อมูลเดินสองทางที่ไม่เกี่ยวกันเลย และเป็นกุญแจของทั้งระบบ:

```
ข้อมูลใหม่ (transactional)   ฟอร์ม → lib/sheet/client.ts → Apps Script /exec → Google Sheet
ข้อมูลเก่า (analytical)      Excel → etl/build_json.py → app/public/data/<dataset>/*.json → fetch
```

ทางแรกเขียนได้ ทางที่สองอ่านอย่างเดียวและแปลงล่วงหน้าตอน build — อย่าเอามาปนกัน

**`app/src/lib/` = ตรรกะ · `app/src/features/` = หน้าจอ** ฟีเจอร์ห้ามมีสูตรของตัวเอง

### สูตรต้นทุนมีที่เดียว

`lib/cost/computeCost.ts` เป็นแหล่งเดียว ของเดิมเขียนซ้ำ 3 ที่แล้วต้องให้ผลตรงกันตลอดโดยไม่มีอะไรบังคับ

`lib/cost/recCost.ts` ต่างออกไป — ทำงานกับ "ใบที่บันทึกแล้ว" ซึ่งอาจมาจากชีตเก่าที่มีแต่ยอดรวม จึงอ่านค่าที่เก็บไว้เป็นหลัก ส่วนแถวจากชีตเก่าที่ใช้สูตรคนละตัวอยู่ใน `lib/cost/adapters/oldRow.ts` แยกไว้ไม่ให้ปนกับสูตรหลัก

`npm test` เทียบ `computeCost` กับผลจากการรัน **โค้ด v5 ตัวจริง** ในแซนด์บ็อกซ์ 400 เคส ต้องตรงทุกฟิลด์ — เทสต์นี้คือ gate ของการแก้สูตร

### ค่าน้ำมัน = รายการบิล (ตั้งแต่ `_v5`)

โซนฝ่ายบัญชีกรอกค่าน้ำมันเป็น **ตารางบิล 1 บิล = 1 แถว** (`features/entry/panels/FuelBillTable.tsx` + `FuelSummary.tsx`) ไม่ใช่ช่องยอดรวม 6+3 ช่องแล้ว · `TripRecord.fuelBills` คือแหล่งความจริง ส่วน **9 ช่องเดิม (`fuelCash`/`fuelDownBill`/…/`fuelOffFleet`) ยังอยู่ครบ** แต่เป็นยอดสรุปที่ `applyFuelBills()` เขียนให้ — `computeCost`, golden test 400 เคส, คอลัมน์ชีต 9–28 และ `FleetDash.COST_ITEMS` จึงไม่ต้องรู้เรื่องบิลเลย **ห้ามลบ 9 ช่องนี้ทิ้ง**

ตารางแมป (ประเภท × วิธีจ่าย) → ช่องเดิม อยู่ที่ `FUEL_BUCKET_OF` ใน `lib/cost/fuelBills.ts` เป็น `Record` เต็มทุกคู่ **ห้ามใส่ fallback `??`** เพิ่มประเภท/วิธีจ่ายใหม่แล้วลืมเติมต้องให้ `tsc` แดง ไม่ใช่เงียบแล้วยอดตกหาย · `fuelCash` เดิมไม่แยกขาล่อง/ขาขึ้น เงินสดสองขาจึงยุบช่องเดียวกัน **ส่วนสรุปต้องคิดจากบิลตรง ๆ (`fuelBillsByCat`) ไม่ใช่จาก 9 ช่อง** ไม่งั้นแยกสองขาไม่ออก · แก๊ส (`gas`) อยู่นอกตารางบิล เป็นเชื้อเพลิงคนละชนิด ไม่มีลิตร/ราคาอ้างอิงชุดเดียวกัน

**`applyFuelBills()` ต้องปิด `fuelAutoOn` เสมอ** — `computeCost` บวก `fuelAuto` เข้า `fuelSum` เมื่อเปิดออโต้ (และ `recomputeTotals` ตีความ `undefined` ว่า "เปิด") ถ้าไม่ปิดจะนับซ้ำทับยอดบิล · ยอดอัตโนมัติย้ายไปเป็นกล่อง "เทียบต้นทุนมาตรฐาน" ที่ไม่บวกเข้าต้นทุน ตรงตามดีไซน์ · และยอดรวมต้อง**เขียนทับ ไม่ใช่บวกทบ** กดบันทึกสองครั้งยอดต้องไม่ขยับ

**`fieldsOwnedBy("account")` ใน `lib/store/save.ts` เป็นรายชื่อ hard-code** ไม่ได้อ่านจาก `ROLES.account.fields` (ที่เป็น `["cost"]` คำเดียว) — เพิ่มฟิลด์ต้นทุนใหม่แล้วลืมที่นี่ ข้อมูลจะหายเงียบ ๆ ทุกครั้งที่บันทึก และอาการดูเหมือน "แก้ไม่เข้า" เพราะ read-modify-write ดึงใบเก่าจากชีตกลับมาทับ ทำให้ไล่ผิดทาง

**การแปลงใบเก่าใช้ `ensureFuelBills()` ตอนเปิดใบในฟอร์ม ไม่ใช่ `migrateSchema()`** — `migrateSchema()` ถูกเรียกจาก `migrateFromLocalStorage()` ที่เดียว ซึ่งรันครั้งเดียวตลอดกาล (ธง `MIGRATION_FLAG`) ใบที่มาจากชีตหรืออยู่ใน IndexedDB แล้วไม่เคยวิ่งผ่านมันเลย · แถวสังเคราะห์ติดธง `src:"legacy"` เพื่อให้ตัวตรวจข้ามไป ไม่งั้นใบเก่าขึ้นป้ายแดง "รอหลักฐาน" ทั้งกระดาน · `_v5` ประทับตอนบันทึกเท่านั้น ไม่ใช่ตอนเปิดดู

หลักฐานบิล **เก็บแค่ลิงก์ที่ผู้ใช้วางเอง** ระบบไม่มีการอัปโหลดไฟล์ · การอนุมัติยอดประมาณการกดได้โดยฝ่ายบัญชี/ผู้จัดการ/ผู้ดูแลระบบ แต่แอปไม่มีระบบล็อกอิน `approvedBy` จึงเป็นบันทึกเชิงกระบวนการ ไม่ใช่หลักฐานทางบัญชี

### ข้อมูลอ้างอิง = ฐานกลาง + ค่าที่แก้เอง

`lib/refdata/*.json` คือฐานกลางที่ commit ขึ้น repo ส่วนที่ผู้ใช้แก้เอง (ราคาน้ำมัน/อัตราค่าซ่อม/สเปกรถ) อยู่ใน localStorage ผ่าน `lib/store/overrides.ts` และต้อง **ส่งเข้า `computeCost` อย่างชัดเจน** เป็นพารามิเตอร์ ไม่ใช่อ่านเองข้างใน

สเปกรถใน `lib/refdata/vehicles.json` เก็บปริมาตรบรรทุกเป็นลูกบาศก์เมตร (`volumeM3`) และน้ำหนักบรรทุกสูงสุด
เป็นกิโลกรัม (`capacityKg`) — **ไม่มีฟิลด์กว้าง/ยาว/สูงและห้ามคำนวณปริมาตรจากมิติ** · ค่าที่แก้จากหน้าการตั้งค่า
อยู่ใน localStorage คีย์ `vehicleSpecOverrides` และฟอร์มบันทึกเที่ยวต้องใช้ค่าที่รวมฐานกับ override แล้วในการเติม
น้ำหนักสูงสุด โดยช่องในฟอร์มและ Load Factor ใช้หน่วยกิโลกรัมเสมอ

`refdata/repair.json` เก็บปีเป็น **เลขสองหลักของ ค.ศ.** — `"24"` = ค.ศ. 2024 = **พ.ศ. 2567** แปลงด้วย `2000 + Number(y) + 543` เท่านั้น อย่าเติม `"25"` ไปข้างหน้า

`lib/repair/` คำนวณอัตราค่าซ่อมจากข้อมูลดิบที่ผู้ใช้วางมาจาก Excel (`parse.ts` อ่าน TSV/CSV · `rates.ts` แบ่งประเภทและหาร · `apply.ts` แปลงเป็น `RefOverrides`) หน้าจออยู่ที่ `features/settings/RepairImport.tsx` ใต้ตารางค่าซ่อม · **เก็บ "อัตรารายปี" ลงตาราง ไม่ใช่ POR ที่ถ่วงน้ำหนักเสร็จแล้ว** เพราะ `repSum()` ถ่วงให้เองตอนคำนวณ ถ้าเก็บ POR จะถูกถ่วงซ้ำสองรอบ 

**หน้านำเข้ารับรายงานค่าซ่อมไฟล์เดียว** (เจ้าของงานสั่ง 23 ก.ย. 2569) — ตารางที่ 2 (ระยะทางรวม/จำนวนวันรวม) ไม่ต้องแนบแล้ว
คิดจากไฟล์ต้นทุนในโมเดล (`costrev/trips.json` ทุกเที่ยว) ผ่าน `lib/repair/fromTrips.ts` · ระยะทาง = Σ km จากตารางเส้นทาง
(เที่ยว km = null ไม่ได้บวก แจ้งจำนวนไว้) · **จำนวนวัน = (ทะเบียน, วันที่ปล่อยรถ) ไม่ซ้ำ** เข้ากับ `computeCost` ที่บวกค่าซ่อมตามเวลา
1 วันต่อใบ · นับทุกคันใน `vs[]` แต่**ข้ามหางเทรลเลอร์เมื่อยุบเข้าหัวลาก** (ไม่งั้นระยะทางหัวลากเบิ้ล) ·
**รถร่วมนอกพิเศษไม่นับในตัวหาร** (เจ้าของงานเคาะ 24 ก.ย. 2569 — รายงานค่าซ่อมจริงไม่มีรถกลุ่มนี้: ทะเบียนเจอในรายงาน รถบริษัท 99% ·
รถร่วม 92% · นอกพิเศษ 1/211 คัน) แต่**ใบที่บันทึกใหม่ของรถนอกพิเศษยังคิดค่าซ่อมด้วยอัตรารถร่วม**เหมือนเดิม ·
**ตัวหารใช้เฉพาะเดือนที่มีในรายงานค่าซ่อม** (`monthsOfMaint` อ่านเดือนจากคอลัมน์วันที่ · ปีที่บางบรรทัดบอกแค่ปีใช้ทั้งปี) —
รายงานเดือนมกราคมถ้าหารด้วยวัน/ระยะทางทั้งปี อัตราต่ำไป ~12 เท่า · เดือนในรายงานที่ไฟล์ต้นทุนไม่มีเที่ยว → เตือนว่าอัตราสูงเกินจริง ·
**ไฟล์ต้นทุนตัวอย่าง (เที่ยวสุ่ม) ใช้เป็นตัวหารไม่ได้** อัตราสูงเกินจริงหลายสิบเท่า หน้าจอเตือนเมื่อ `manifest.isSample` · `parseOperations` ยังอยู่ (เทสต์ใช้) แต่หน้าจอไม่ได้เรียกแล้ว

**ระดับการยุบก้อนต้นทุนไม่เท่ากันสองฐาน — ตามโครงของตารางในแอป** อัตรา**ตามเวลา**ยุบ ปี × ชนิดรถ × **ประเภทรถ** แล้วหารด้วยจำนวนวันของประเภทนั้น (ตารางแยกแท็บรถบริษัท/รถร่วม) ส่วนอัตรา**ตามระยะทาง**ยุบ ปี × ชนิดรถ รวมทุกประเภท หารด้วยระยะทางรวม (ตารางมีแถวเดียวใช้ร่วมกัน) · ถ้าไฟล์ไม่มีคอลัมน์ประเภทรถ จะยุบตามเอกสาร (ปี × ชนิดรถ) แล้วลงอัตราเดียวกันทุกแท็บ โดยเก็บไว้ใต้คีย์ `ANY_FLEET` (`"*"`)

**ชื่อชนิดรถจากไฟล์ต้องผ่าน `canonVehicle()` ก่อนเสมอ** รายงานจริงพิมพ์ `รถเทรเล่อร์(แม่)` บ้าง `รถเทรเล่อร์ (แม่)` บ้าง ถ้าไม่ปรับให้ตรงคีย์ในตาราง อัตราจะถูกเก็บใต้ชื่อที่ไม่มีรถคันไหนเรียกใช้ · ปีให้อ่านจาก **วันที่ตามงวด** ก่อน `วันที่ซ่อม` เพราะรายงานจริงมีปีในวันที่ซ่อมพิมพ์ผิด (พบ `09/03/2556` ปนอยู่) · ไฟล์ `.csv` จาก Excel ไทยเป็น **Windows-874 ไม่ใช่ UTF-8** อ่านผ่าน `decodeThai()` เท่านั้น ไม่งั้นภาษาไทยกลายเป็นตัวขยะทั้งไฟล์โดยไม่มี error 

**`lib/repair/xlsx.ts` อ่าน `.xlsx` เองโดยไม่ใช้ไลบรารี** — `.xlsx` คือ ZIP ที่ข้างในเป็น XML และเบราว์เซอร์มี `DecompressionStream("deflate-raw")` ให้อยู่แล้ว จึงเดินสารบัญ ZIP เองแล้วแปลง `sharedStrings.xml` กับ `worksheets/sheet1.xml` เป็นตาราง แทนการลากไลบรารีอ่าน Excel ราว 400 KB เข้ามาใน bundle ที่ทุกคนต้องโหลด · **อ่านทุกชีตต่อกัน** (รายงานจริงแยกชีตรายปี 6701/6801/6901 — เดิมอ่านชีตแรกเลยได้ปีเดียว แก้ 24 ก.ย. 2569) · **วันที่ใน `.xlsx` เป็นเลขลำดับวันไม่ใช่ข้อความ** `toBEYear()` จึงรู้จักเลข 5 หลักช่วง 20000-80000 ว่าเป็นวันที่ (นับจาก 30/12/1899) ซึ่งไม่ทับกับปีสี่หลักที่ดักไปก่อนแล้ว · **PDF อ่านไม่ได้** เพราะชั้นข้อความของรายงานสลับรูปสระไทย ชื่อชนิดรถจะเพี้ยน — ปฏิเสธพร้อมบอกให้ส่งออกเป็น `.xlsx`

**อัตราค่าซ่อมเก็บเป็น "ชุด" ได้ (`lib/store/repairSnapshots.ts`)** นำเข้าไฟล์แล้วระบบบันทึก **ชุดใหม่** ให้ ส่วนการสลับชุดไม่สำรองชุดปัจจุบันให้ (ผู้ใช้กด "บันทึกชุดค่าปัจจุบัน" เอง) — ของเดิมที่เป็นฐานกลางกลับได้ด้วย "ย้อนกลับไปใช้ค่าเดิมทั้งหมด" อยู่แล้ว ถ้าสำรองทุกครั้งรายการจะรกด้วยชุด "ก่อน..." ที่ซ้อนกันไม่รู้จบ · **ต้อง `structuredClone` ตอนเก็บและตอนเรียกใช้** ไม่งั้นชุดที่สำรองไว้จะเปลี่ยนตามค่าที่ผู้ใช้แก้ต่อ แล้วย้อนกลับไม่ได้จริง

**อัตราที่นำเข้าอยู่แค่ใน localStorage ของเครื่องนั้น — ค่าตั้งต้นของทุกเครื่องคือ `refdata/repair.json` ที่ commit**
ปุ่ม "ส่งออกอัตราที่ใช้อยู่ (.json)" ในหน้านำเข้า (`lib/repair/export.ts` · เจ้าของงานสั่ง 24 ก.ย. 2569) ดาวน์โหลดตารางที่ใช้คำนวณจริง
(ผ่าน `repairTables()` ของ `computeCost.ts` ตัวเดียวกัน) **รูปเดียวกับ repair.json ทุกคีย์ + `_meta`** ลง Downloads · ขั้นอัปเดตค่าตั้งต้น:
เจ้าของงานส่งไฟล์มา → ตัด `_meta` → แทน `repair.json` → commit · **ทำครั้งแรกแล้ว 25 ก.ย. 2569** (ไฟล์ส่งออก 24 ก.ย. 2569 จากรายงานค่าซ่อมจริง + ไฟล์ต้นทุนจริง เจ้าของงานยืนยันให้เป็นค่าตั้งต้น) ·
**golden test ใช้ตารางค่าซ่อมของ v5 ที่แช่ไว้ใน `lib/cost/__fixtures__/repair-v5.json`** ไม่ใช่ `repair.json` — เทสต์ตรวจสูตร ส่วนอัตราเปลี่ยนตามรายงานได้ · **`_meta.costDataIsSample = true` ห้ามนำไปใช้** (ตัวหารจากเที่ยวสุ่ม
อัตราสูงเกินจริงหลายสิบเท่า) · repo เป็น public อัตราจากรายงานจริงจะเปิดเผยเมื่อ commit — เจ้าของงานต้องยืนยันก่อนทุกครั้ง

**ตารางค่าซ่อมคงไว้ตามชื่อชนิดรถในรายงานค่าซ่อม (17 คีย์) ไม่ต้องมี 19 แถว** — ชนิดรถในโมเดลชี้แถวผ่าน `repairKey` ใน vehicles.json
(`รถ 10 ล้อพ่วง(แม่)` → รถ 10 ล้อ · `รถ 6 ล้อคอก` → รถ 6 ล้อใหญ่) · **ช่องที่ไม่มีอัตรา = ค่าซ่อม 0 ต่อไป ไม่ยืมตาม `approxFrom`**
(เจ้าของงานเลือก 24 ก.ย. 2569 — `approxFrom` เป็นแค่ป้าย) · **รายงานค่าซ่อมตามงวดจริง** (29 ชีตรายเดือน 6701–6905 · หัวตารางแถว 4 ·
ตัวอ่านของแอปอ่านครบ) ใช้ชื่อชนิดรถเดิม 16 ชนิด ตรงคีย์ตารางทุกชื่อ · **ไม่มี `รถ 10 ล้อพ่วง(แม่)/(ลูก)` และ `รถปิกอัพ 3 ตัน`** — พ่วงแม่/ลูก
เป็นรถร่วมนอกพิเศษทั้งหมดจึงไม่ต้องแปลงชื่อเข้าตัวหาร (ตัดทิ้งไปพร้อมรถนอกพิเศษ) · ห้ามแปลงเข้าแถว "รถ 10 ล้อ" เพราะจะเพิ่มวัน/ระยะทางโดยไม่มีค่าซ่อม
· เส้นทางต้นทาง = ปลายทาง (วิ่งในพื้นที่ เช่น เชียงใหม่-เชียงใหม่) **ไม่ใส่ระยะทาง** (เจ้าของงานเลือก) ไม่เข้าตัวหารระยะทาง/ตัน-กม.

### บิล → ใบรายการ (เปลี่ยนโครง 22 ก.ย. 2569)

**ฝ่ายบริการลูกค้ากรอก "บิล" ไม่ใช่ "ใบรายการ" แล้ว** (สเปก `ปรับปรุงโมเดล (1).pdf`) —
`features/bills/BillEntry.tsx` (ไม่มีช่องเลขที่ใบรายการ · ระบบออกเลขที่บิลให้ตอนกดบันทึก ·
จำนวน/น้ำหนัก/กว้าง×ยาว×สูง → คิดปริมาตรให้ · ทุกค่าต้อง > 0 · มีหน้าสรุปให้ตรวจก่อนบันทึกจริง ·
บันทึกแล้วได้สถานะ "รอจัดรถ") · แท็บที่สองเป็นรายการบิลที่ยังไม่ได้จัดรถ ยกเลิกได้
**ยกเลิก = เปลี่ยนสถานะ ไม่ลบทิ้ง** เลขที่บิลเดิมจะได้ไม่ถูกใช้ซ้ำ

**ฝ่ายจัดรถรวมบิลเป็นใบรายการ** `features/dispatch/DispatchPage.tsx` — ติ๊กเลือกบิล → ระบบรวม
จำนวนบิล/ลูกค้า/น้ำหนัก/ปริมาตร/รายได้ · เลือกรถ **ประเภทรถ → ชนิดรถ → ทะเบียน** (เฉพาะสถานะ "ใช้งาน" · ชุดช่อง
`VehiclePickFields` ตัวเดียวกับหางพ่วง · ประเภทรถเทียบตรงตัว เลือก "รถร่วม" ไม่รวมรถร่วมนอกพิเศษ · เลือกทะเบียนก่อน = เติมประเภท/ชนิดให้
· กล่องค้นหารถ/ค้นหาทะเบียนเดิม**เอาออกแล้ว** เจ้าของงานสั่ง 24 ก.ย. 2569) →
**Load Factor คิดจากฝั่งที่เต็มกว่า** ระหว่างน้ำหนักกับปริมาตรเทียบความจุรถ (`vehicles.json`) ·
**ติ๊กบิลแรกแล้วตารางบิลล็อกเส้นทาง** (เจ้าของงานสั่ง 24 ก.ย. 2569 · `lib/route/enRoute.ts`) เหลือเฉพาะบิล**ต้นทางเดียวกัน** ที่ปลายทางเดียวกันหรืออยู่ในรายการ "จุดระหว่างทาง" ของคู่ ต้นทาง → ปลายทาง ของบิลแรก · `routes.json` ไม่มีระยะระหว่างปลายทาง จึงคิดระหว่างทางจากข้อมูลไม่ได้ ใช้ตารางรายคู่แทน — ค่าเริ่มต้นร่างจากถนนสายหลัก (ขาขึ้นตามทางผ่าน · ขาล่องจัดกลุ่มจุดส่ง กทม. ที่อยู่ใกล้กัน) ผู้ดูแลแก้รายคู่ได้ในหน้าตั้งค่า เก็บ localStorage `enRouteStops` **ของเครื่องนั้น** (เจ้าของงานเลือก) · จุดที่ยังไม่แน่ใจตำแหน่งตอนร่าง: ท่าลี่ · กองลอย · เวียงป่าเป้า · บิลแรก = บิลที่ติ๊กก่อนสุดที่ยังติ๊กอยู่ · ช่องเลือกทั้งหมดซ่อนจนกว่าจะติ๊กบิลแรก ·
**ขั้นที่ 1 แบ่งสองฝั่ง** ตารางบิล (ซ้าย · ตารางเต็ม + ตัวกรองเดิม) กับ "สถานะการบรรทุก" (ขวา · `LoadTruck.tsx` ตามแบบ
`Truck Load Factor (standalone).html` แต่ใช้สี/ฟอนต์ของโมเดล) รูปรถเติมของตามบิลที่ติ๊ก (ของในตู้ ยังไม่เต็ม = เหลือง · ≥95%/ล้น = แดง) + หลอด + ข้อความ Load Factor/คำเตือน
(ย้ายมาจากขั้นที่ 2 — ไม่มีซ้ำแล้ว) · มีหางพ่วง = วาดหางต่อท้าย **เติมตู้หัวก่อน ล้นไปตู้หาง** (`lib/dispatch/loadSplit.ts`
ใช้วาดรูปเท่านั้น Load Factor รวมยังคิดจากความจุหัว + หาง) ·
**หางพ่วง (ทะเบียนรถคันที่ 2)** เลือกเพิ่มได้ ไม่บังคับ ทุกหัว (เลือก ประเภท → ชนิด → ทะเบียน · ช่องหางเห็นเฉพาะชนิด `หาง*`
ผ่าน `isTrailerKind()` และช่องหัวตัดหางออก) **ความจุ = หัว + หาง รวมกัน** ทั้ง กก. และ ลบ.ม. → `capacity` ในใบเป็นยอดรวม ·
เก็บ `trailerPlate`/`trailerFleetType`/`trailerVehicle` (ต้องอยู่ใน `ROLES.dispatch.fields` ไม่งั้น saveRecord ทิ้ง) ·
ต้นทุนพยากรณ์ยังคิดตามชนิดรถของหัว (เจ้าของงานเคาะ 24 ก.ย. 2569) ·
เกินความจุฝั่งใดฝั่งหนึ่ง = กดยืนยันไม่ได้ · กดยืนยัน → ออกเลขที่ใบรายการ 13 หลัก สร้าง `TripRecord`
(ประทับ `_csDone` + `_dispatchDone` ให้เลย) แล้วเขียน `docNo` กลับลงบิลทุกใบพร้อมเปลี่ยนสถานะเป็น "จัดรถแล้ว"
**การจัดรถต้องไม่ค้างครึ่งทาง** (แก้ 23 ก.ย. 2569 — เดิม `saveRecord()` เขียนใบลงเครื่องก่อนแล้วพังตอนส่งชีต
ใบค้างแต่บิลยังรอจัดรถ กดซ้ำได้ใบซ้ำ คนขับเห็นงานเกิน) กันสามชั้น: ① ยังไม่เชื่อมชีต = กดยืนยันไม่ได้
② สร้างใบไม่สำเร็จ = `remove()` ใบในเครื่องทิ้ง บิลคงสถานะเดิม ③ ใบสร้างแล้วแต่สถานะบิลส่งไม่ถึงชีต =
`splitStuckBills()` (`lib/bill/reconcile.ts` จับคู่ เลขที่บิล + ผู้ส่ง กับ `TripRecord.bills`) ซ่อนบิลนั้นจากรายการที่จัดได้
พร้อมแถบ "อัปเดตสถานะบิล" · **`saveBills()` ไม่โยน error ตอนส่งชีตไม่ผ่าน** (คืน `synced=false`) ผู้เรียกต้องดูธงเอง ·
`useBills` ส่งบิลที่ `synced=false` ขึ้นชีตอีกรอบทุกครั้งที่โหลด (เดิมหัวไฟล์บอกว่า "ส่งรอบหน้า" แต่ไม่มีโค้ดทำจริง)

**ที่เก็บบิล = Google Sheet แท็บ "บิลรอจัดรถ" (Apps Script v17)** เพราะ CS กับฝ่ายจัดรถอยู่คนละเครื่อง
ต้องเห็นของกันและกัน · IndexedDB (`store "bills"`, DB v2) เก็บสำเนาไว้ใช้ตอนออฟไลน์เหมือน `TripRecord`
**ต้อง Deploy Apps Script ใหม่ทุกครั้งที่แก้ `Code.gs`** ไม่งั้น `GS_VERSION` ไม่ตรงแล้วบันทึกไม่ได้

**เลขที่บิล/เลขที่ใบรายการออกในเครื่องตอนกดบันทึก** (`lib/bill/number.ts` — 13 หลัก:
`5`/`6` + ปี พ.ศ. 2 หลัก + เดือน + ลำดับ 5 หลัก + สุ่ม 3 หลัก) เจ้าของงานเลือกทางนี้เอง —
**ไม่ซ้ำเท่าที่เครื่องนี้มองเห็น** สองเครื่องกดพร้อมกันยังชนกันได้ (ปัญหาเดียวกับ `custNewCodes`)
แก้ถาวรต้องย้ายการแจกเลขไปฝั่ง Apps Script

**กลุ่มบริการเปลี่ยนเป็น 5 ตัวใหม่** (`SERVICE_GROUPS_V2` ใน `types/bill.ts`: สินค้าทั่วไป · แช่เย็น ·
แช่แข็ง · บิลเคลียร์ · ของเหมาตีเปล่า) แทนชุดเดิมใน `enums.json` ทั้งหมด — ใบเก่ายังแสดงชื่อกลุ่มเดิมได้ แต่เลือกใหม่ไม่ได้

**ราคารวมของบิล** = คิดตามน้ำหนัก → น้ำหนักรวม × ราคา/หน่วย · คิดตามหน่วย → จำนวน × ราคา/หน่วย
(`billTotalOf` ใน `types/bill.ts` — สูตรอยู่ที่เดียว ห้ามคิดซ้ำในหน้าจอ)

**ต้นทุนพยากรณ์** `lib/forecast/forecast.ts` — ค่าเฉลี่ยต้นทุนจริงต่อเที่ยวจาก `costrev/trips.json`
N เดือนล่าสุด**ของไฟล์** (ไม่ใช่เดือนปัจจุบัน) ไล่ชั้น เส้นทาง×ชนิดรถ → ชนิดรถ → เส้นทาง → ทั้งชุด ·
ไม่รวมเที่ยววิ่งเปล่า · จำนวนเดือนตั้งค่าได้ที่หน้าการตั้งค่า (`features/settings/ForecastSettings.tsx`
เก็บใน localStorage `forecastMonths` ค่าเริ่มต้น 5) · ใช้สองที่: กล่องสรุปของหน้าจัดรถ (กำไร/รายได้/ต้นทุนพยากรณ์
กรอบเขียว-แดง) กับป็อบอัพของฝ่ายบัญชี · ใต้กล่องสรุปของหน้าจัดรถมี**รายละเอียดต้นทุนพยากรณ์แยกกลุ่ม** (เฉลี่ยต่อเที่ยว
เจ้าของงานขอ 23 ก.ย. 2569) ชื่อกลุ่มใช้ `COST_PART_LABELS` ใน `lib/forecast/forecast.ts` ชุดเดียวกับป็อบอัพฝ่ายบัญชี
ห้ามเขียนรายชื่อซ้ำในหน้าจอ · "อื่น ๆ" ติดลบได้ แถบวาดเฉพาะค่าบวก · หน้าจัดรถแสดง**จำนวนสินค้า (ชิ้น)** = ช่อง `qty` ของบิล
ทั้งในตารางบิลและการ์ดสรุป

**กดแถวในหน้า "รายการทั้งหมด" = ป็อบอัพรายละเอียดเที่ยว** (`features/records/TripDetailModal.tsx`)
สองมุมมอง: รายการบิล (ปันต้นทุนเข้าบิลตามสัดส่วนรายได้) · ต้นทุนจริงเทียบพยากรณ์รายกลุ่ม —
**เฉพาะใบที่บันทึกใหม่ในโมเดล** แถวข้อมูลเก่ากดไม่ได้ เพราะไม่มีบิลและไม่มีรายละเอียดกลุ่มต้นทุนให้เทียบ

**แผงทะเบียนรถ (Fleet Roster) ย้ายจากฟอร์มบันทึกข้อมูลไปอยู่ในแท็บ "สถานะกองรถ"** และตาราง
สถานะรายคัน**ตัดคอลัมน์ "ว่างมาแล้ว" ออก** · ตารางนี้อยู่ใน `GrowBox` (เลื่อนในกล่อง หัวตารางติดบน) และมีคอลัมน์**ประเภทรถ**
ต่อจากชนิดรถ (เจ้าของงานขอ 23 ก.ย. 2569) · ทะเบียนใน dropdown ของฟอร์มแสดงเฉพาะคันที่สถานะ "ใช้งาน"

**ปุ่มแนบรูปไมล์ของคนขับเป็นเดโม** — กดแล้วนับว่าแนบแล้วเพื่อปลดล็อกปุ่มจบงาน แต่ระบบไม่ได้เก็บไฟล์จริง
(เจ้าของงานเคาะ 22 ก.ย. 2569 · โมเดลนี้ไม่มีที่เก็บไฟล์ ดูหัวข้อหลักฐานบิล)

### workflow 3 ฝ่าย

`lib/record/roles.ts` ถือทั้งหมด: `ROLE_ORDER` (cs/dispatch/account — สามฝ่ายที่ต้องกรอกครบใบถึงจะสมบูรณ์), `ROLE_VIEWS` (แต่ละตำแหน่งเห็นเมนูไหน — ตั้งแต่ 22 ก.ย. 2569 cs เห็นแค่ `bills` · dispatch เห็น `dispatch` + `fleet-status` ไม่เข้าฟอร์มใบรายการแล้ว), `manager`/`admin`/`driver` เป็นตำแหน่งดูอย่างเดียว/ดูแลระบบ/คนขับ **ไม่นับ**ในความครบถ้วน — **ห้ามเติมตำแหน่งใหม่ลง `ROLE_ORDER`** ไม่งั้นใบทุกใบทั้งระบบจะกลายเป็น "ยังไม่ครบ" ทันที

**หน้า "บันทึกข้อมูล" สร้างใบใหม่ไม่ได้แล้ว** (เจ้าของงานเคาะ 24 ก.ย. 2569 — เดิมสร้างใบได้สองทาง ซ้ำกับ บันทึกบิล + จัดรถ)
โซนฝ่ายบริการลูกค้า/ฝ่ายจัดรถเปิดเฉพาะตอนแก้ใบที่เปิดจากรายการ (`zoneOpen()` ใน `EntryForm.tsx` เช็ค `editing`) ไว้แก้ข้อมูลผิด ·
ฟอร์มว่างเหลือโซนค่าใช้จ่าย (ไม่มีช่องเลขที่ใบ จึงบันทึกไม่ได้ ขึ้นข้อความให้เปิดใบ) · ผู้ดูแลระบบประทับ `alsoRoles` เฉพาะโซนที่เปิดอยู่จริง
ไม่งั้นฟอร์มว่างจะประทับว่าฝ่ายลูกค้า/จัดรถกรอกแล้วทั้งที่ไม่ได้กรอก ·
**ผู้ดูแลระบบเหลือเมนูเดียว "บันทึกข้อมูลรวม"** (`entry-all` · เจ้าของงานสั่ง 24 ก.ย. 2569 — ลองป็อบอัพแท็บย่อยและฟอร์มเต็มในหน้าเดียวแล้ว
เจ้าของงานเปลี่ยนเป็นแบบนี้) = **หน้าจริงของ 4 ฝ่ายเรียงต่อกัน** (`features/entry/EntryAll.tsx`): บันทึกบิล → จัดรถ → เที่ยวรถของฉัน →
บันทึกข้อมูล (ค่าใช้จ่าย) · **ห้ามทำหน้าตาแยกในหน้านี้ แก้ที่หน้าของฝ่ายนั้น** · ผู้ดูแลระบบไม่มีเมนู bills/dispatch/driver/entry แล้ว
(ฝ่ายอื่นเห็นหน้าของตัวเองแยกเหมือนเดิม) · ปุ่ม "แก้ไข" ของรายการทั้งหมด/ใบที่ยังไม่ครบส่งมา `#/entry` → `readHash()` ใน App
พาผู้ดูแลระบบไป `entry-all` แล้ว EntryAll เลื่อนไปส่วน Accounting (อ่าน `editRecordId` ตอน render แรก เพราะ EntryForm ลบคีย์ใน effect ก่อน) ·
**ปุ่ม "ทำซ้ำใบ" เอาออกแล้ว** (ใบใหม่สร้างจากหน้าจัดรถเท่านั้น · `lib/record/duplicate.ts` ยังอยู่ให้เทสต์)

การบันทึก (`lib/store/save.ts`) เป็น **read-modify-write ข้าม HTTP สองครั้ง** — โหลดใบล่าสุดจากชีต → ทับเฉพาะฟิลด์ของฝ่ายตัวเอง → เขียนกลับ **ถ้าอ่านชีตไม่สำเร็จต้องโยน `SaveAbortedError` ไม่ใช่เขียนทับ** ไม่งั้นงานฝ่ายอื่นหาย

### สถานะกองรถ · ปุ่มจบงาน

`lib/record/tripEta.ts` เดาว่ารถคันไหนยังวิ่งอยู่ โดยไม่มี GPS: **วันปล่อยรถ (ไม่มีค่อยใช้วันที่ใบ) + ระยะทาง ÷ `KM_PER_DAY` (500 ตรึงไว้ ไม่มีหน้าตั้งค่า) ปัดขึ้น อย่างน้อย 1 วัน** = วันที่คาดว่าถึง · ระยะทางเอาจากช่อง `dist` ก่อน ไม่มีค่อยเปิด `distanceFor()` · **ถ้าไม่รู้ระยะทางต้องถอยไปใช้กฎเดิม "ออกวันนี้ = กำลังวิ่ง"** ไม่งั้นใบพวกนั้นค้างสถานะเดินทางตลอดกาล (ช่องระยะทางผู้ใช้แก้เองได้และปล่อย 0 ได้)

ปุ่ม "จบงาน" (แท็บสถานะกองรถ + หน้า "งานของฉัน" ของคนขับ) เขียนผ่าน `lib/store/finishTrip.ts` — **ห้ามเรียก `saveRecord()`** เพราะตัวนั้นทับเฉพาะช่องที่ฝ่ายผู้กดเป็นเจ้าของ ฟิลด์ `_tripDone*` ไม่มีเจ้าของจะถูกทิ้งเงียบ ๆ · ฟิลด์พวกนี้**ไม่มีคอลัมน์ในชีต** เดินทางไป-กลับในคอลัมน์ `_DATA` (JSON) ที่ `readTripRecords_` อ่านกลับอยู่แล้ว จึงไม่ต้องแตะ `serialize.ts` ที่คอลัมน์เรียงตามตำแหน่ง · ทุกครั้งที่กดจะ append แถวลงแท็บ **`จบงาน`** เป็น log ดูย้อนหลัง **แยก `try` จาก `pushRecords`** log ที่พังต้องไม่ทำให้การบันทึกใบล้ม · **หน้าคนขับแสดง "งานที่รอออกเดินทาง"** = จัดรถแล้วแต่ยังไม่ถึงวันปล่อยรถ (`tripProgress().upcoming` = ไม่จบ และ today < start
เจ้าของงานขอ 23 ก.ย. 2569) ทั้งของรถตัวเองและทั้งกอง กดจบงานไม่ได้ ถึงวันปล่อยรถแล้วย้ายไป "กำลังวิ่ง" เอง
(`upcoming` กับ `moving` ไม่มีวันจริงพร้อมกัน) · คนขับระบุตัวเองด้วยการเลือกทะเบียนเก็บใน localStorage `driverPlate` เพราะ**ในใบไม่มีช่องชื่อคนขับ** (`drv` คือเบี้ยเลี้ยง)

### ที่เก็บในเครื่อง

IndexedDB (`lib/store/records.ts`, db `fleet-cost-model`) เป็นหลัก มี migration จาก localStorage ของ v5 ครั้งเดียว `migrateSchema()` ยกระดับใบเก่าเป็น `_v2/_v3/_v4` — **ธงพวกนี้ต้องถูกเขียนลงที่เก็บจริง** ไม่งั้นรอบหน้าขั้น `_v4done` จะประทับว่าฝ่ายนั้นกรอกแล้วทั้งที่ยังไม่ได้กรอก

`_v4` ยังเป็นตัวกรองของส่วน Load Factor ในแดชบอร์ด — ใบก่อนหน้านั้นไม่มีช่องความจุ ถ้าไม่กรองออกจะถูกอ่านว่า "ความจุ 0" แล้วโผล่เป็นเที่ยวเสียโอกาสทั้งหมด

`useRecords()` ต้องรอ migration ให้จบก่อนอ่าน IndexedDB ไม่งั้นรอบแรกได้ลิสต์ว่าง

## กติกาที่พลาดแล้วพัง

**`index.css` ส่วนที่ 1 คัดลอกมาจาก `<style>` ของ `index.html` บน branch `main` ทั้งดุ้น ห้ามแก้** ของใหม่ให้เติมในส่วนที่ 2 ท้ายไฟล์ (มีคอมเมนต์คั่นไว้) — เทียบความตรงด้วยการ diff กับต้นฉบับได้

**โทเคนสีของแดชบอร์ด (`--d-card`, `--d-ink`, `--d-num`) ประกาศไว้ใต้ `#view-dash` เท่านั้น** ทุกหน้าจึงต้องถูกครอบด้วย `<section class="view active" id="view-...">` ใน `App.tsx` ถ้าลืม ตัวแปรจะเป็นค่าว่าง กราฟกับตัวกรองกลายเป็นพื้นใสโดยไม่มี error

**ฟอนต์ทั้งโมเดล = LINE Seed Sans TH เก็บในเครื่อง** (`src/fonts.css` + `src/assets/fonts/`, SIL OFL 1.1 แนบ `OFL.txt`) ทับโทเคน `--font-thai`/`--font-num`/`--d-num` ในส่วนที่ 2 ของ `index.css` และ `DFONT` ของกราฟ · ฟอนต์นี้มีแค่ 400/700/800 และ**ตัวเลขกว้างไม่เท่ากัน (ไม่มี tnum)** `tabular-nums` จึงไม่มีผล · ไม่มีสัญลักษณ์ ↻ ↺ → ▾ (มาจาก Noto Sans Thai ที่ยังโหลดจาก Google Fonts)

**หน้าแดชบอร์ดทุกหน้า (`view: "dash"`) ใช้การ์ดหัว `lib/ui/DashShell.tsx`** (ดีไซน์ 1A) — App ไม่วาดแถบตำแหน่ง/แถบเตือนข้อมูลตัวอย่าง/หัวเรื่องให้หน้าพวกนี้ แต่ส่งผ่าน `DashPageContext` แทน · แถวตัวกรองของแท็บต้องห่อด้วย `FilterBar` (portal ขึ้นไปวางข้างแท็บในหัว) ไม่ใช่ `div.dz-filters` · `DashShell` ต้องครอบทั้งหัวและเนื้อหา ไม่งั้น portal หาช่องไม่เจอ

**ป้าย "ข้อมูลตัวอย่าง" ต้องเป็นของชุดที่ส่วนนั้นใช้จริง** (เจ้าของงานสั่ง 24 ก.ย. 2569) — แต่ละชุด (costrev · loadfactor · alloc · debtors)
เลือก real/sample แยกกัน วางไฟล์จริงไม่ครบชุดแล้วหน้าเดียวปนกันได้ · **แท็บที่อ่านชุดของตัวเองทับหัวด้วย `useShellSource()`**
(`lib/ui/dashContext.ts` — แท็บต้นทุนที่จม · ตัน-กม. ผ่าน `lfShellSource()`) ข้อความเป็นสตริงล้วนเพราะใช้เป็นคีย์ของ effect ·
**ส่วนย่อยที่อ่านคนละชุดกับหัวใช้ `lib/ui/SourceTag.tsx`** (การ์ด LF ใน Demo ข้อ 2 · การ์ดตัน-กม. ใน Demo · สองส่วนของกำไรลูกค้า)
ขึ้นเฉพาะเมื่อชุดไม่ตรงกับป้ายที่หัว ทั้งหน้าจริงหมดจึงไม่มีป้ายเพิ่ม · Demo เป็นหน้ายาวแล้ว หัวจึงเป็นของไฟล์ต้นทุนเสมอ
**ช่วงข้อมูล + ข้อจำกัดของไฟล์ลูกหนี้อยู่ที่หัวส่วน DSO** (`coverageLimit` ใน `OverdueSection.tsx` — ไฟล์จริงมีเฉพาะปี 2569
ยังไม่ครบปี) คิดจากข้อมูลทุกครั้ง ห้ามเขียนปี/เดือนตายตัว

**`normal` กับ `sheetTotal` ไม่สมมาตรกัน — ตั้งใจ ไม่ใช่บั๊ก**

```
normal     = แก๊ส + น้ำมัน + ค่าแรง + ค่าธรรมเนียม + ค่าซ่อม     (ไม่รวมสูญเปล่า)
sheetTotal = แก๊ส + น้ำมัน + ค่าแรง + ค่าธรรมเนียม + สูญเปล่า   (ไม่รวมค่าซ่อม)
```

`sheetTotal` มีไว้ให้ตรงกับคอลัมน์ 9–28 ของชีตเดิมเท่านั้น อย่า "แก้ให้เหมือนกัน"

**`GS_VERSION` = 18** (v17 เพิ่มแท็บ "บิลรอจัดรถ" + action `loadBills`/`pendingBills` · v18 เพิ่มคอลัมน์หางพ่วง 3 ช่อง) — แก้ `Code.gs` แล้วต้อง Deploy ใหม่เสมอ ·
**คอลัมน์ใหม่ของแท็บ "ข้อมูลใหม่" ต้องต่อท้าย `_DATA` ห้ามแทรกก่อน** — `getSheet_` เขียนแค่หัวใหม่ ข้อมูลแถวเดิมไม่ขยับตาม
ถ้า `_DATA` เลื่อนจากคอลัมน์ 61 ใบที่บันทึกไปแล้วทุกใบจะหายจากแอป (`recordToRow` จึงส่ง 64 ช่อง โดยช่อง 61 ว่างให้ Apps Script เติม JSON)

**`Content-Type` ต้องเป็น `text/plain;charset=utf-8`** ถ้าเปลี่ยนเป็น `application/json` เบราว์เซอร์จะยิง CORS preflight ซึ่ง Apps Script ไม่มี `doOptions` รองรับ แล้วพังทันที · `redirect: "follow"` ก็จำเป็นเพราะ `/exec` ตอบ 302 ก่อนเสมอ · `GS_VERSION` ใน `lib/sheet/client.ts` ต้องตรงกับ `apps-script/Code.gs`

**แกนกราฟต้องเขียน `<XAxis>` `<YAxis>` `<CartesianGrid>` ตรง ๆ เป็นลูกของกราฟ** Recharts หาลูกจาก `displayName` ถ้าห่อไว้ในคอมโพเนนต์ของเราเอง แกนกับกริดจะหายทั้งใบโดยไม่มี error — ห่อ**ทั้งกราฟ**ได้ (`lib/chart/dcharts.tsx` ทำแบบนั้น) แต่ห่อลูกไม่ได้

**ตัวเลขนับขึ้นในการ์ดแดชบอร์ด (`useCountUp` ใน `lib/chart/dashfx.ts`) ห้ามจำค่าจริงไว้เอง** ต้องอ่านจาก `data-real` ที่ React วาดใหม่ทุกครั้ง และ `.v` ต้องมี `key={ค่า}` · ของเดิมเก็บค่าแรกไว้ใน `dataset` แล้วเขียนทับกลับทุกครั้งที่เปลี่ยนตัวกรอง ทุกปีจึงแสดงเท่ากับ "ทุกปี" โดยไม่มี error — และเพราะ `textContent` ถอด text node ของ React ออก ถ้าไม่มี `key` React จะอัปเดตโหนดที่หลุดไปแล้ว ตัวเลขก็ค้างอีกทาง

**หน้าที่แยกก้อน (code-split) ต้องใช้ `lazyPage()` จาก `lib/ui/lazyPage.ts` ห้ามใช้ `lazy()` ของ React ตรง ๆ**
Vite ตั้งชื่อก้อนย่อยด้วย hash พอ deploy ใหม่ชื่อเปลี่ยนและไฟล์เก่าถูกลบ แท็บที่เปิดค้างไว้ยังถือ `index.html` เก่า
กดเข้าหน้าที่ยังไม่เคยโหลด → 404 → "Failed to fetch dynamically imported module" (ไม่ใช่ข้อมูลหาย — เรื่องแคชล้วน ๆ)
· **`location.reload()` เฉย ๆ ไม่พอ** GitHub Pages ส่ง `index.html` มาพร้อม `Cache-Control: max-age=600`
รีโหลดธรรมดาอาจได้ HTML เก่าจากแคชกลับมาอีกจนครบสิบนาที ต้องต่อ `?v=<เวลา>` ให้เป็นคนละคีย์แคช
· **ตัวกันลูปใน `reloadFresh()` ห้ามตัดออก** (sessionStorage `staleChunkReloadAt`) ไม่งั้นเน็ตหลุดทีเดียว
หน้าเว็บจะรีโหลดตัวเองไม่หยุด · `isStaleChunkError()` ต้องจับแคบ ๆ เฉพาะข้อความของก้อนหาย ถ้าจับกว้างไปบั๊กจริงจะถูกกลบ
· รีโหลดแล้วต้องกลับมาที่เดิม — `role` เก็บใน **sessionStorage** (`lib/store/sessionRole.ts`) ดูหัวข้อถัดไป

**ตำแหน่งที่เลือก (`role`) จำระดับ "แท็บ" เท่านั้น — sessionStorage ห้ามย้ายไป localStorage**
(`lib/store/sessionRole.ts` · เจ้าของงานเคาะ 23 ก.ย. 2569) เดิมไม่จำเลย เปิดเว็บทีต้องเลือกใหม่ทุกครั้งตาม main:2129
แต่ `lazyPage` รีโหลดหน้าเองได้เมื่อมี deploy ทับ ถ้าไม่จำผู้ใช้จะถูกเด้งกลับหน้าเลือกตำแหน่งกลางคัน ·
sessionStorage ตายตอนปิดแท็บ **"เปิดเว็บใหม่ต้องเลือกตำแหน่งใหม่" จึงยังจริงอยู่** ถ้าย้ายไป localStorage จะผิดดีไซน์เดิม ·
ค่าที่อ่านกลับมาต้องผ่าน `parseRole()` (เช็คด้วย `hasOwnProperty` ไม่ใช่ `in` — `"constructor" in ROLES` เป็น true)
ไม่งั้น `ROLES[role]` เป็น undefined แล้วแอปพังตั้งแต่ render แรก · ปุ่ม "เปลี่ยนหน้าที่" ต้องล้างค่าทิ้งด้วย ·
**`restoredRole` ใน `App.tsx` คือตัวแยกว่า "กู้มาจากรีโหลด" หรือ "ผู้ใช้เพิ่งเลือก"** — กู้มา = อยู่หน้าเดิมตาม hash
เพิ่งเลือก = เด้งไปหน้าแรกของตำแหน่งนั้นเสมอตาม main:2241 ถ้าไม่แยก สลับตำแหน่งแล้วจะค้างหน้าเดิมของตำแหน่งก่อน

**วันที่ต้องใช้ `todayISO()` จาก `lib/record/date.ts` เสมอ** ห้ามใช้ `new Date().toISOString().slice(0,10)` เพราะเป็น UTC — ผู้ใช้ในไทย (UTC+7) ที่เปิดแอปก่อนเจ็ดโมงเช้าจะได้วันที่ของเมื่อวาน

## branch และ deploy

`main` กับ `โมเดล-Anda` **แยกกันคนละสายโดยตั้งใจ** — `main` คือไฟล์ HTML เดี่ยวเวอร์ชันเก่า ใช้เป็น**ต้นแบบดีไซน์อ่านอย่างเดียว** (`git show origin/main:index.html`) **ห้าม merge เข้ามา**

push ขึ้น `โมเดล-Anda` = deploy ขึ้น GitHub Pages อัตโนมัติ workflow บังคับ `VITE_DATASET=sample` เสมอ และ fail ทันทีถ้าเจอไฟล์ใน `app/public/data/real/`

`base: "/fleet-cost-model/"` ใน `app/vite.config.ts` ต้องตรงกับชื่อ repo ตั้งผิดแล้ว asset 404 ทั้งหน้า

## ห้าม commit

repo นี้เป็น **public** — `.gitignore` กัน `*.xlsx`, `etl/data/`, `app/public/data/real/` ไว้แล้ว ทั้งหมดมาจากข้อมูลลูกค้าจริง · ลิงก์ `/exec` ให้กรอกในแอปตอนใช้งาน เก็บใน localStorage เท่านั้น

ข้อยกเว้นคือ `app/public/custmap.bin` — **commit ขึ้น repo แล้ว** เจ้าของข้อมูลยืนยันว่ารหัสลูกค้าที่ hash ไว้ไม่ใช่ความลับ และไฟล์นี้ต้องขึ้นไปด้วย ไม่งั้นหน้าค้นรหัสใช้ไม่ได้บน GitHub Pages · ส่วนไฟล์ต้นทาง `แปลงรหัสลูกหนี้รวม.xlsx` ยังไม่ขึ้น เพราะแอปไม่ต้องใช้ และกฎ `*.xlsx` ยังต้องกันไฟล์บิลจริงอยู่
